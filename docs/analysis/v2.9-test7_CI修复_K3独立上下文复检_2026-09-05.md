# v2.9-test7 CI 修复 K3 独立上下文复检报告（2026-09-05）

## 〇、自检声明

- 身份：本轮 CI 修复的正式独立复检代理，未承担本轮任何实现职责。
- 自身 request/context/descriptor 可见信息：系统提示自述「coding agent powered by the k3 model」——**model：k3**（来自系统提示自述，非 descriptor 三路互证）；**provider：无法核实**（我的可见上下文中未暴露路由 descriptor）；**reasoning effort：无法核实**（我的可见上下文中未暴露）。
- 环境：DSH Windows workspace-write 沙箱，批准提示禁用（无法提权）；本机 Python 3.10.9、Git bash 存在但沙箱内无法启动（见「核对记录 4」）。

## 一、实跑／核对记录

| # | 操作 | 结果 |
|---|------|------|
| 1 | `git status --porcelain` | 恰 2 个修改文件：`.github/workflows/validate.yml`、`docs/testing/v2.9-test7_实机回归清单.md`，无其他改动 ✓ |
| 2 | `git diff` 全文 | 与待审描述逐字一致：workflow 末步改版本感知；清单头行与门禁行 5 撤销字面 SHA 回填 ✓ |
| 3 | `python tools/build_release.py` 独立重建 | 通过（0 错误 0 警告，34 文件清单验证通过），**SHA-256 = `11e8f23e1f0e22b83328bf5cea684ee94d0c5daa093b8aef1f881e87bd9d404d`**，与 dist 现存 zip、`SHA256.txt` 三者一致（复跑确定性实证）✓ |
| 4 | Git bash 实跑 `dist/ci_sim.sh` | **无法执行**：MSYS2 启动即报 `couldn't create signal pipe, Win32 error 5`（沙箱禁止命名管道；批准提示禁用不可提权）。以 Python 语义仿真＋记录 3 的字面重建替代，见下 |
| 5 | Python 语义仿真 bash 步骤 | `grep -m1 -oE 'version="[^"]+"'` 首个匹配行 = descriptor.mod:1，提取 `2.9-test7`；build_release.py 锚定正则提取同为 `2.9-test7`；`*test*` 匹配 → 测试版；glob `dist/*_v2.9-test7_测试版.zip` 命中现存 zip ✓ |
| 6 | case 口径对照表 | `2.9`→两者皆正式版；`2.9-test7`→两者皆测试版；`2.9-TEST1`→bash=正式版／Python casefold=测试版（**分叉，仅大写场景**，见观察 2）|
| 7 | `Get-FileHash` vs `SHA256.txt` | 均为 `11e8f23e…9d404d` ✓ |
| 8 | PyYAML 解析 validate.yml | 解析通过；7 个 step；末步 name=验证重复构建结果一致、shell=bash ✓ |
| 9 | `git show 495deef:.github/workflows/validate.yml` | 确认失败时末步两处写死 `dist/*_正式版.zip`——失败根因属实 |
| 10 | `git show 495deef:…回归清单.md` | 当时头行为「以包外 SHA256.txt 为准（构建后回填）」、门禁行 5 结果空缺「打包时回填」；现修复版措辞有意改写（解释句＋结果回填），故现哈希 11e8f23e ≠ 回填前 750173b3 属预期（内容本就不逐字相同）|
| 11 | `git check-ignore dist/ci_sim.sh` | exit 0，被忽略，不会入库 ✓ |
| 12 | glob `.github/workflows/*` | 仅 validate.yml 一个 workflow，无其他 CI 面 |
| 13 | grep `vanilla`：build_release.py / validate_mod.py | 均无匹配；build_release 全流程不依赖 vanilla 路径 ✓ |

## 二、逐项判定

### A. workflow 语法与 bash 正确性 —— **通过**

- YAML：PyYAML 解析通过，7 steps 结构完整，末步 `shell: bash` 保留（validate.yml:24-35）；diff 仅触及末步 run 块与新增 3 行注释（validate.yml:26-28）。
- 版本提取：`grep -m1 -oE 'version="[^"]+"' descriptor.mod | cut -d'"' -f2`（validate.yml:30）对 descriptor.mod:1 `version="2.9-test7"` 提取得 `2.9-test7`（仿真记录 5）；与 build_release.py:38 锚定正则结果一致。
- case：`case "$version" in *test*) …`（validate.yml:31）POSIX 合法，当前版本判「测试版」正确。
- glob：测试版 `dist/*_v2.9-test7_测试版.zip` 命中构建器实际产物 `开局一键爽玩_v2.9-test7_测试版.zip`（build_release.py:223 `f"开局一键爽玩_v{version}_{package_label}.zip"`，前缀由 `*` 吸收）；正式版路径 `version="2.9"` → `dist/*_v2.9_正式版.zip` 与同式生成名逐字吻合 ✓。
- glob 带版本号可防 dist 残留他版本包多行哈希干扰（注释 validate.yml:28 所述属实）；构建器写包前先 unlink 同名 zip（build_release.py:270-271），同版本不可能双份。
- 空 glob 时 sha256sum 报错→GHA bash `-eo pipefail` 默认使步骤失败，属「响亮失败」语义，正确。

### B. 与 build_release.is_test_version 语义一致性 —— **通过（附观察 2）**

- 构建器：`"test" in version.casefold()`（build_release.py:44-46），大小写不敏感子串。
- workflow：bash `*test*` glob，大小写敏感子串。
- 对全部现行与可预见小写版本号（`2.9-test7`、`2.9`）两者同口径（记录 6 对照表）。仅当版本号含大写 `TEST` 时分叉，且分叉后果为 glob 失配→CI 响亮失败，非静默错误。现行版本线全小写，不阻断。

### C. 无副作用 —— **通过（附观察 3）**

- `git status --porcelain` 恰 2 文件，无其他非授权改动；workflow 其余 4 步（validate_mod/unittest/MIO --check --no-vanilla/build_release）diff 未触及。
- `dist/ci_sim.sh` 为仿真临时文件：`git check-ignore` 确认 dist/ 不入库；构建器自临时目录 staging 打包（build_release.py:234-249），dist 内杂项永不进包。建议事后清理，不阻断。

### D. 清单修正与 test6 先例、包外哈希一致性 —— **通过**

- test6 先例（docs/testing/v2.9-test6_实机回归清单.md:4）：「以包外 `dist\…_SHA256.txt` 为准」。test7 现行头行（v2.9-test7_实机回归清单.md:4）同口径并附原因注记（清单随包打包、字面回填即失效）——该注记属实：package_docs 把 docs/testing 内版本匹配文档打进包（build_release.py:49-56、51），包内字面 SHA 必自引用失效。
- 门禁行 5（:99）同步改回「以包外 SHA256.txt 为准」✓。
- 哈希三方一致：SHA256.txt = Get-FileHash 实测 = 复检独立重建输出 = `11e8f23e…9d404d`（记录 3、7）；重建逐比特复现，确定性实证 ✓。

### E. CI 遗漏面 —— **通过**

- 逐行读全 workflow（仅 35 行）：「正式版/测试版」仅出现于 26、27（注释）、31（case）；其余 4 步均版本无关。无其他写死位置。
- build_release.py 无 vanilla 依赖（grep 无匹配）；其内部调 validate_mod.py（:212-218）继承 stdio，本次本机重建全程跑通即实证。MIO 步骤已带 `--no-vanilla`（validate.yml:21）。
- 未来正式版推送路径：`verify_tagged_release_payload` 对正式版跑 git（build_release.py:72-108），但 GHA `checkout@v4` 默认浅克隆无本地标签 → `rev-parse` 非零 → 静默返回（:79-80），不会误 fail；若改全量克隆且标签存在，漂移拦截属设计意图。测试版在 :69-70 直接 return，不触 git。均无痛点。

## 三、非阻断观察（不修改本轮 diff 亦可合并）

1. **grep 未锚定行首**：`supported_version="1.19.*"`（descriptor.mod:7）同样含 `version="…"` 子串；当前因 version= 居第 1 行且 `-m1` 先中而安全。若日后行序变更将提取 `1.19.*` 致 glob 失配（响亮失败）。可选用 `'^version="[^"]+"'` 加固，与 build_release.py:38 锚定口径对齐。
2. **大小写口径微差**：bash `*test*` 大小写敏感 vs 构建器 casefold（build_release.py:46）。大写 `TEST` 时分叉且为响亮失败；版本线小写约定下无实际风险。可选用 `[Tt][Ee][Ss][Tt]` 对齐。
3. **`dist/ci_sim.sh` 残留**：不入库、不进包，但按「dist/ 只由构建器生成」的红线精神建议本轮收尾时删除。
4. **验证方法限制声明**：沙箱内 MSYS2 bash 无法启动（信号管道 Win32 error 5，提权被禁用），本复检未能字面重跑 bash 步骤；以「ci_sim.sh 与 workflow 步骤逐字同逻辑核对＋Python 语义仿真＋字面重建确定性复跑」三重替代，结论置信度充分。

## 四、总结论

**通过。** 两处改动均正确、最小且对口根因；A–E 五项全部通过，无「需修改」项。3 条非阻断观察（grep 行首锚定、大小写口径、ci_sim.sh 清理）供后续可选加固，不影响本轮合并与推送。
