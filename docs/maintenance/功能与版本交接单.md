# 功能与版本交接单

> 总入口与状态快照见根目录 `AGENTS.md`；本文档记录功能边界、版本演进和后续修改纪律。最近更新：2026-08-02（v2.6正式包已构建并上传工坊）。

## 一、项目边界

本项目是独立MOD《开局一键爽玩 / One-Click Sandbox Start》，不得与《钢四人物小传》或《一键建设海军》混用版本号、源码、决议、事件、测试结论或工坊材料。

## 二、当前版本状态

- v1.2c：PRC专用稳定历史基准，已上传Steam工坊；保留作回退和技术参考。
- v2.0a：通用架构阶段，德国实测无报错。
- v2.0b：装备、库存和编制通用化阶段，德国实测通过。
- v2.0c：建设通用化初版，存在`Multiple limits in target effect`错误，已作废。
- v2.0c1：修复重复limit，并将非PRC民兵模板改为普通步兵；用户验证无误。
- v2.0c2：新增24个满编主力纯步师；用户验证无误并明确认定为稳定版。
- v2.0：由v2.0c2仅清理测试标识形成的正式发布版。
- v2.1：在v2.0之上新增五类特殊科研项目一键完成决议（需《Gotterdammerung》DLC），并直接授予衍生科技与原型互斥选择科技、核能决议在首都补建核反应堆；经维护者实机验证通过并上传Steam工坊（2026-07），曾是历史稳定基准。
- v2.2：在v2.1之上新增PRC特殊科研后二阶段编制与最终设计，并将一键24师改为可重复执行；维护者在中文环境完成实机回归并确认通过，2026-07-30 已上传Steam工坊既有物品 `3767025052`（Manifest `6814589756424432401`），曾是历史稳定基准。
- v2.3：完成全国家通用MIO路线、通用进化编制、中自火纠正、每国一次性标志与跨国家切换修复；2026-07-31 上传Steam工坊既有物品 `3767025052`（Manifest `7223939852636260892`），曾是历史稳定基准。
- v2.4：继承v2.3的修复集（雷达科技归位空军特殊科研、进化装备设计与库存同步、船坞/民用工厂可重复点至上限、全地图碉堡收敛、点数/经验剥离为独立决议、本地部署加载结构修复）；v2.4-test1实机回归通过、正式包已构建、Git标签`v2.4`已推送，并于2026-08-01上传Steam工坊既有物品 `3767025052`（Manifest `385131838239799795`），曾是历史稳定基准。
- v2.5：在v2.4修复集正式化之上新增间谍情报条线（需《La Resistance》DLC）——"一键组建间谍机构"（`PRC_OCS_create_intelligence_agency`）与"一键升满机构部门"（`PRC_OCS_unlock_all_agency_upgrades`）两个决议，0成本、无科技国策前置、无等待，每国独立一次、无DLC不可见；维护者已于2026-08-01实机点验通过（四份日志干净），Git标签`v2.5`已推送，并于2026-08-01上传Steam工坊既有物品 `3767025052`（Manifest `7423542268120748938`），曾是历史稳定基准。
- v2.6：在v2.5稳定基准之上完成特殊科研原型奖励·逐项选择版 26 组扩展——按 reward 独立生成事件 id/flag（同 project 多组互不遮蔽，映射见 `docs/analysis/v2.6_特殊科研组事件映射.json`），组事件选项末返回本专精菜单（48/49/50/51），新增陆/核"选择原型奖励"决议、空军/海军主菜单 `.e` 续入口，菜单 z 设专精完成 flag 隐藏决议；数值全部取自原版；维护者已于2026-08-02实机点验通过（v2.6-test2，四份日志干净、30 个选择事件全加载），正式包已在单位机同一工作区构建两次且 SHA-256 均为 `484030065363ab1366604d17e2f92ee114208b2edb7ffe13b001a0172de48ff1`、Git标签`v2.6`（附注标签对象 `1ff3aac`，目标提交 `99e7c60`）已推送，并于2026-08-02上传Steam工坊既有物品 `3767025052`（Manifest `3609146315690391433`），为当前稳定基准。发布辅助工具同步：`tools/deploy_to_local_mod.ps1`（本地部署）、`tools/publish_workshop.py`（工坊简介 ASCII 引号改弯引号并压缩至 4832 字符后方可上传）。

仓库标签补充说明：旧 `v2.0` 标签创建在源码导入前，标签树中只有 `README.md`；真正的 v2.0 源码位于提交 `9593154`，使用补充标签 `v2.0-source-baseline`。旧标签不强制移动，以免破坏外部引用。

## 三、v2.0通用功能（v2.1 沿用）

所有非AI玩家国家均可使用：

1. 一键完成全部科技、预设学说和军种精神。
2. 强化当前国家已有军工机构的等级与资金。
3. 创建22个装备设计；PRC使用中国型号，其他国家使用中性名称。
4. 创建8个师级编制；PRC保留原民兵结构，其他国家使用普通步兵。
5. 给予巨量陆空装备、变体与战略库存，并可重复补充。
6. 独立决议将全部现役师统一改编为“卫戍部队”；初始化本身不自动改编。
7. 一次性组建24个100%人力、装备和训练度的“主力纯步师”。
8. 完全解锁本国拥有且控制州的共享建设槽至25。
9. 全国石化与储油体系、沿海船坞、民用工业。
10. 全国交通与基地、全国防御工事。

## 四、PRC增强层

PRC或`original_tag = PRC`额外保留：

- 10名顾问、理论家和军事高层；
- 四家指定军工机构及特质路线；
- 中国型号名称；
- 民兵版模板；
- 23座固定战略补给站；
- 有油州高容量工业电网、无油州强化电网。

其他国家绝不获得PRC人物、PRC军工机构特质、中国固定补给站或任何电网。

## 五、建设作用域

通用建设只处理“当前国家拥有且控制”的州，不处理临时占领的敌国州。合法拥有并控制的殖民地会被处理。铁路仍不自动建设。

## 六、优先级

共享槽位建设优先级仍为：

`合成炼油厂 → 储油罐 → 海军船坞 → 民用工厂 → PRC电网`

其他国家没有最后的电网阶段。

## 七、v2.1新增：特殊科研项目

- 五类专精（空军含雷达、陆军、海军、核能、火箭）各一个决议，一键完成全部 49 个特殊科研项目，奖励与正常研究一致；实现细节见 `技术实现与通用化边界.md` 第九节。
- 直接授予 57 项衍生科技与原型迭代互斥选择科技（含离心法同位素分离、海军补给与维修方案）。
- 核能决议在首都补建 1 座核反应堆，替代正常研究时设施州自动获得的反应堆。
- 决议可重复执行：跨专精前置项目被跳过时，完成其他专精后再次点击即可补齐；全部完成后决议自动消失。

## 八、后续修改纪律

- 从当前仓库（v2.1）完整源码复制后修改，不在正式包上直接试验。
- 不改变AI、原版国策、原版国家历史和事件链。
- 测试包必须有版本号、README、SHA-256和测试清单。
- CI 必须同时通过结构化静态检查、正式包构建和重复构建哈希一致性检查。
- 必须检查`error.log`、`game.log`、`setup.log`和非空的`text.log`。
- 正式标签最后创建，必须指向可直接重建该正式包的提交。
- 用户明确确认稳定后，才能形成新的正式基准。

## 九、v2.2

- PRC新增一次性“进化特殊科研陆军编制”，精确检查喷火坦克、军用工程车、直升机、洲际轰炸机、空天母舰、轴流式喷气引擎6个项目及9项相关科技，不捆绑全部49个项目。
- 新建4个独立二阶段模板，不覆盖原模板、不自动改编现役师。
- 新司令部使用目标兵牌0和原版总部本地化键；主力步坦师使用目标兵牌104。目标存档未手调装甲师和海军陆战队装甲师兵牌，因此两者保持引擎默认，不得补写臆测值。
- 基础初始化移除“43式喷火预备”；二阶段创建最终“中型喷火坦克 III型”、全5“改进型履带登陆车”、4个角色不同的全5“装甲支援车”和4种全5特殊轰炸机。
- 4种飞机在人民航空公司军工机构存在时自动选用该机构；PRC后继身份缺少该机构时仍创建同规格无MIO设计。新飞机和登陆车会退役旧装备设计。
- 一键24师改为可重复执行，冷却1天。
- 8个原版模板不自动清理。原版 UI 确有手动退役/复役功能，但 1.19.2 自带 effect 文档没有模板退役 effect，二进制存档字段也尚未确认；`delete_unit_template_and_units` 会连使用模板的现役师一起删除，禁止用于本功能。
- 已接受限制：基础初始化“装甲师”仍为8机械化＋10中型自行火炮。维护者在 `test2` 实测后接受当前版本保留该结构，并要求后续版本改为10机械化＋8中型自行火炮；不要把这个待办套到12＋6的“装甲师·进化型”。
- v2.2 Steam上传只将简介中的中英文稳定版本号从v2.1同步为v2.2，其余简介正文不改，且不提交工坊 `changenote`。
- 维护者已确认完整测试项目通过；测试过程与已接受限制见 `docs/testing/v2.2-test1_实机回归清单.md`。

## v2.3版本演进

- v2.3-test1—test4：按维护者存档将基础装甲师定稿为12机械化＋6中型自行火炮，纠正主力步坦师及进化型中的轻型自行火炮，并向所有非AI玩家国家开放进化编制。
- v2.3-test5—test10：从德国、英国、日本、苏联、澳大利亚、捷克斯洛伐克、意大利和美国存档整理MIO目标路线；确认不应额外增加MIO资金或等级，方针继续由玩家手动选择。
- v2.3-test11：生成器维护全国家通用MIO表，共444家公司、4945项目标特质；先确认本国实际拥有，再直接进入对应 `mio:` 作用域，不使用国家tag分支。
- v2.3-test12—test13：确认 `fire_only_once = no` 不能解决分类未实例化；分类改为 `allowed = { always = yes }`，仍由决议AI守卫阻止AI执行。维护者实机确认同局切换国家后分类正常出现。
- v2.3：维护者确认测试通过；2026-07-31上传Steam工坊既有物品 `3767025052`（Manifest `7223939852636260892`），成为当前稳定基准。
## v2.3稳定功能补充

- 通用初始化只处理当前国家实际拥有的MIO；目标特质自身按原版规则同步等级，不额外注入资金或等级。
- 初始化、统一改编、进化编制和五类特殊科研均由国家标志控制每国一次；完成后对该国隐藏。
- 分类开局为所有国家实例化，但分类及全部决议只对非AI玩家显示和开放。
- 通用国家使用普通步兵、中性装备名称；PRC或 `original_tag = PRC` 继续使用民兵、中国型号及专属增强层。
- 基础装甲师为12机械化＋6中型自行火炮；主力步坦师及其进化型均不再含轻型自行火炮。

## v2.4版本演进

- v2.4-test1：完成6项修复的回归验证。雷达科技1–5级在空军特殊科研后全亮；进化装备执行进化编制后设计与库存同时出现；船坞/民用工厂可重复点至上限20；陆地不再获得五级碉堡；新增可重复点数决议 `PRC_OCS_add_points`；修复本地部署加载结构。
- 维护者实机逐项点验未发现问题；四份日志干净（`error.log`仅原版网络噪音、`game.log`仅原版Conflict Risk杂音、`setup.log`正常加载PRC_OCS分类#1/决议#17/事件#13、`text.log` 0字节）。
- v2.4正式包由 `tools/build_release.py` 两次构建，SHA-256 一致（`b65afcb8b685c01aeb554736f11927115329029a8590eaf3cc0f1945ec203aa1`）；Git标签与工坊上传在维护者确认后按门禁完成。
- 测试包构建中发现并修复检查器回归测试的断言缺陷：`test_evolution_dependency_and_variant_contract` 用子串计数误把 `variant_name = "装甲支援车"` 计入设计数，改为解析式统计 `create_equipment_variant` 块的 `name` 标量。
- 已成立待办继续继承：潜艇MIO全局隐蔽线（涉及约467K共享MIO文件重构）、特殊研发科技强度优化（`complete_special_project` 引擎限制）。

## v2.5版本演进

- v2.5：新增间谍情报条线（需《La Resistance》DLC）——"一键组建间谍机构"（`PRC_OCS_create_intelligence_agency`）与"一键升满机构部门"（`PRC_OCS_unlock_all_agency_upgrades`）两个决议：0 成本、无需科技与国策前置、无等待，五大部门（情报/防御/行动/特工训练/密码破译）全部拉满，每国独立一次、无DLC不可见。
- 实现要点：沿用引擎内建命令 `create_intelligence_agency = yes` 与 32 条 `upgrade_intelligence_agency` 调用（重复次数=等级数），并挂 `agency_upgrade_time = -1.0` 隐藏修正免等待；`allowed` 块不写 `is_ai`、无 `has_decision` 死引用，规避参考 MOD 的两类日志杂音。
- 维护者于2026-08-01实机点验通过（机构创建成功、部门全部满级）：`error.log` 仅原版网络噪音、`game.log` 仅原版Conflict Risk杂音、`setup.log` 正常加载 PRC_OCS 且无参考 MOD 痕迹、`text.log` 0字节。
- v2.5正式包由 `tools/build_release.py` 两次构建，SHA-256 一致（`4bb5a8ef72d08310a93259082789eb4cc3fcf8887e1a776bc1d58ef71c470c80`）。
- 2026-08-01：Git标签 `v2.5` 已推送；GitHub Release v2.5 挂正式包 ZIP 附件（ASCII 文件名 `OCS_one_click_sandbox_start_v2.5.zip`）；Steam 工坊物品 `3767025052` 已更新至 v2.5（Manifest `7423542268120748938`）。`tools/publish_workshop.py` 已同步升级到 v2.5 指向（简介与 changenote）。
- 待办销项："间谍机构没有研发"已在 v2.5 完成，从待办中移除。

## v2.6版本演进

- v2.6 核心：特殊科研原型奖励·逐项选择版 26 组扩展——空军 4（地震炸弹/直升机/洲际轰炸机/空天母舰）、陆军 3（要塞网络·混凝土加固一/二/通信改造）、海军 17（护航航母 A/B、冰航母、袖珍潜艇、现代战列舰 A/B、现代航母 A/B、核导弹潜艇、核潜艇、导弹潜艇、潜水母舰、超级战列舰 A/B、支援船·支援/维修方案、补给船）、核能 2（核同位素分离/反应堆测试完成）；效果数值全部取自原版 `unique_prototype_rewards`，未发明新数值。
- 修复生成器按 project 去重缺陷：`tools/generate_special_project_choice_events.py` 按 reward 生成唯一事件 id 与唯一 country flag（flag 含 reward 后缀），同 project 多组（要塞网络 3 组、海军 `_a/_b` 组、核反应堆 2 组）互不遮蔽；权威映射 `docs/analysis/v2.6_特殊科研组事件映射.json`。
- 交互接线：组事件选项末自动返回本专精菜单（48/49/50/51）；新增陆/核"选择原型奖励"决议；空军/海军主菜单 14/18 末尾 `.e` 续入口跳 50/51；菜单 z 选项 AND 覆盖该专精全部 reward flag 后设专精完成 flag，对应决议隐藏。
- 本地化：`tools/finish_choice_events.py` 幂等生成四菜单与双语键（含 46/47 标题错位修复：同位素分离/反应堆测试完成各归其位）；菜单选项字母跳过 `d`（保留描述键占用）。
- 验证：`validate_mod.py` 23 决议 / 292 对本地化键 / 0 警告；`unittest` 21 项全过（含 26 组映射唯一性、菜单 z 契约、跨机器路径和发布暂存回归）；v2.6-test2 实机点验通过（`setup.log` 加载 30 个选择事件＝26 组＋4 菜单，精确吻合）。
- 发布：v2.6 正式包 SHA-256 `484030065363ab1366604d17e2f92ee114208b2edb7ffe13b001a0172de48ff1`（单位机同一工作区内两次构建一致）；2026-08-02：Git 标签 `v2.6`（附注标签对象 `1ff3aac`，目标提交 `99e7c60`）已推送、GitHub Release v2.6 挂正式包附件、Steam 工坊物品 `3767025052` 已更新至 v2.6（Manifest `3609146315690391433`）。
- 跨机器审计：官方附件与标签/家用机重建包的差异均来自文本换行；统一换行后内容一致。旧官方附件保留不替换；当前构建器规范化 LF 后的 v2.6 审计重建 SHA-256 为 `9b4cc601ca9c82e59541665a076f16c972dab9c18d6871677f4887e2df1e7467`。
- 工坊上传踩坑（已写入交接经验）：`+login` 后必须带账号名否则 steamcmd 打印 `Usage` 退出；工坊简介英文段 ASCII 引号会提前闭合 VDF description（报 `got } in key` / `key name too long`），一律改弯引号；提交阶段 `Invalid Parameter` 优先压缩英文简介长度（中文 2209 + 英文 2595、合计 4832 字符可通过）。
- 本地部署回归脚本 `tools/deploy_to_local_mod.ps1`：独立脚本代替超长内联 PowerShell，避免执行器输出吞没与批准中断问题。