# 实机监测机制 · 立项与调研（A 流程 ①–⑥）

> 立项时间：2026-08-19。流程依据：`docs/maintenance/维护与重构标准流程.md` A 全流程（新机制建设，走 A 六步）。
> 状态：①对齐需求（门一**已拍板**，结论见 1.4）→ ②调研（完成：外部调研＋本仓盘点＋本机官方文档实测三路）→ ③再次对齐（完成：方向＝统一监测体系·六模块）→ ④分步方案（已落盘，见第六节，门二**已拍板**，结论见 6.9）→ ⑤K3 复检（**七轮终审通过**，14 项需修改＋10 条附记全部落实，历程见第七节）→ ⑥小步实现（步骤1–5 完成，见第八节；diff 经 K3 分块审计：块1–5 已修毕并复核全绿，见第九节）→ 门三**已确认**（2026-08-19 机主拍板）→ git 提交 `4a978a5` 并已推送远端（推送仅凭「推送」二字）。2026-08-20 实机轮：首跑 50 用例全 PASS→两处机制修正→复跑 check_logs 判 **PASS**，归档 `docs/testing/v2.8-test5/`（第九节「实机轮修正」；修正的提交待门三）。
> 定位：把「需要进游戏实测」的验证环节，从纯人工点验升级为成熟、固定化、可留痕的监测机制。本批＝立项＋调研＋分步方案＋六步实施；不动 MOD 功能代码（自检套件为 debug 门控监测埋点）。

## 一、需求（门一：已拍板，结论见 1.4）

### 1.1 机主原话

> 「一键开局立项，现在，我们的监测的机制还不健全，特别是那种需要进游戏实测的，我认为应该有成熟的，固定化的监测机制，需要进行一波调研并吸纳进来。」

### 1.2 AI 对齐理解

- **问题**：现有监测机制不健全，最薄弱的是「进游戏实测」环节——该环节目前纯人工、无固定流程、证据多次断档、错误靠运气撞见（佐证见第二节）。
- **要做**：调研业界/社区成熟做法，形成**标准化、固定化**的实测监测机制，并**吸纳**进本项目（工具/流程/留痕入仓，与本仓既有门禁衔接）。
- **关键词**：监测机制＝发现问题的机制，重点覆盖静态门禁抓不到、必须实机才能暴露的缺陷；固定化＝步骤、工具、模板、留痕可重复可核对；成熟＝有业界或社区先例支撑，不另造轮子。
- **产出物预期**：调研报告＋分步方案＋落地工具/模板/文档（前三者已落盘，落地实施见第八节）。

### 1.3 边界（门一已确认）

1. 不改变 MOD 面向玩家的运行时行为：自检能力走 debug/console 通道，不影响正常游玩与「AI 守卫」等既有红线。
2. 不追求「全自动无人测试」：游戏 GUI 无法无头启动，实机仍需人点；本批目标是**把人点之外的环节全部固定化、自动化、留痕化**。
3. 本批不改任何游戏功能代码；新增工具/脚本按仓库门禁（validate/unittest/mio --check/build）验收。
4. 证据脱敏：日志含本机路径/用户名，入库前按 `docs/DEVELOPMENT.md` 既有口径脱敏。
5. 不复制已有能力：现有静态门禁、CI（`.github/workflows/validate.yml`）、部署脚本继续沿用，新机制只补「实机」空缺并与其衔接。

### 1.4 门一待拍板问题 → 门一结论（2026-08-19 机主拍板，问答原文见下）

1. **范围**：本批只做「进游戏实测监测」，还是把静态门禁＋实机监测＋证据归档合成一套统一监测体系？
2. **形态**：接受在 MOD 内新增 debug 门控的自检事件/效果（随发布包分发，但玩家不可见、AI 不可用）吗？还是监测能力必须全部放在 MOD 之外（工具/存档/日志解析）？
3. **自动化边界**：接受「维护者手动点验 + AI 自动判读、归档、出报告」的半自动分工吗（这是 `docs/DEVELOPMENT.md` 已授权分工的自然延伸）？
4. **证据强度**：是否要求每轮实机回归都做到「仓库内可独立复核」（日志/截图/自检输出入仓），废除「只记维护者结论」的做法？

**门一拍板结论（机主全选推荐项）**：

1. 范围＝**统一监测体系**：以实机监测为核心，把静态门禁、实机实测、证据归档串成一套固定机制，一处编排、统一留痕。
2. 形态＝**接受 MOD 内 debug 门控自检套件**：hidden 隐藏、玩家不可见、AI 不可用，随发布包分发；门控红线照旧。（实现口径细化见 6.9-2：`is_triggered_only`＋无玩家入口；事件级 `hidden` 待步骤0 核实。）
3. 自动化边界＝**半自动分工**：维护者手动点验＋一条 console 指令触发自检；AI 自动判读日志/存档、归档证据、出报告（`docs/DEVELOPMENT.md` 已授权分工的自然延伸）。
4. 证据强度＝**收紧为强制可复核**：每轮日志/截图/自检输出必须脱敏入仓，逐项结果必填，缺证据即不过关；废除「只记维护者结论」。

## 二、现状痛点（本仓盘点，2026-08-19 盘点子代理逐文件核实）

### 2.1 现有监测/验证机制一览

| 机制 | 类型 | 自动化 | 覆盖 | 不覆盖 |
|---|---|---|---|---|
| `tools/validate_mod.py` | 静态检查 | 全自动 | 文件存在性、descriptor↔packaging 一致、编码/BOM、本地化键对等、effect 重名、多 limit、AI 守卫、PRC 双 tag、受控州等红线 | effect 是否真实存在于引擎/作用域；DLC 分支极性；数值是否生效；文本渲染 |
| `tools/test_validate_mod.py` | 检查器自测＋31 条契约 | 全自动 | 解析器行为＋模板编制/MIO 计数/事件映射/菜单接线/骷髅师/B 系列修复契约 | 实机行为 |
| `generate_universal_mio_effect.py --check` | 生成产物一致性 | 全自动 | 共享 MIO 产物与生成器重建一致 | 游戏内 MIO 实际效果 |
| `tools/build_release.py` | 构建检查 | 全自动 | 确定性 ZIP、LF 统一、MANIFEST 复核、跨机字节级复现 | 游戏运行 |
| CI `.github/workflows/validate.yml` | 构建检查 | 全自动 | 三项检查＋构建＋两次构建哈希一致 | 无游戏环境，不可能实机 |
| `tools/deploy_to_local_mod.ps1` | 部署脚本 | 半自动 | robocopy 镜像、descriptor 核验、dlc_load 启用、VERIFY 报告 | 进游戏点验本身 |
| 维护者人工实机回归 | 人工实测 | 纯人工 | 逐项清单点验、四份日志、截图 | 留痕强制、可独立复核证据 |
| AI 日志判读 | 日志判读 | 半自动 | 白名单经验人脑执行 | 白名单未机器化 |
| K3 复检 / 用户反馈整理 | 半自动 | 半自动 | 方案与 diff 审计、反馈分档 | 不实跑门禁；依赖用户主动上报 |

一句话现状：**静态四门禁强、实机环节弱、留痕靠自觉**。

### 2.2 「进游戏实测」证据链断档实例（全部有出处）

- **v2.2**：只确认「主力步坦师·进化型」兵牌；司令部**尚未检查**，部分项不记「失败」（`测试状态与回归清单.md` 18 行）。
- **v2.6-test2**：回归清单逐项结果与证据位置**空白**，原始日志与截图**均未提交**，仅维护者结论＋摘要，明示「不能作为可独立复核证据」（同档 50 行）。
- **v2.7-test1**：当轮**未留存截图**，逐项结果留空不追填（同档 43 行）。
- **v2.8-test1**：清单结论段「是否通过/发现问题/日志判读」**三栏空白**（`docs/testing/v2.8-test1_实机回归清单.md` 53–57 行）。
- **v2.8-test3→4**：`error.log` 4 条 `Invalid effect 'enable_equipment_modules'`——静态门禁全绿但游戏报错，**错误靠实机日志才抓到**（`开发状态快照与交接.md` 186 行）。
- **日志「最新一轮」判定靠人**：读取工具可能返回缓存旧内容，须看文件时间戳（同档 248 行）。

### 2.3 静态门禁抓不到的错误类型（均有实例）

1. effect 有效性/作用域：`enable_equipment_modules` 仅存在于科技/特殊科研专用作用域，被误移植进事件——静态 parser 无法验证 effect 是否真实存在（HJ 186）。
2. DLC 分支极性：B3 的 OR 极性反了，K3 复检前两轮才查出（HJ 120）。
3. 标签与效果语义错位：B4 通用模板套错 42 组选项标签（HJ 195）。
4. 隐藏块遮蔽：B5 `tech_effect|` 嵌进 `hidden_effect` 后不可见（HJ 196）。
5. 遗漏奖励项：B1 漏 `upgrade_training_centers`、B6 漏石墨/重水组、Issue 5 generic 迭代奖励缺口——**全部靠用户反馈暴露**（HJ 4.4/4.8 节）。

### 2.4 日志判读的人工经验（噪音白名单现状）

白名单散落于 markdown 正文、无集中机器可读文件：

- `error.log` 0 字节＝干净；顶部 `gamelobby.cpp`/`remotefile.cpp`「Failed allocate data buffer」＝原版网络噪音（HJ 249）。
- `game.log`「Conflict Risk」＝原版杂音（DEV 60）。
- 第三方 MOD「大礼万/daliwan」`Invalid supported_version`＝他人杂音（HJ 239）。
- `text.log` 0 字节＝未发现文本错误；`setup.log` 需人工核对加载文件/决议数/事件数。

### 2.5 本机可用资产（实机核验存在）

- 游戏本体 `D:\SteamLibrary\steamapps\common\Hearts of Iron IV`（含官方 `documentation\` 全套文档）；`tools/hoi4_paths.py` 已支持环境变量＋已知路径探测。
- Paradox 用户目录 `C:\Users\Admin\Documents\Paradox Interactive\Hearts of Iron IV`（logs\、save games\、mod\）。
- 日志资产 23 个文件：除四份常查日志外还有 `system.log`、`system_debug.log`、`memory.log`、`graphics.log`、`code_revisions.log`、`executed_commands.log` 等（当前多为 0 字节、无利用先例）。
- 存档：`test1–test5.hoi4` 等二进制存档（约 48–68 MB），当前仅人工点验参考，无解析先例。
- **console/存档解析先例：无**。现有 `tools/analyze_*.py` 只解析原版定义文件，不解析存档。

### 2.6 缺口清单（固定化实机监测要补的）

- **工具类**：日志自动回收/判读脚本；机器可读噪音白名单；`.hoi4` 存档解析器（自动核对 MIO 特质/模板/数值）；console 驱动/回放利用；effect 有效性引擎级校验。
- **流程类**：实机回归固定触发/门禁脚本；日志时间戳判定与脱敏固化；「最新一轮日志」定位标准。
- **留痕类**：截图/原始日志/逐项结果入库强制化；「维护者结论」与「仓库可复核证据」自动区分校验；测试轮次归档目录约定与结构化产物。
- **自动化类**：进游戏点验与截图比对无法自动化（无操作回放/画面校验）；部署→启动→采集→判读全链路无编排脚本；回归结果无 JSON 结构化产物，无法 diff/趋势比对。

## 三、调研（A ②）

### 3.1 外部成熟机制调研（来源与证据强度见各条）

> 可靠性标注：★★★★ 官方文档/引擎实文件；★★★ 广泛使用工具/官方百科；★★ 社区实践/个人先例；★ 未查证传闻。本节为立项侧直接检索＋外部调研子代理报告合并，未查证项已在 4.7 如实登记。

#### 3.1.1 Paradox 官方与引擎自带

- **引擎自带场景断言测试框架**（★★★★）：游戏根目录 `tests\*.txt` 的 `success`/`fail` 触发器断言格式与 `scenario_testing.log` 通道，即 Paradox 自家「进游戏实测」的固定化机制（本机实文件已核，见 3.2 节第 12 条）。
- **console 测试原语**（★★★★）：`run`/`effect`/`event`/`testevent`/`savecheck`/`test_log`/`test_save_mode`/`debug` 均在游戏自带 `documentation\console_commands_documentation.md` 有正式条目（见 3.2 节）。社区维基同收：Hearts of Iron 4 Wiki [Console commands](https://hoi4.paradoxwikis.com/index.php?title=Console_commands&oldid=62089)（★★★）。
  - ⚠️ 证据冲突与裁定：社区 wiki 称「HOI4 没有 EU4 那种 `run <文件>`」（[wiki Effect 页](https://hoi4.paradoxwikis.com/index.php?title=Effect&diff=98402&oldid=24355)与 EU4 文档 [vawser/EU4-Documentation](https://github.com/vawser/EU4-Documentation/blob/master/effects.txt) 口径），但**本机游戏自带官方文档（1.19.2）明确收录 `run`＝「Runs the specified file with list of commands」**。裁定：以本机游戏文档为最高证据（wiki 落后于版本），`run` 存在；其脚本文件存放路径官方未载明，列为「本机实测待确认」（见 4.7）。
- **游戏内断言官方原语**（★★★★）：effects `log` 写 game.log（本机文档行 4802）；wiki 记载 **`debug = yes` 可加在 meta effect（scripted effect/trigger 头部），把该块求值结果追加进 game.log**（[wiki Effect](https://hoi4.paradoxwikis.com/index.php?title=Effect&diff=98402&oldid=69340)、[wiki Triggers](https://hoi4.paradoxwikis.com/index.php?title=Triggers&diff=cur&oldid=59483)）——比 `log` 更省力的留痕手段；`list_flags`（运行时列出激活 flag）、`reload [<type>]`（按类型热重载，免重启迭代）同为官方查询/迭代原语（[wiki Console commands](https://hoi4.paradoxwikis.com/Console_commands)）。
- **官方调试入口**（★★★★）：启动器内置「以调试模式打开游戏」＝`-debug_mode`（本机 `launcher-settings.json` 实文件）；wiki Modding 页佐证 debug 模式解锁开发者专属命令、有错误时 error.log 载入自动弹出（[wiki Modding](https://hoi4.paradoxwikis.com/index.php?diff=67142&oldid=65685&title=Modding)）。完整启动参数表 wiki 页被反爬拦截、未取得权威清单（未查证项）。
- **隐藏 debug 事件/决议自检范式**（★★★）：事件/决议 `hidden = yes` 不打扰玩家、`fire_only_once = yes` 单次执行，控制台 `event`/`eval_effect` 触发——社区通用做法（[wiki Event_modding](https://hoi4.paradoxwikis.com/index.php?title=Event_modding)、[wiki Decision_modding](https://hoi4.paradoxwikis.com/index.php?title=Decision_modding&diff=72902&oldid=65229)）。
- **开发者命令解锁生态**（★★）：社区存在 [rxzyx/paradox-developer-commands](https://github.com/rxzyx/paradox-developer-commands)（解锁 HOI4/EU4 developer commands），佐证 debug 模式生态成熟。

#### 3.1.2 社区工具链

- **静态校验：HOI4 Validator → CWTools**（★★★）：[SpaceKraken/HOI4-Validator](https://github.com/SpaceKraken/HOI4-Validator)（[Kaiserreich/HOI4-Validator](https://github.com/Kaiserreich/HOI4-Validator) 的活跃 fork，自述「抓常规 debugger 抓不到的错误」）；老牌 Kaiserreich/HOI4-Validator 已弃用并明示「replaced by CWTools」；[cwtools-vscode 上游](https://github.com/cwtools/cwtools-vscode)（tboby 维护、未归档）与 [Millennium Dawn 衍生的活跃 fork（自述 CWTools MD Edition）](https://github.com/MillenniumDawn/cwtools-vscode)，连同 [cwtools-action](https://github.com/marketplace/actions/cwtools-action) 为当前社区主流。均为**静态**校验——与本 MOD 静态门禁互补，但完全无法验证运行时状态，恰是「进游戏实测」机制存在的原因。
- **Irony Mod Manager**（★★★）：[bcssov/IronyModManager](https://github.com/bcssov/IronyModManager)（含[官方 FAQ](https://bcssov.github.io/IronyModManager/faq.html)）——MOD 冲突检测/合并/诊断；对本项目价值＝固定 MOD 组合与加载顺序（保证每轮测试环境一致、排查第三方 MOD 干扰），不做运行时测试。
- **Pdx-Unlimiter**（★★★）：[crschnick/pdx_unlimiter](https://github.com/crschnick/pdx_unlimiter)（Steam 物品 4170030）——全系 Paradox 存档管理器/编辑器；支持 HOI4 确定，但为 GUI 应用、细粒度字段（如 MIO 资金）逐项支持**未逐字段验证**，适合人工核对、难进无人值守流水线（未查证项）。
- **存档解析库（rakaly 生态，可编程断言的关键先例）**（★★★）：`.hoi4` 存档为 Clausewitz 格式（`HOI4txt`/`HOI4bin` token 流＋zlib）；[rakaly/jomini](https://github.com/rakaly/jomini)（多 PDS 标题低层解析器）、[rakaly/hoi4save](https://github.com/rakaly/hoi4save)（HOI4 专版「Parse and melt」）、[rakaly/cli](https://github.com/rakaly/cli)（把 pdx.tools 存档分析本地化为命令行，可接入脚本/CI）。→ 4.4 存档断言的可行性因此从「无先例」升为「有成熟库可依」。（⚠️ 勘误：立项时记载的 PyPI `jomini` Python 绑定经步骤4 选型实测为无关同名包、rakaly 系 PyPI 无发行版——证伪记录见第八节步骤4，绑定路线改走 rakaly/cli 二进制。）
- **真实大型 MOD 的开发者工作流先例**（★★★）：[Millennium Dawn 开发者文档](https://millenniumdawn.github.io/Millennium-Dawn/dev-resources/developer-setup/)（Developer Setup & Workflow、Git Workflow、PR 最佳实践）＋其维护的 cwtools-vscode 衍生 fork；其开发者文档另载 coding-pipeline＋pre-commit 工作流——大型 HOI4 MOD 工程化治理的直接参照。

#### 3.1.3 游戏 QA 通用成熟机制（★★ 通用工程实践，非单一出处）

- **冒烟/回归套件固定化**：固定测试档（golden save/黄金存档）＋固定操作序列（脚本化 console 指令）＋逐项断言清单，是本行业处理「必须实机跑」的标准组合；本项目 test1–test5.hoi4 已是雏形，缺固定化制度。
- **黄金主/快照测试思想**（★★★）：保存已知正确快照，后续运行逐字节/结构化对比，不一致即回归——业界有 pytest-remaster、php-golden、Flutter golden 等成熟实现（[pytest-remaster](https://pypi.org/project/pytest-remaster/)、[php-golden](https://github.com/franiglesias/php-golden)）；可对应 HOI4「黄金存档＋固定启动参数＋固定指令序列」。
- **日志基线白名单**：已知噪音白名单化＋「新出现错误类型」告警（baseline diff），对应本仓散落 markdown 的人工白名单（2.4 节）。
- **结构化结果留痕**：回归结果机器可读（JSON），支持跨轮 diff 与趋势比对，对应本仓「清单 markdown 人工填、多次留空」的断档（2.2 节）。
- **人工判定与自动判定分层**：GUI 画面/手感仍由人判（测试预言 test oracle），可脚本化断言交给引擎/工具判——本立项即按此分层设计（4.1–4.6）。

#### 3.1.4 CI＋游戏日志解析先例

- 静态侧进 CI 有成熟先例：cwtools-action、Millennium Dawn 工作流（见 3.1.2）。
- 存档侧可进 CI：`rakaly/cli`＋`hoi4save`/`jomini` 可离线解析真机生成的存档做断言（先例确凿，★★★）。
- **「真机启动游戏→控制台自动输入→跑自检→抓日志→CI 判读」的端到端自动化无公开先例**；HOI4 无官方无头/后台启动证据（大概率不可行）。→ 实机环节的最后一步仍须真机＋人工/半人工触发，可固定化的是复现路径、游戏内自检、存档/日志程序化断言与 JSON 留痕。

#### 3.1.5 外部调研小结

业界成熟做法与本立项候选方向一一对应：引擎断言测试（官方 tests 框架＋`log`/`debug = yes` 原语）→ 4.1；固定脚本＋黄金存档（QA 通用＋黄金主/快照思想）→ 4.2；日志基线白名单（QA 通用）→ 4.3；存档核对（rakaly 生态，成熟库确凿）→ 4.4；结构化留痕（QA 通用）→ 4.5；编排门禁（本仓流程需求＋Millennium Dawn 工作流参照）→ 4.6。除「游戏无头自动化」外，无一处需要自造轮子。

### 3.2 本机能力实测（已完成，权威出处＝游戏自带官方文档与本机实文件）

以下能力均在单位机实文件层面核实，不依赖网传信息：

| # | 能力 | 出处与证据 |
|---|------|-----------|
| 1 | console 命令 `run`：「Runs the specified file with list of commands」——可用固定命令文件批量驱动一局实测 | `D:\SteamLibrary\steamapps\common\Hearts of Iron IV\documentation\console_commands_documentation.md` 行 1484–1486 |
| 2 | console 命令 `effect`（别名 `e`）：`effect <tag> scripted_effect_name`，可在控制台直接对任意国家执行具名 scripted effect | 同上，行 803–807 |
| 3 | console 命令 `event`：`event <id> <tag>`，控制台直接触发事件 | 同上，行 850–853 |
| 4 | console 命令 `eval_trigger` / `eval_effect`：内联触发器/效果求值 | 同上，行 842–848 |
| 4a | console 命令 `testevent <Event ID>`：「Tests an event without triggering it」——事件合法性冒烟测试原语 | 同上，行 1746–1749 |
| 4b | console 命令 `savecheck`：「Makes a savegame (Test_01), loads the savegame, makes a new savegame (Test_02). Those savegames should look the same」——引擎自带存档往返一致性检查 | 同上，行 1488–1490 |
| 4c | console 命令 `debug`：开关 debug 模式（若干测试命令标注「Not available in release build」，实测需 debug 模式启用） | 同上，行 519–521 |
| 5 | scripted effect `log`：「Print message to game.log, console … Can be localized」——MOD 内可写带标记的断言输出到 game.log | `documentation\effects_documentation.md` 行 4802 |
| 6 | 控制台历史留痕：`console_history.txt` 持久记录已执行命令（含历史会话的 `research_on_icon_click` 等），可作操作审计 | 游戏根目录 `console_history.txt` |
| 7 | 版本留痕：`code_revisions.log` 记录游戏本体/外部库哈希与时间戳，可绑定「哪个游戏版本上测的」 | `logs\code_revisions.log` |
| 8 | 日志资产远超「四份日志」：`logs\` 下还有 `executed_commands.log`、`system.log`、`system_debug.log`、`graphics.log`、`random*.log`、`metrics\` 等，监测面可扩 | 本机 `logs\` 目录实列（2026-08-19 会话） |
| 9 | 固定测试档先例：`save games\test1–test5.hoi4` 等历史测试存档已在用，可升格为「黄金存档」制度 | 本机 `save games\` 目录实列 |
| 10 | 监测缺口的活证据：本机当前 `error.log` 仍含 4 条 `Invalid effect 'enable_equipment_modules'`（`PRC_OCS_choice_events_naval.txt` 54/78 行）——修复已在 #4 完成但本机部署未跟进，恰好演示「静态门禁抓不到 + 实机发现靠运气 + 修复与部署不同步」三类缺口 | 本机 `logs\error.log`（2026-08-19 13:57:56 条目） |
| 11 | 现有监测埋点为零：MOD 源码无任何 `log =`、`[OCS_TEST]`、debug 自检先例 | 全仓 grep（common/events 无匹配） |
| 12 | **引擎自带场景断言测试框架**：游戏根目录 `tests\*.txt`（德/中/英等逐国一档）定义 `last_date`＋`tests = { <name> = { acceptable_fail_rate / success = {触发器} / fail = {触发器} } }`——这就是 Paradox 官方"进游戏实测断言"的成熟格式；对应日志通道 `logs\scenario_testing.log`（本机已存在，当前 0 字节）与 console `test_log`（Enable/Disable tests）、`test_save_mode`（失败测试存档取当天/前一天） | `tests\germany.txt`、`tests\china.txt` 实文件；`console_commands_documentation.md` 行 1732–1744；`logs\scenario_testing.log` |
| 13 | **官方调试模式启动方式**：启动器自带「以调试模式打开游戏」＝`hoi4.exe -gdpr-compliant -debug_mode`，为 debug-only 命令提供标准入口 | 游戏根目录 `launcher-settings.json`（`alternativeExecutables` 节） |

## 四、可吸纳方向（候选，供再次对齐与门二）

> 按「游戏内自检 / 控制台脚本驱动 / 日志基线判读 / 存档断言 / 证据留痕归档 / 流程固化」六类归纳；外部调研结论到达后校正补充。

### 4.1 游戏内自检（MOD 内 debug 门控断言套件）

- 依据：effects `log`（可写 game.log，可本地化）＋ `debug = yes` meta effect（求值结果自动追加进 game.log）＋触发器/效果断言能力（`if/else` + `log` 输出 PASS/FAIL 标记）＋官方 `tests\*.txt` 场景断言格式（success/fail 触发器）为范式。
- 形态候选：新增 debug 门控事件/效果（如 `PRC_OCS.9xxx` 自检事件链），对关键功能做游戏内断言：flag 已设、科技已授、装备已入库存、MIO 资金/规模、决议可用性；每条断言输出 `[OCS_TEST] PASS/FAIL <case>` 到 game.log。单入口 `test_suite` effect 内部串联全部用例（scripted effect 可互相调用），控制台一条 `eval_effect`/`event` 跑完整套；配套 `list_flags`（运行时查 flag）、`reload`（免重启迭代）。
- 门控红线：`is_triggered_only = yes`＋无玩家入口达成玩家不可见、AI 不可用（事件级 `hidden` 字段本仓无先例、引擎支持性待步骤0 核实，不支持则不用，见 6.9-2）；不进正式玩家路径；门一已拍板：随发布包分发（1.4 结论 2）。
- 优点：把「人眼看界面」变为「引擎自己判」，覆盖 effect 有效性/DLC 极性/语义错位等静态抓不到的错误类型；缺点：需新增游戏脚本，验收依赖实机触发一次。

### 4.2 控制台脚本驱动（固定实测脚本 + 黄金存档）

- 依据：console `run`（执行命令文件）、`effect <tag> <scripted_effect>`、`event <id> <tag>`、`testevent`（事件合法性冒烟）、`savecheck`（存档往返一致性）、`debug`/`-debug_mode` 官方入口；`console_history.txt` 可审计操作。
- 形态候选：每轮实测用固定 `run` 脚本（切国家→触发自检事件→执行点验效果→自动存判定档），配套「黄金存档」（test1–test5 升格为制度：版本化、留档、随轮复用），人只负责启动游戏与执行一条命令。
- 优点：操作序列可重复、可审计、可复现；缺点：`run` 脚本文件位置需本机实测确认（候选：游戏根目录相对路径）。

### 4.3 日志基线判读（机器白名单 + 自动判读工具）

- 依据：现有四份日志＋23 个日志资产；噪音白名单现散落 markdown；本机 `error.log` 活证据。
- 形态候选：`tools/check_logs.py` 之类——机器可读白名单文件（已知噪音模式：gamelobby/remotefile、Conflict Risk、daliwan Invalid supported_version 等）＋「本轮 vs 基线」diff 告警＋`[OCS_TEST]` 标记提取＋`code_revisions.log` 版本绑定＋结果 JSON 落盘。
- 优点：把「人脑经验」固化进仓库、可 CI 复用（若上传日志）或本机复用；缺点：白名单需随原版更新维护。

### 4.4 存档断言（.hoi4 解析，rakaly 生态）

- 依据：二进制存档 48–68 MB 已在本机；成熟解析库已确认存在——`rakaly/jomini`＋`rakaly/hoi4save`（Parse and melt）＋`rakaly/cli`（命令行，可进脚本/CI）。（PyPI `jomini` 绑定经步骤4 选型实测证伪，见第八节步骤4。）
- 形态候选：用 rakaly 工具链解析实测后存档，核对 MIO 特质/资金、科技、装备库存、模板、flag 等「界面上看不见全貌」的状态；结果并入 4.3 的结果 JSON。与 4.1 分工：4.1 引擎内实时断言为主，4.4 存档事后深度核对为辅（覆盖 4.1 漏判或需要历史快照对比的场景）。
- 优点：独立于游戏内埋点、可事后核对、可进 CI（存档由真机步骤生成后离线断言）；缺点：库路线需选型验证（PyPI 绑定已证伪；rakaly/cli 二进制路线待另立变更记录评估），实现成本中等。

### 4.5 证据留痕归档（强制入库 + 结构化产物）

- 依据：v2.6-test2/v2.7-test1/v2.8-test1 清单空白、截图缺失、日志未入库的多轮断档；`docs/DEVELOPMENT.md` 已要求「仓库内可独立复核证据」与脱敏。
- 形态候选：`docs/testing/<版本-轮次>/` 归档目录约定（清单 JSON 化、日志脱敏副本、截图命名规范、SHA-256 清单）；回归清单模板改为「逐项必填 + 缺证据＝不通过」；AI 判读工具产出机器可读结果供 diff/趋势比对。
- 优点：直接堵住历史上最大的断档点；缺点：无技术风险，纯纪律+工具，成本低。

### 4.6 流程固化（实机回归门禁脚本 + 编排）

- 依据：现状部署→启动→采集→判读全链路无编排；A/C 流程要求每步回归留痕。
- 形态候选：`tools/run_regression.ps1` 之类编排——部署测试包→清旧日志→提示维护者启动实测→回收日志/存档/截图→跑判读工具→生成回归报告→归档；把 `docs/maintenance/维护与重构标准流程.md` 的「实机回归」环节接到固定脚本上。
- 优点：一命令串起全链路；缺点：启动游戏这一步仍需人手（无头自动化不可行），编排脚本只做前后置。

### 4.7 校正与开放项（外部调研合并完成）

- 已校正/升格：4.4 存档断言从「无先例」升为「有成熟库可依」（rakaly/jomini/hoi4save/cli；PyPI 绑定经步骤4 实测证伪，见第八节步骤4）；4.1 增补 `debug = yes`、`list_flags`、`reload` 官方原语；4.3 白名单机制与 QA 通用实践、黄金主/快照思想均有出处支撑。
- 证据冲突裁定：`run` 命令以本机游戏自带官方文档（1.19.2）为最高证据（存在），社区 wiki 口径落后；`run` 脚本文件存放路径官方未载明，仍列「本机实测待确认」。
- 未查证项（如实登记，不编造）：HOI4 完整启动参数表（wiki 页反爬）；HOI4 无头/后台启动（大概率不可行）；HOI4 专用自动化截图/录制方案；Pdx-Unlimiter 对 HOI4 细粒度字段的逐项支持。（PyPI `jomini` 绑定项已由步骤4 实测终结——证伪，不再列未查证。）
- 落地排序建议（按证据强度×成本）：先上 4.3＋4.5（日志白名单＋JSON 留痕，纯本机、成本最低、直接根治证据断档）→ 再上 4.1＋4.2（游戏内自检＋固定脚本/黄金存档，官方原语证据最强）→ 后按需上 4.4（存档断言）与 4.6（全链路编排）。

## 五、留痕

- 本文件为 A 流程 ①–⑥ 的落盘载体；⑤K3 复检逐轮历程见第七节；⑥实施记录与 diff 分块复检历程见第八/九节；提交记录＝`4a978a5`（2026-08-19，门三确认后提交并推送远端）。
- 调研出处、对齐问答原文与机主拍板结论逐条记入本文件（流程要求「全程留痕」）：门一问答与结论见 1.4；门二结论见 6.9。

## 七、K3 复检历程（A⑤）

### 第 1 轮（2026-08-19，kimi CLI moonshot/k3 0.36.1）→「需修改」6 项，已全部落实

| # | 问题位置 | 问题 | 修改思路（已按此落实） | 落实位置 |
|---|---|---|---|---|
| 1 | 步骤1＋步骤3 验收 | 「提取 PASS/FAIL 标记」未规定 0 标记判定：忘开 `-debug_mode`/触发失败/`log` 未写入时「全部 PASS」空真，断档重演 | check_logs 约定「0 个 `[OCS_TEST]` 标记＝本轮 FAIL/无效」；验收写明「标记数＝用例清单数且全 PASS」 | 6.2 步骤1/步骤3 验收 |
| 2 | 步骤3 自检用例 | 「AI 守卫抽查（决议对 AI 不可见）」无法用游戏内触发器断言（不存在求值决议对别国可见性/可用性的 trigger） | 移出游戏内断言，归入 `test_validate_mod.py` 静态契约（`validate_mod.py` 已强制，登记即可）；游戏内只断言守卫依赖的条件本身（如 initialized flag） | 6.2 步骤3 |
| 3 | 步骤3 自检用例 | 「特殊科研 42 组映射」与仓库口径不符（`docs/analysis/v2.6_特殊科研组事件映射.json`＝26 组，AGENTS.md 同口径；42＝26＋v2.8 新增 16，但必须注明口径） | 用例清单直接读映射 JSON 派生（当前 26＋16＝42 组），不写死数字 | 6.2 步骤3 |
| 4 | 步骤3 验收 | 「四份日志干净」与步骤1/6.5「白名单过滤后无新错误」口径冲突；`-debug_mode` 下 game.log 必有额外噪音，按字面判定不可能通过 | 统一为「白名单过滤后无新错误签名」 | 6.2 步骤3 验收 |
| 5 | 6.4 回滚 | 「每步独立可回滚」对中间步骤不成立：步骤5 依赖步骤1–4，单独 revert 步骤1/3 留悬空引用与失效文档 | 注明按依赖逆序回滚（5→4→3→2→1）；文档引用写成「工具存在才生效」的降级表述 | 6.4 |
| 6 | 6.3 风险 | 缺「自检用例随 MOD 演进漂移」风险：B1–B6 断言、映射组数随版本变化 → 假 PASS/假 FAIL | 增列风险与对策：功能改动须同批更新自检用例；用例清单由映射 JSON/契约派生而非手工维护 | 6.3 |

K3 第 1 轮同时确认：步骤 0→1→2→3→4→5→6 依赖方向正确；事件 id「PRC_OCS 1–70 已占用」属实（events/ 下恰好 70 个）；新增键/命名空间/文件全部增量；未触碰任何红线；其余审查项（步骤4 选型门槛、每步静态四门禁、BOM 合规、提交纪律）均无问题。

### 第 2 轮（2026-08-19，kimi CLI moonshot/k3）→「需修改」2 项＋3 附记，已全部落实

- 上轮 6 项经 K3 逐条核对原文与仓库事实：**全部落实到位**。
- 本轮需修改 2 项：
  1. **步骤3 用例模板语法错误**：`else = {…}` 不得嵌在 `if = {}` 块内，须与 `if` 同级紧随（本仓 events/ 全部 317 处 else 均为同级）。→ 模板已改两段式（6.2 步骤3），步骤0 增验 scripted_effect 内 if/else 与 log 行为。
  2. **自检事件本地化缺口（红线 6 敞口）**：本仓全部既有事件带 title/desc，自检事件须补英/简中两份 yml 键（BOM、双语同步），文件清单与工作量已更新（10–14 文件）。
- 附记 3 条已顺手落实：状态行「门二待拍板」过期表述已改（文件头/第六节标题）；check_logs 直接解析自检 effects 文件取 case id 作用例清单唯一真源（6.2 步骤1/步骤3）；6.5「历史断档类型无法再发生」改为可判措辞「缺证据轮次被 FAIL_NOT_EVIDENCED 拦截」。

### 第 3 轮（2026-08-19，kimi CLI moonshot/k3）→「需修改」2 项（一行级数字口径）＋2 附记，已全部落实

- 前两轮 8 项＋3 附记经 K3 逐条核验均已落地；依赖顺序、风险覆盖、回滚路径、红线合规均成立。
- 本轮需修改 2 项（本机已亲自复核属实）：① 组数口径——v2.8 映射 JSON 实为 17 条（eid 54–70），合计 26＋17＝**43** 组（原写 42）；② else 计数——events/ 实为 **53** 处（原写 317）。均已改为实测数字并注明「以映射 JSON 当前条目数为准」。
- 附记已落实：步骤2 验收注明「标记判读自步骤 3 起生效」；AGENTS.md 状态行同步随步骤 5 执行（勿漏）。

### 第 4 轮（2026-08-19，kimi CLI moonshot/k3）→「需修改」1 项＋3 附记，已全部落实

- 前三轮 10 项＋附记经 K3 逐条亲验均已落地；依赖顺序、回滚、红线无新问题。
- 本轮需修改 1 项：步骤1 铁律（0 标记＝FAIL）× 步骤2 首轮归档冲突——check_logs 增「标记判读豁免」入口（默认不豁免、豁免事实入 result.json 高亮），步骤2 验收引用该豁免。
- 附记 3 条已落实：6.9 括注改「is_triggered_only＋无玩家入口」达成不可见（事件级 hidden 引擎支持性由步骤0 核实，不支持则不用）；6.3 增列「游戏版本升级复合风险」（重建基线存档＋复跑步骤0）；6.4 注明步骤0 纯文档产物不入逆序链。

### 第 5 轮（2026-08-19，kimi CLI moonshot/k3）→「需修改」2 条（文档一致性）＋复核确认，已全部落实

- 前四轮 11 项＋附记经 K3 逐条复核到位（除本轮 2 条收口）。
- 本轮需修改 2 条：① 步骤0 增列第⑤项验证（事件级 `hidden` 在 1.19.2 支持性及对 error.log 影响），验收由「四项」改「五项」，与 6.9-2 核实指派对齐；② 6.2 总起句「每步独立可回滚」改为「每步可回滚（多步回退按 6.4 依赖逆序执行）」，与 6.4 对齐。
- K3 本轮确认：依赖与顺序自洽（含步骤2 豁免衔接）、验收可机械判定、红线无一步触碰、回滚可行。

### 第 6 轮（2026-08-19，kimi CLI moonshot/k3）→「需修改」1 项（文件头状态行漂移）＋2 附记，已全部落实

- 前五轮 13 项经 K3 逐条亲验均到位；K3 并独立复核仓库事实（53 处 else、PRC_OCS.1–70 全占用、26＋17＝43、validate 397-413 强制、build_release 打包 glob 不吞步骤0/2 产物）。
- 本轮需修改 1 项：文件头状态行（第 4 行）再次漂移（仍写「第 3 轮进行中」）。已改与 §7 同步；并在步骤6 增「每轮复检结论落定即同步文件头状态行与第七节历程」防漂移规则。
- 附记 2 条已落实：步骤5「check_save 存在才跑（步骤4 暂缓时跳过）」与 6.4 降级口径对齐；§7 第 6 轮占位已回填（本条）。

### 第 7 轮（2026-08-19，kimi CLI moonshot/k3）→「通过」（终审）

- K3 对可验证事实逐项仓库实测：53 处 else、26/17→43 组、id 占用至 70、validate 397-413 强制 AI 守卫、deploy 脚本与文档目标均真实存在，全部属实。
- 六维度审查全部成立：依赖顺序（0→6 自洽）、风险 8 条无缺口、验收可机械判定、红线零触碰、回滚可行、前六轮落实逐条核对到位。
- 非阻断建议 3 条已顺手落实：① 步骤3 验收显式列「新增 common/scripted_effects/ 与 events/ 文件 UTF-8 无 BOM」；② 步骤5 提示语含「已用 -debug_mode 启动」确认项；③ 步骤1 版本绑定失败降级为人工登记版本号并记入 result.json。

### 终审结论

**A⑤ 完成：方案经 K3 七轮复检终审「通过」**（第 1–6 轮共 14 项需修改＋10 条附记全部落实，逐轮记录在案）。进入 A⑥ 小步实现。

## 八、A⑥ 实施记录（小步实现，每步四门禁）

> 数字口径：本节验证数字为各步完成时的历史快照；K3 分块审计修复轮（第九节）后的现行口径＝test_check_logs **18 项** OK、test_validate_mod **36 项** OK、build 31 文件 SHA `d0470c2e…`，演变见第九节块1/块2。2026-08-20 实机轮修正后口径＝test_check_logs **19 项** OK（增脱敏单测）、build SHA `e8df5bd4…`，见第九节「实机轮修正」。

### 步骤0 · 本机能力实测（2026-08-20 完成，与步骤3/5 合并一轮实机执行）

- 实机轮：v2.8-test5 首轮＋复跑（首个按新归档制度落盘的轮次，见 `docs/testing/v2.8-test5/`）。
- 五项能力实测结论（详见 `docs/testing/能力实测记录_2026-08.md`）：① `run` 位置未实测（`effect` 直触发兜底两轮实测完全可行）；② **`log` 行为有出入**——消息串开头的 `[...]` 分组被引擎消费（替换为引擎自身前缀），自检标记由 `[OCS_TEST] ` 修正为无方括号 `OCS_TEST `；③ `effect` 实测可用；④ `is_triggered_only` 无 error.log 噪音；⑤ 事件级 `hidden` 未实测（未采用，不阻塞）。
- 附带发现：中文存档名保存失败（rename errno 2）→ 判定档/存档名一律 ASCII；判定档 `v2.8test5verdict.hoi4` 留本机（新黄金存档候选，待机主拍板登记）。

### 步骤1 · 日志基线判读工具（2026-08-19 完成）

- 新增 `tools/log_noise_whitelist.json`：4 条白名单（gamelobby.cpp/remotefile.cpp/Conflict Risk/daliwan Invalid supported_version），逐条带出处，禁止宽泛通配。
- 新增 `tools/check_logs.py`：白名单过滤＋`[OCS_TEST]` 标记提取＋用例清单比对＋`code_revisions.log` 版本绑定（失败降级人工登记）＋result.json 与人类摘要；0 标记铁律与「标记判读豁免」（`--exempt-markers`，豁免事实入 result.json）已实现。
- 新增 `tools/test_check_logs.py`：14 项单测（白名单过滤、注入告警、签名归一化、标记提取、铁律/豁免、清单比对、版本降级、端到端三场景）。
- **实现口径（较方案细化，意图不变）**：game.log 为运行日志，逐行白名单过滤会把正常运行行（defines loaded/Executing History 等）误报为新签名——改为 game.log 仅对「错误样」行（error/invalid/unknown/exception/failed/warning）告警；error.log/text.log 保持全行过滤（text.log 无白名单条目，任何内容都计为本地化错误签名，0 字节＝干净）。
- **验证**：14 项单测全绿；本机真实日志冒烟——版本绑定 a729d47bd、0 标记判 INVALID（铁律生效）、仅报 3 类真实新签名（旧部署的 `enable_equipment_modules` 错误，正常行零误报）、exit 1 正确反映非 PASS；四步门禁全绿（validate 0 警告/33 项 OK/mio 一致/build 通过 SHA `b378e5c4…`）。

### 步骤2 · 证据留痕归档制度（2026-08-19 完成）

- 新增 `docs/testing/实机回归归档制度.md`：归档目录约定（README/result.json/逐项清单/脱敏日志/截图/SHA256SUMS）、逐项必填与 `FAIL_NOT_EVIDENCED` 判定、日志判读口径（与 check_logs 一致）、脱敏规则、流程衔接。
- 新增 `docs/testing/黄金存档制度.md`：登记规则（用途/游戏版本/MOD 版本线/SHA-256/本机位置）、使用规则、版本升级重建对策；历史 test1–test5 以「待补登」登记（不臆造历史数据）。
- 更新 `docs/DEVELOPMENT.md` 实机回归节：指向新制度与 check_logs，证据强度收紧为「强制可复核」。

### 步骤3 · 游戏内自检套件（2026-08-19 源码完成；2026-08-20 实机验证完成）

- 新增 `tools/generate_selftest_effects.py`（生成器，`--check` 防漂移）→ 生成 `common/scripted_effects/PRC_OCS_selftest_effects.txt`：单入口 `PRC_OCS_selftest_run_suite`＋7 固定用例（init_flag/skull_idea/mio_exists/mio_size_max/jet_polarity/n1_cruiser_submarine/b6_heavy_water）＋43 选择组用例（由两份映射 JSON 派生：26＋17，case id＝`choice_group_<eid>`，断言＝维护者逐项点选后对应 flag 已设）。用例清单唯一真源＝产物中的 `[OCS_TEST]` 字面量（check_logs `--case-source` 解析）。
- 新增 `events/PRC_OCS_selftest_events.txt`：`PRC_OCS_selftest.1`，`is_triggered_only = yes`，无决议入口/无 on_action（玩家不可见、AI 不可用）；选项运行套件。事件级 `hidden` 未采用（步骤0 待核实，K3 第 4/5 轮口径）。
- 双语本地化追加 3 键×2（BOM 保留、单 BOM 已核实）：`PRC_OCS_selftest.1.t/.d/.a`。
- `tools/check_logs.py` 增 `--case-source`（解析自检 effects 文件为用例清单）；`tools/test_validate_mod.py` 增 3 条契约（生成产物一致且 50 用例、事件触发条件与双语键、自检红线抽查）。
- 断言口径说明：自检套件为**只读后置断言**——维护者按固定序列（一键开局→骷髅师→MIO 拉满→空军/海军特殊科研→核能菜单取重水→43 组逐项点选）操作完毕后运行套件；DLC 极性类用例按环境分支（BBA 有/无各取正确预期），每用例恒输出 1 条 PASS/FAIL 标记。选择组未点选＝对应 FAIL，即「证据不足」信号（与强制可复核口径一致）。
- **验证**：check_logs 17 项单测全绿（块2 后现行 18 项，见第九节）；test_validate_mod 36 项全绿（含 3 条新契约）；validate 0 警告（20 脚本文件/89 effects/527 键对）；mio --check 一致；selftest --check 一致；build 通过（31 文件，SHA `93d951be…`；块1 后现行 `d0470c2e…`，见第九节）。实机验证：2026-08-20 完成——首跑 50 用例全 PASS（旧标记格式，暴露步骤0 修正项）→ 修正标记格式后复跑，check_logs 判读 **PASS**（50/50、白名单净、版本绑定 a729d47bd），归档 `docs/testing/v2.8-test5/`。

### 步骤4 · 存档断言选型（2026-08-19 完成，结论＝暂缓，不阻塞主线）

- 选型实测：PyPI `jomini` 包实为**重名无关库**（umitkaanusta/Jomini，历史战役模拟包，0.1.4，已卸载）；rakaly 系 Python 绑定名（`jomini-py`/`hoi4save`/`pdx-tools`）在 PyPI **均无发行版**（pip index 确认 No matching distribution）。rakaly 生态为 Rust crate＋预编译 CLI（rakaly/cli），非 pip 可用。
- 结论：按方案「不可行则如实登记、暂缓」——存档断言暂缓；引擎自检（步骤3）已覆盖九成需求；后续如需存档级断言，走 rakaly/cli 二进制路线（另立变更记录评估），本批不引入。

### 步骤5 · 全链路编排与流程衔接（2026-08-19 完成）

- 新增 `tools/run_regression.ps1`：部署（复用 deploy_to_local_mod.ps1，红线 9）→ 备份旧日志（改名留档）→ 维护者实机操作提示（含「已开启 -debug_mode」确认项＋固定序列指引）→ 回收日志 → check_logs 判读（`--case-source` 自检文件）→ check_save 存在才跑（步骤4 暂缓时跳过）→ 脱敏日志副本归档（USERNAME/USERPROFILE/COMPUTERNAME → `<REDACTED>`）→ SHA256SUMS＋README 占位。
- 流程衔接（门二结论 4，随批提交）：`AGENTS.md` 验证门禁新增「实机监测机制（2026-08-19 起）」段；`docs/maintenance/维护与重构标准流程.md` 第四节补实机回归衔接句；均用「工具存在才生效」降级表述（K3 第 1 轮修正口径）。
- **验证**：四步门禁全绿（validate 0 警告 / unittest 36 项 OK / mio 一致 / selftest --check 一致 / build 31 文件 SHA `93d951be…`；块1 后现行 `d0470c2e…`，见第九节）。

## 九、A⑥ diff 复检历程（K3 分块审计实施改动）

### 第 1 轮·块1（MOD 脚本与白名单，2026-08-19）→「需修改」4 项阻断＋6 建议，已全部落实

- K3 亲自复核：43 映射用例与两份 JSON 派生一致；`has_mio_size`/`any_military_industrial_organization` 经本机 vanilla 文件实证存在；三科技 id 均真实；if/else 同级、单 limit、log 格式与 check_logs 正则自洽；生成器 --check 确定一致；yml 双 BOM、3 键对等；红线 1–9 逐条无触碰。
- 阻断修复：① `events/PRC_OCS_selftest_events.txt` 补 `add_namespace = PRC_OCS_selftest`（仓库体例 4/5 文件有声明，新命名空间必须声明；契约 2 增断言）；② `.gitignore` 增否定规则 `!docs/testing/**/logs/`＋`!docs/testing/**/logs/*.log`（原 `*.log` 会吞归档日志副本，`git check-ignore` 复核已不入忽略）；③ `test_validate_mod.py` 契约 1 的 subprocess 注入 `PYTHONIOENCODING=utf-8`＋`errors="replace"`（原在 cp936 控制台会 UnicodeDecodeError，跨机不可复现；已实测无外层环境变量时 36 项 OK）；④ `check_logs.build_verdict` 增「标记总数＝用例清单数」铁律（原只比集合，重复执行会漏报；补重复标记单测）。
- 建议落实：check_logs 摘要正确打印 case 来源；run_regression 的 SHA256SUMS 移到 README 写入之后＋提示语改「手动刷新」；版本正则锚定 `(?m)^version\s*=`；生成器重复 eid 防线改为去重集合比较（原为死代码）；n1 无 MtG 恒 PASS 语义加注释；run_regression.ps1 存为带 BOM UTF-8（PS 5.1 中文兼容）；实施记录「17 项单测」与实际一致（补重复标记单测后恰为 17）。
- 复核门禁：test_check_logs 17 项 OK；test_validate_mod 36 项 OK（含无环境变量复测）；validate 0 警告；build 31 文件 SHA `d0470c2e…`。

### 第 1 轮·块2（Python 工具，2026-08-19）→「需修改」2 项＋4 建议，已全部落实

- K3 独立复跑门禁确认全绿；重点核查通过面：自检 effects 50 块 if/else 同级、单 limit、触发器全部真实；映射派生与事件实赋值一致；`--case-source` 实测恰得 50 用例；生成器确定性；本地化双 BOM 对等；红线零触碰。
- 阻断修复：① `run_regression.ps1` 删除以 `$LASTEXITCODE` 判 deploy 成败的检查（deploy 脚本不设退出码，robocopy 成功即返回 1–7，正常部署必误抛；真实失败由 `$ErrorActionPreference='Stop'` 传播）；② check_logs「标记数＝清单数」铁律与摘要来源打印已在块1 修复轮落实（块2 审计的是旧快照），经 grep 复核在文。
- 建议落实：run_regression 提示补「控制台切国 tag PRC」；SHA256SUMS 路径改 `/` 分隔（跨 shell `sha256sum -c`）；白名单 daliwan 条目按 `ugc_1326075315` 收窄（防吞本 MOD 同类错误），并增「本 MOD 自身同类错误仍必报」单测。
- 复核门禁：test_check_logs 18 项 OK；test_validate_mod 36 项 OK；validate 0 警告；build SHA `d0470c2e…`。

### 第 1 轮·块3（编排脚本与制度文档，2026-08-19）→「需修改」6 阻断＋4 建议，已全部落实

- 首轮结论与修复：① deploy `$LASTEXITCODE` 检查＝块2 修复轮已删除（块3 审计的为旧快照，现行文件仅 `&` 调用＋注释说明，复核确认）；② `run_regression.ps1` 重存 UTF-8 带 BOM（块2 编辑轮曾回归掉 BOM，本轮恢复，首三字节 EF BB BF 已字节级核实）；③ SHA256SUMS 路径 `/` 分隔＝块2 已修（旧快照）；④ 自检命令口径三处统一——ps1 提示补 `event PRC_OCS_selftest.1 PRC` 等价命令、黄金存档制度 §二.1 改为与自检文件头一致的双口径（ps1 提示本已含 `tag PRC`，首轮「提示中无」为误读）；⑤ README 占位必填项加固——「问题清单/例外说明」拆为独立行＋补「版本线」行，与归档制度 §一 README 必含项一一对应（首轮「必填项无落点」判定实为旧快照误读，按加固口径处理）；⑥ 黄金存档制度登记表补「创建日期」「本机位置」两列，历史行按不臆造口径填「待补登」。
- 建议落实：pwsh 7.3+ 显式 `$PSNativeCommandUseErrorActionPreference = $false`（FAIL 判级不得中断归档）；脱敏改大小写不敏感 `-replace [regex]::Escape(...)`；新增 `-RefreshSumsOnly` 开关（仅重建 SHA256SUMS）；黄金存档制度明文 `C:\Users\Admin` 路径改 `%USERPROFILE%` 形式。
- 复核门禁：复核轮通过（2026-08-19，K3 逐项三态复核＋门禁重跑全绿）。

### 第 1 轮·块4a（立项文档上部 diff，2026-08-19）→「需修改」7 项＋3 建议，已全部落实

- ① 附记计数不可回溯：第 3 轮标题「＋3 附记」→「＋2 附记」（正文实列 2 条），状态行与终审「12 条附记」→「10 条附记」（逐轮加总 3＋2＋3＋2 可复现）；② 状态行「⑥小步实现（进行中，步骤1 起）」→「步骤1–5 完成，见第八节；diff 分块审计块1/块2 已修毕复核全绿，块3–5 审计中，见第九节」；③ 门一 framing 四处置旧换新：标题「（A 流程 ①–③）」→「①–⑥」、定位行「本批只立项与调研」→「本批＝立项＋调研＋分步方案＋六步实施」、第一节标题「门一：待机主拍板」→「已拍板，结论见 1.4」、1.3「待机主确认」→「门一已确认」、1.2 产出物预期同步；④ 3.1.2 SpaceKraken「Audax Validator 的 HOI4 版」→「Kaiserreich/HOI4-Validator 的活跃 fork」；⑤ 3.1.2 cwtools-vscode「现由 Millennium Dawn 维护」→上游（tboby、未归档）＋MD 衍生 fork（MD Edition）双列；⑥ 3.1.2/4.4/4.7 rakaly PyPI 口径——3.1.2 加勘误括注（步骤4 实测证伪）、4.4 依据与缺点改 rakaly/cli 二进制路线、4.7 未查证项移出并登记证伪；⑦ 4.1 门控红线「`hidden = yes` 玩家不可见」→「`is_triggered_only = yes`＋无玩家入口（事件级 `hidden` 待步骤0 核实）」。
- 建议落实：§五留痕改「A 流程 ①–⑥ 的落盘载体…提交记录将在门三后追加」；3.1.2 Millennium Dawn 条「自带 CI」→ coding-pipeline＋pre-commit；4.7 未查证项同步（同⑥）。
- 复核门禁：复核轮通过（2026-08-19，K3 逐项三态复核＋门禁重跑全绿）。

### 第 1 轮·块4b（立项文档下部 diff，2026-08-19）→「需修改」3 项＋2 建议，已全部落实

- 首轮 3 项中 2 项与块4a 同源（状态行「步骤1 起」、附记计数），修复见块4a ① ②；第 3 项＝第八节验证数字与现行实测不符：八节开头增「数字口径」注（现行＝test_check_logs 18 项 OK / test_validate_mod 36 项 OK / build SHA `d0470c2e…`，演变见第九节），步骤3「17 项单测」与步骤3/5 SHA `93d951be…` 均加括注指向第九节。
- 建议落实（与块4a 建议同源）：标题 ①–⑥、§五载体表述、4.7 PyPI 括注——均已随块4a 落实。
- K3 同时核实通过：§8 步骤1–5 文件清单全部属实；else 53 处、PRC_OCS.1–70 全占用、映射 26＋17＝43（eid 54–70）、validate 397–413 强制 AI 守卫均实测属实；6.9 门二四条与正文落地一致；红线无触碰（git 全未提交、实机项标「待维护者点验」）。
- 复核门禁：复核轮通过（2026-08-19，K3 逐项三态复核＋门禁重跑全绿）。

### 第 1 轮·块5（衔接与余量 diff，2026-08-19）→「需修改」2 项＋2 建议，已全部落实

- ① 快照 §9 状态行与真实进度不符：已更新为「块1/块2 已修并复核全绿；块3–5 首轮结论已出并修复完成、复核中」；② test_validate_mod.py 契约1 subprocess 编码（`PYTHONIOENCODING`＋`errors="replace"`）＝块1 阻断③ 修复，块5 审计的为旧快照，提交以工作树修复版为准（复核确认）。
- 建议落实：状态行随复核结论一次性刷新为最终态（本复核通过后执行）；评审分发以工作树为准重新出 diff。
- K3 同时核实通过：AGENTS.md/标准流程/DEVELOPMENT.md 衔接句均含「工具存在才生效」降级表述、引用路径无悬空；契约与实现一致（50 用例＝7 固定＋43 选择组，与两份映射 JSON 实测 26/17 吻合）；本地化两 yml 单 BOM、3 键对等、纯追加；`PRC_OCS_selftest` 新命名空间与既有 `PRC_OCS.` 键不冲突。
- 复核门禁：复核轮通过（2026-08-19，K3 逐项三态复核＋门禁重跑全绿）。

### 复核轮（块3–5 修复汇总复核，2026-08-19）→「通过」（终审）

- K3 逐项三态复核：块3 6 阻断＋4 建议、块4a 7 项＋3 建议、块4b 3 项＋2 建议、块5 2 项＋2 建议全部「已落实」；首轮审计旧快照误报项（块3 阻断1/3、块5 问题2）经工作树复核确认均已随块1/块2 修复轮落地，提交以工作树为准。
- 复核中仅余 1 处交叉文件残留：快照 §9 状态行「12 条附记」→「10 条附记」（与立项文档 4/274 行对齐）；终审确认小轮「通过」。另按 K3 非阻断观察顺手落实 3 处：§6.2 步骤4 指针括注（3.1.2 rakaly 条已证伪）、1.4 门一结论 2 括注指向 6.9-2、run_regression.ps1 刷新路径补 `$checkLogsExit`/`$saveNote` 显式占位（保持 UTF-8 带 BOM，parse OK）。
- K3 复核手段：门禁重跑全绿（validate 0 警告 / test_validate_mod 36 OK / test_check_logs 18 OK / mio --check 一致 / selftest --check 一致 / build 31 文件 SHA `d0470c2e…`）；字节级检查（ps1 首三字节 EF BB BF、单 BOM 键对）；事实核对（黄金存档 birth time 与登记表逐档吻合、git 全未提交、grep 无「12 条附记/待机主/①–③/Audax/自带 CI」残留）。
- **结论：A⑥ diff 经 K3 分块审计第 1 轮＋复核轮终审「通过」**（块1–5 全部修毕并复核全绿）。
- **提交**：`4a978a5`（2026-08-19 门三机主确认后提交，已推送 origin/agent/cross-machine-governance）。

### 实机轮修正（2026-08-20，v2.8-test5 首轮实机实测驱动）

- **首跑结果**：50 用例游戏内全 PASS（旧标记格式，game.log 01:13:06 证据入 `docs/testing/v2.8-test5/logs_首跑_旧标记格式/`），但暴露两处实机才可见的机制缺陷。
- **修正① 引擎消费消息串开头 `[...]` 分组**：`log = "[OCS_TEST] PASS <case>"` 写入 game.log 时 `[OCS_TEST]` 前缀被引擎替换为自身 `[时间][日期][源]` 前缀，标记字面量丢失→判读工具 0 标记判 INVALID（步骤0 能力实测②的定论）。修复：标记统一改无方括号 `OCS_TEST PASS/FAIL <case>`（生成器/生成产物/check_logs 正则/19 项单测/契约/AGENTS/DEVELOPMENT/归档制度/本地化 .d 键/立项文档同步）。
- **修正② 中文存档名保存失败**：`save v2.8test5判定` 的 _temp→正式名 rename 报 errno 2。修复：判定档改 ASCII 名 `v2.8test5verdict.hoi4`（内容完整）；编排提示与制度定「存档名一律 ASCII」。
- **修正③ 归档脱敏正斜杠路径漏脱**：首跑 error.log 出现 `C:/Users/...` 形态，USERPROFILE 反斜杠替换漏脱。修复：`check_logs._redact_identity` 双分隔符覆盖＋`result.json` 的 `logs_dir` 输出即脱敏＋增单测（现行 19 项）。
- **复跑验证**：载入判定档→`effect PRC PRC_OCS_selftest_run_suite`→新格式标记 52 处字面量完整存活（BEGIN 1＋PASS 50＋END 1）→ check_logs 判读 **PASS**（50＝清单 50 全 PASS、白名单净、版本绑定 a729d47bd、text.log 0 字节、setup.log PRC_OCS 9 条）→ 首个按新制度归档的轮次 `docs/testing/v2.8-test5/` 落盘（README/result.json/清单.md/脱敏日志/SHA256SUMS 47 行）。
- 门禁：validate 0 警告 / test_validate_mod 36 OK / test_check_logs 19 OK / mio・selftest --check 一致 / build 31 文件 SHA `e8df5bd4…`。
- 状态：实机轮修正已实现、K3 复检**通过**（2026-08-20，逐项亲验：新格式自洽/归档证据链独立复核/脱敏零残留/红线与 BOM/门禁复跑全绿；3 条非阻断建议中 2 条已落实——SHA256SUMS 行尾 LF 化＋README 问题清单补第 3 项，1 条为 UTC 时间戳知悉项）、提交待机主门三确认。

## 十、待维护者实机项（半自动分工，步骤0/3/5 合并一轮）

- ✅ 已执行（2026-08-20）：步骤0 五项能力实测（四项有结论、`run` 位置未测）＋步骤3 实机验证（50 用例全 PASS）＋步骤5 端到端首跑——首轮合并实机完成，归档 `docs/testing/v2.8-test5/`，判定 **PASS**；历程见第九节「实机轮修正」与 `docs/testing/能力实测记录_2026-08.md`。
- 遗留待测：`run` 脚本文件位置；控制台 `event`/`list_flags`/`reload` 可用性（注意 `event` 与 `effect` 同轮执行会标记翻倍，须二选一）；事件级 `hidden` 支持性（如需采用）。
- 判定档 `v2.8test5verdict.hoi4` 留本机（新黄金存档候选，待机主拍板登记）。

## 六、分步方案（A ④，门二已拍板，结论见 6.9）

> 范围（门一结论 1）：统一监测体系＝静态门禁（既有，衔接不重做）＋实机实测（新增）＋证据归档（新增）。目标：把「进游戏实测」从纯人工点验升级为「固定化：一键自检＋自动判读＋强制留痕」，全程与本仓既有四步门禁、A/C 流程、K3 复检衔接。
> 门二判定：预计新增/修改文件远超 5 个、超过 200 行 → A 流程④必设门二，等机主拍板后进⑤K3 复检。

### 6.1 需求（固化）

- 见第一节与门一结论（统一监测体系 / MOD 内自检套件 / 半自动分工 / 强制可复核）。
- 不改：MOD 现有功能逻辑、`PRC_OCS_` 既有键、AI 守卫、原版文件、BOM 规则等全部红线（AGENTS.md 十条）照旧；静态四门禁与 CI 原样保留。

### 6.2 分步改动点（六步，每步可回滚（多步回退按 6.4 依赖逆序执行）、每步过静态门禁）

**步骤0 · 本机能力实测（探索性，产出实测记录，不进发布包）**
- 验证：① `run` 脚本文件存放位置（候选：游戏根目录相对路径；失败则用 `effect PRC <effect>` 直触发兜底，官方文档已确认该命令可用）；② `log` 与 `debug = yes` 实际写 game.log 的行为与格式；③ 控制台 `effect`/`event`/`list_flags`/`reload` 在 `-debug_mode` 下可用性；④ 自检事件用 `is_triggered_only = yes` 是否影响 error.log；⑤ 事件级 `hidden` 字段在 1.19.2 的支持性及其对 error.log 的影响（对应 6.9 门二结论 2 的核实指派）。
- 产物：`docs/testing/能力实测记录_2026-08.md`（逐项 PASS/FAIL＋日志摘录）。
- 验收：五项均有明确结论；`run` 不可用时确认 `effect` 直触发兜底可行。

**步骤1 · 日志基线判读工具（纯 Python，不碰游戏脚本）**
- 新增 `tools/log_noise_whitelist.json`：机器可读白名单（gamelobby/remotefile「Failed allocate data buffer」、game.log「Conflict Risk」、daliwan「Invalid supported_version」等，自 2.4 节人工经验转写，逐条标注出处）。
- 新增 `tools/check_logs.py`：给定日志目录与轮次标识——按白名单过滤 error.log/game.log/text.log/setup.log → 输出「本轮 vs 基线」新错误签名告警 → 提取 `[OCS_TEST]` PASS/FAIL 标记 → 绑定 `code_revisions.log` 版本 → 产出 `result.json`（机器可读）＋人类可读摘要。**含显式「标记判读豁免」入口（轮次类型字段/开关，默认不豁免）：豁免轮次跳过 0 标记铁律，豁免事实写入 result.json 并在摘要高亮，防静默滥用**（K3 第 4 轮修正，供步骤2 自检落地前的首轮归档使用）。
- 新增 `tools/test_check_logs.py`：白名单过滤、新错误告警、标记提取的单元测试（含注入用例）。
- 验收：构造已知噪音日志→判净；注入未白名单错误→告警；`python -m unittest tools.test_check_logs` 全绿；既有四门禁不受影响。**判定铁律：`[OCS_TEST]` 标记数为 0＝本轮 FAIL/无效（防空真）；标记数＝自检用例清单数且全 PASS 才算通过；仅「标记判读豁免」轮次例外（豁免事实入 result.json）**（K3 第 4 轮修正）。**`code_revisions.log` 版本绑定失败时降级为人工登记版本号并记入 result.json**（K3 第 7 轮附记）。

**步骤2 · 证据留痕归档制度（强制可复核）**
- 新增归档约定：`docs/testing/<版本-轮次>/`（如 `v2.8-test5/`）——`result.json`、`清单.md`（逐项必填，缺证据＝FAIL_NOT_EVIDENCED）、脱敏日志副本、截图（命名规范）、`SHA256SUMS.txt`。
- 更新 `docs/testing/` 下回归清单模板与 `docs/DEVELOPMENT.md` 实机回归节：把「每轮至少保存并检查」升级为「逐项必填＋证据入仓＋自动判读工具出报告」，废除只记维护者结论的口径。
- 新增 `docs/testing/黄金存档制度.md`：test1–test5.hoi4 升格登记（用途、版本、SHA-256；二进制存档本身过大不入 git，本地归档＋哈希入仓；如后续需要可评估 LFS）。
- 验收：以一轮真实实测跑通新归档流程；`docs/testing/` 出现首个按新约定归档的轮次目录。**此轮使用 check_logs 的「标记判读豁免」入口（自检套件步骤 3 才落地），豁免事实随 result.json 入仓并高亮；标记判读自步骤 3 起生效**（K3 第 3/4 轮附记）。

**步骤3 · 游戏内自检套件（MOD 内 debug 门控断言）**
- 新增 `common/scripted_effects/PRC_OCS_selftest_effects.txt`：单入口 `PRC_OCS_selftest_run_suite`（内部串联全部用例）；用例模板（K3 第 2 轮修正语法）＝`if = { limit = { <触发器> } log = "[OCS_TEST] PASS <case>" }`＋**同级紧随** `else = { log = "[OCS_TEST] FAIL <case>" }`（`else` 与 `if` 同级、不得嵌在 if 块内，与本仓 events/ 既有 53 处 else 写法一致；步骤0 顺带验证 scripted_effect 内 if/else 与 log 的实际行为）。用例 case id 写在 `[OCS_TEST] PASS/FAIL <case>` 的 `<case>` 位，check_logs 直接解析自检 effects 文件取 case id 集合作「用例清单」唯一真源（K3 第 2 轮附记）。覆盖：初始化 flag、一键骷髅师决议可用（守卫依赖的条件本身，如 `PRC_OCS_initialized` flag）、特殊科研选择组关键断言（用例清单由 `docs/analysis/v2.6_特殊科研组事件映射.json` 与 `docs/analysis/v2.8_特殊科研组事件映射.json` 派生，当前口径 26 组＋17 组＝43 组，不写死数字、以映射 JSON 当前条目数为准）、MIO 资金/规模、B1–B6 已修项回归断言。**AI 守卫不进游戏内断言**：不存在能求值「决议对 AI 不可见」的 trigger，改由 `tools/test_validate_mod.py` 静态契约负责（`validate_mod.py` 已强制 is_ai/ai_will_do，登记即覆盖）。
- 新增 `events/PRC_OCS_selftest_events.txt`：`is_triggered_only = yes` 的自检事件（命名空间 `PRC_OCS_selftest`，避开 PRC_OCS 1–70 已占用 id），控制台 `event PRC_OCS_selftest.x PRC` 或 `effect PRC PRC_OCS_selftest_run_suite` 触发。**自检事件按本仓惯例带 title/desc**（K3 第 2 轮补正）：两份本地化 yml 各增相应键（UTF-8 带 BOM、英/简中同步），文件清单与工作量估计已含。
- 更新 `tools/test_validate_mod.py`：登记自检 effect 唯一性/事件命名空间/`is_triggered_only` 契约。
- 验收：实机 `-debug_mode` 触发全套自检，game.log 中 `[OCS_TEST]` 标记数＝用例清单数且全 PASS；故意破坏一条断言→对应 FAIL（负向测试）；四份日志**白名单过滤后无新错误签名**（口径同步骤1）；**新增 `common/scripted_effects/` 与 `events/` 文件显式为 UTF-8 无 BOM、本地化 yml 为 UTF-8 带 BOM**（K3 第 7 轮附记）；validate/unittest/mio --check/build 全绿。

**步骤4 · 存档断言（选型门槛，可推迟）**
- 先做选型实测：PyPI `jomini` Python 绑定能否解析本机 test1.hoi4 并读出国家 flag/科技/MIO 资金（3.1.2 rakaly 条；结论已经 §8 步骤4 实测证伪，本步＝暂缓）。
- 可行 → 新增 `tools/check_save.py`（断言清单 JSON 化，结果并入 step1 的 result.json）；不可行 → 本步登记为「引擎自检已覆盖，存档断言暂缓」，不阻塞主线。
- 验收：能对黄金存档输出结构化断言结果；或如实记录不可行结论与替代路径。

**步骤5 · 全链路编排与流程衔接**
- 新增 `tools/run_regression.ps1`：部署测试包（复用 deploy_to_local_mod.ps1 逻辑）→ 清旧日志 → 提示维护者启动实测并执行自检指令（**提示语含「已用 -debug_mode 启动」确认项**，K3 第 7 轮附记）→ 回收日志/存档/截图 → 跑 check_logs（**check_save 存在才跑，步骤4 暂缓时跳过**，与 6.4 降级口径一致）→ 生成回归报告 → 按步骤2 约定归档。
- 更新 `AGENTS.md` 验证门禁、`docs/maintenance/维护与重构标准流程.md` 衔接节：实机回归环节引用 run_regression 与归档制度（文档改动本身走 B/C 流程并在本批门三一并提交）。
- 验收：一命令跑通「部署→采集→判读→归档」；文档引用准确无悬空。

**步骤6 · K3 复检与提交（A ⑤⑥）**
- 方案与每步 diff 交 K3（kimi CLI moonshot/k3，口径见流程第三节）审计至「通过」；**每轮复检结论落定即同步文件头状态行与第七节历程，防状态漂移**（K3 第 6 轮修正）；机主确认后 git commit（推送仅凭「推送」二字）。

### 6.3 风险与对策

1. `run` 脚本位置未载明 → 步骤0 实测；兜底用 `effect` 直触发（官方文档确认，不依赖 `run`）。
2. `debug = yes`/`log` 实际行为与文档有出入 → 步骤0 实测后再定步骤3 写法。
3. 自检事件随包分发引 error.log 噪音 → `is_triggered_only = yes`＋validate 门禁＋步骤3 负向测试；仍不净则改为仅测试包注入（回退预案，改动仅 build 脚本过滤）。
4. 白名单随原版更新失效 → 白名单文件逐条标出处、check_logs 输出「未匹配新行」告警引导人工更新。
5. 存档解析库不成熟 → 步骤4 选型门槛，不阻塞主线（4.1 自检已覆盖九成需求）。
6. 二进制存档入库膨胀 → 不入 git，只入哈希与制度文档（如需 LFS 单独评估）。
7. **自检用例随 MOD 演进漂移**（K3 第 1 轮增列）：B1–B6 断言、映射组数随版本变化 → 假 PASS/假 FAIL。对策：功能改动须同批更新自检用例；用例清单由映射 JSON/契约派生而非手工维护（不写死数字）。
8. **游戏版本升级复合风险**（K3 第 4 轮附记增列）：黄金存档版本锁定不可载；日志格式/console 行为漂移。对策：版本切换即重建基线存档（制度已含版本登记）；日志格式/console 行为漂移时复跑步骤0。

### 6.4 回滚

- 步骤间存在依赖：**回滚须按依赖逆序（5→4→3→2→1）**；单步 revert 仅在该步无下游依赖时成立（K3 第 1 轮修正）。**步骤0 为纯文档产物、无下游依赖，可留可删，不入逆序链**（K3 第 4 轮附记）。
- 自检 effect/事件为新增文件，删除即完全移除；不重命名/不修改任何既有 `PRC_OCS_` 键（红线 1）。
- 流程文档对工具的引用一律写成「工具存在才生效」的降级表述，避免回滚中间步骤留悬空引用。
- `dist/` 不入库；部署侧回滚用 `tools/deploy_to_local_mod.ps1` 重装前一版本。

### 6.5 验收标准（总）

- 静态四门禁（validate/unittest/mio --check/build）全程全绿；新增单测通过。
- 实机一轮完整跑通：自检标记数＝用例清单数且全 PASS＋负向测试按预期 FAIL＋四份日志判读（白名单过滤后）无新错误签名＋证据按新制度归档入库（逐项必填）。
- 「维护者结论」与「仓库可复核证据」分离；缺证据轮次被 FAIL_NOT_EVIDENCED 拦截——历史断档类型（2.2 节）在流程上被强制阻断（K3 第 2 轮附记改可判措辞）。
- K3 对方案与 diff 终审「通过」；机主确认（门三）后提交。

### 6.6 不做什么

- 不做游戏无头/后台自动化（无官方先例，3.1.4）；不做端到端控制台键盘模拟（脆弱，超出半自动口径）；不重做静态门禁；不在本批改任何玩家可见功能；不引入新依赖超出 Python 标准库＋可选 jomini 绑定（步骤4 选型通过才引入）。

### 6.7 工作量估计

- 步骤0 实测记录、步骤1 工具＋单测、步骤2 制度＋模板、步骤3 自检套件＋契约＋两份本地化增键、步骤4 选型、步骤5 编排＋文档衔接：新增约 10–14 个文件、约 900–1300 行（含自检用例与文档），分 6 步小步实施，每步静态门禁全绿。

### 6.8 门二待拍板问题

1. 六步范围与顺序是否照批？（步骤4 存档断言按「选型门槛、不阻塞主线」处理是否接受？）
2. 自检套件是否随正式发布包常驻分发（门一已接受），还是仅测试包注入？
3. 黄金存档二进制不入 git（只入哈希与制度文档）是否接受？
4. 本批提交时，`AGENTS.md`/流程文档的衔接修订（步骤5）随批一并提交是否接受？

### 6.9 门二结论（2026-08-19 机主拍板，全选推荐项）

1. 六步方案**照批**（步骤4 存档断言按选型门槛、不阻塞主线）。
2. 自检套件**随正式发布包常驻分发**（`is_triggered_only = yes`＋无玩家入口达成玩家不可见、AI 不可用；事件级 `hidden` 字段本仓无先例、引擎支持性由步骤0核实，不支持则不用）。
3. 黄金存档**哈希入仓、二进制不入 git**（本地归档＋SHA-256 登记＋制度文档）。
4. 流程文档衔接修订（AGENTS.md/标准流程）**随本批一并提交**（门三）。

门二通过 → 进⑤K3 复检（审方案），随后⑥小步实现（每步过验证门禁）→ diff 经 K3 复检 → 机主确认（门三）→ git commit；推送仅凭「推送」二字。
