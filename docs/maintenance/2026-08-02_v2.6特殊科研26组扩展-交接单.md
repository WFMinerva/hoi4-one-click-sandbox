# 交接单：v2.6 特殊科研"逐项选择版"26 组扩展

> 2026-08-02 创建。供下一串对话直接续做。

## 基线
- 稳定基准 v2.5；v2.6-test1 已实机通过（核心 6 组，部署在 mod 目录）。
- 目标：26 组扩展（空4+陆3+海17+核2）→ v2.6-test2 → 实机 → 一次发 v2.6。

## 已完成产物
- `tools/analyze_special_project_choices.py`：已修复保留 DLC 引号；JSON 已更新。
- `docs/analysis/v2.6_特殊科研互斥选项清单.json`：32 组权威清单。
- `tools/generate_special_project_choice_events.py`：能生成事件脚本。
- `events/PRC_OCS_choice_events_more.txt`：26 组事件+陆核菜单(48/49)，has_dlc 引号正确。
- `tools/finish_choice_events.py`：已追加陆核菜单+基础本地化；静态检查通过(17脚本/221键/0警告)。

## 关键缺陷（先修）
- 生成器按 project 去重，同 project 的多个 reward 组被合并：陆要塞网络 3 组只出 1；海军 _a/_b 组丢 _b。
- 修法：改按 reward 生成唯一 id 与 flag（加 reward 后缀）。

## 待办
1. 修去重 → 重新生成 → 重跑 finish（注意 id 表）
2. 接线：组事件选项末加"返本专精菜单"(空50海51陆48核49)；补 50/51 中转菜单；主菜单 14/18 加".f"续入口跳 50/51；修 48/49 的 z 循环依赖
3. 新增陆核决议(→48/49)；test_validate_mod 的 one_time_flags 登记
4. 本地化补齐（事件 title/desc/option + 50/51 + .f）
5. 门禁：validate + unittest → 构建 v2.6-test2 + SHA → 实机 → 定版 v2.6 正式发版（工坊需维护者输密码）