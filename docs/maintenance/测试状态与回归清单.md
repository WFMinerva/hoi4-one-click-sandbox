# 测试状态与回归清单

> 总入口与状态快照见根目录 `AGENTS.md`；本文档记录各版本实机验证结论与待补回归。最近更新：2026-08-02（v2.6-test2 由维护者判定通过，证据完整性边界见下文）。

## 已确认

- v2.0a：德国启动、初始化与通用架构无报错。
- v2.0b：德国装备设计、库存、模板及独立改编逻辑通过。
- v2.0c：发现并准确定位沿海船坞效果的重复`limit`错误，版本作废。
- v2.0c1：重复`limit`修复；非PRC模板民兵改普通步兵；用户验证无误。
- v2.0c2：新增24个满编主力纯步师；用户验证无误并认定稳定。
- v2.0：只做正式化清理，游戏逻辑与v2.0c2一致。
- v2.1：五类特殊科研项目决议（含衍生科技授予、首都补建核反应堆、重复执行补齐机制）经维护者实机验证通过（2026-07），认定稳定并已上传Steam工坊。
- v2.2：PRC特殊科研后二阶段编制、最终喷火坦克、全5登陆车、4个同名装甲支援车、4种全5特殊轰炸机及可重复24师均由维护者在中文环境确认通过；2026-07-30 已上传Steam工坊（Manifest `6814589756424432401`）。完整清单见 `docs/testing/v2.2-test1_实机回归清单.md`。

## v2.2 验证过程与限制

- 上一测试包（SHA-256 `5b1f5fbfd54c5f4e4eafdfd59e290dcb78c6e4a6774b5a3ca57851160947288e`）的中文环境实测只确认“主力步坦师·进化型”兵牌修改成功。司令部尚未检查；装甲师、海军陆战队装甲师原本没有手调兵牌目标，不能记为“修改失败”。
- 同轮日志中 `error.log` 为0字节，`game.log` 仅见已知的原版 `Conflict Risk` 杂音。
- `test2`（2026-07-30）确认“司令部”兵牌正确。维护者同时确认基础初始化“装甲师”实际为8机械化＋10中型自行火炮，并接受当前版本保留、后续版本改为10＋8；该结论不涉及12＋6的“装甲师·进化型”。
- `test2` 对应新日志中 `error.log` 为0字节，`game.log` 仅见已知 `Conflict Risk` 杂音，`setup.log` 正常生成，`text.log` 为0字节且中文文本正常显示，判为未发现文本/本地化错误。维护者随后明确确认其余回归项目全部通过。

## 建议发布前或下一阶段补做的回归

1. PRC：完整检查顾问、军工机构、中国型号、民兵模板、23座补给站和双电网。
2. 英国：检查本土、殖民地和临时占领区作用域。
3. 美国或苏联：检查大量州的建设规模、性能和重复执行。
4. 小国：检查无沿海州、少量州或无军工机构时的行为。
5. 傀儡国：检查可用性与PRC身份分支不串线。

这些是扩大样本的建议，不改变维护者已经作出的 v2.2 稳定版认定。

## 每轮日志

- `error.log`
- `game.log`
- `setup.log`
- `text.log`（必须检查并保存；0字节可表示未发现文本/本地化错误）
- 建设、模板、设计和新增部队截图

## 当前稳定线：v2.6

- v2.6-test2 覆盖特殊科研原型奖励逐项选择版26组扩展：空军4组、陆军3组、海军17组、核能2组；生成映射保证每组独立事件与国家标志，同项目多组互不遮蔽。
- 维护者于2026-08-02判定实机点验通过。仓库保存的日志摘要为：`error.log` 仅6行原版网络噪音、`game.log` 仅1条原版 Conflict Risk 杂音、`setup.log` 正常加载5个事件文件且 `PRC_OCS_choice_events_more.txt` 加载30个事件、`text.log` 0字节。
- 证据边界：`docs/testing/v2.6-test2_实机回归清单.md` 的逐项结果与证据位置没有填写，原始日志和截图也未提交。上述内容属于维护者确认和摘要记录，不能作为每一项均可在仓库中独立复核的证据；不进行事后补填。
- v2.6 正式包在单位机同一工作区由 `tools/build_release.py` 连续两次构建，SHA-256一致（`484030065363ab1366604d17e2f92ee114208b2edb7ffe13b001a0172de48ff1`）；Git标签`v2.6`已推送，GitHub Release已挂附件，Steam工坊物品`3767025052`已于2026-08-02更新（Manifest `3609146315690391433`）。
- 跨机器字节审计发现官方附件含单位机工作区混合 LF/CRLF；13 个差异文本统一换行后与标签内容完全一致，但官方 SHA 不能从干净标签检出复现。当前构建器统一暂存文本为 LF，v2.6 规范化审计重建 SHA-256 为 `9b4cc601ca9c82e59541665a076f16c972dab9c18d6871677f4887e2df1e7467`；该结论属于发布溯源，不改变实机功能判定。

## v2.5 历史稳定线

- v2.5 在v2.4修复集之上新增间谍情报条线：一键创建间谍机构与一键点满五大部门升级；维护者于2026-08-01判定实机回归通过。
- 正式包SHA-256为`4bb5a8ef72d08310a93259082789eb4cc3fcf8887e1a776bc1d58ef71c470c80`；Git标签、GitHub Release与Steam工坊上传均已完成，旧Manifest `7423542268120748938`已被v2.6覆盖。

## v2.4 历史稳定线

- v2.4-test1 覆盖 v2.3 之上的 6 项修复：雷达科技归位空军特殊科研（1–5 级全亮）、进化装备设计与库存同步、船坞/民用工厂可重复点至上限 20、陆地不再获得五级碉堡、新增可重复点数决议 `PRC_OCS_add_points`、本地部署加载结构修复。
- 维护者于2026-08-01实机逐项点验未发现问题；`error.log` 仅原版网络噪音（gamelobby.cpp/remotefile.cpp）、`game.log` 仅原版 Conflict Risk 杂音、`setup.log` 正常加载 PRC_OCS 分类#1/决议#17/事件#13、`text.log` 0 字节，被认定测试通过。
- 正式包由 `tools/build_release.py` 连续两次构建，SHA-256 一致（`b65afcb8b685c01aeb554736f11927115329029a8590eaf3cc0f1945ec203aa1`）；Git 标签 `v2.4` 已推送，Steam 工坊物品 `3767025052` 已于 2026-08-01 更新（Manifest `385131838239799795`）；GitHub Release 挂正式包 ZIP 附件。

## v2.4 后续可选扩大回归

1. PRC：完整检查顾问、中国型号、民兵模板、23 座补给站与双电网（本次回归以功能修复为主，未逐项重查增强层）。
2. 英国：抽查本土、殖民地与临时占领区作用域。
3. 美国或苏联：抽查大量州建设的规模和性能。
4. 无海岸小国与傀儡国：抽查灰显说明、可用性与身份分支。

这些是扩大样本建议，不改变维护者已经作出的 v2.4 稳定版认定。

## v2.3 历史稳定线

- v2.3-test1的10＋8装甲师目标与维护者手调的 `test3.hoi4` 不一致，测试线作废。
- v2.3-test2按 `test3.hoi4` 将基础装甲师修正为12机械化＋6中型自行火炮；PRC实机回归未发现问题。
- v2.3-test3—test4将主力步坦师及其进化型中的轻型自行火炮纠正为中型自行火炮，并把进化编制开放给所有非AI玩家国家；德国实测通过。
- v2.3-test5—test8根据德国 `test4.hoi4`、英国 `test5.hoi4` 与后续存档逐步验证共享MIO直接作用域；删除会留下剩余点数的额外资金和等级，只保留目标特质。
- test9/test10整理日本、苏联、澳大利亚、捷克斯洛伐克、意大利、美国等国家的实际已点路线。
- v2.3-test11将共享MIO扩展为生成器维护的全国家通用表：444家公司、4945项目标特质，不按国家tag分支；PRC原有4家专属路线独立维护。
- MIO方针仍是人工步骤：原版公开脚本接口没有为指定MIO挂载或选择方针的效果。
- v2.3-test12只设置 `fire_only_once = no`，实测切换国家后整个分类仍消失，确认问题不在决议完成记录。
- v2.3-test13将分类 `allowed` 从 `is_ai = no` 改为 `always = yes`，让开局由AI控制的国家也预先实例化分类；决议仍以 `is_ai = no` 与 `ai_will_do = 0` 阻止AI使用。
- 维护者于2026-07-31实机确认同局切换国家后分类正常出现，并认定测试通过。
- v2.3已上传Steam工坊既有物品 `3767025052`（Manifest `7223939852636260892`）。

## v2.3 后续可选扩大回归

1. PRC：完整检查顾问、中国型号、民兵模板、23座补给站与双电网。
2. 英国：抽查本土、殖民地与临时占领区作用域。
3. 美国或苏联：抽查大量州建设的规模和性能。
4. 无海岸小国与傀儡国：抽查灰显说明、可用性与身份分支。

这些是扩大样本建议，不改变维护者已经作出的v2.3稳定版认定。