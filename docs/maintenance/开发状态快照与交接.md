# 开发状态快照与交接

> 本文定位：**工具链无关的开发交接快照**。在任意机器、任意 AI 工具链（单位机 Cline + DeepSeek、家用机 Codex 等）下拉取本仓库后，先读本文即可无缝继续工作。
>
> **一句话交接（2026-08-20，v2.8 发版串）：** 本串已完成 **v2.8 正式发版**（提交线 `717fcec..d4085e7`，标签 `v2.8` 已推送）——v2.7 之上缺陷修复与补全集（#2–#6：情报训练中心/支援补给船 tooltip/史前喷气 BBA 反门/42 组官方文案/石墨重水/巡洋潜艇/22 组互斥装备加成/陆军 9 项 generic 加成/MIO 资金规模与最强路线）＋#7 共享 MIO 机构守卫（DLC 门禁 354 个＋PRC 双覆盖＋自检 51 用例）；正式包 SHA-256 `e34f0e462b07e5b7d0f810655eb7a31069a893ae5cb00d2eb60860c074c8b6e9`（31 文件），GitHub Release v2.8 已发布（资产 digest 一致）；Steam 工坊物品 `3767025052` 已上传 v2.8（Manifest `6381266305931940230`，workshop_log `Upload finished : OK`）；玩家致谢匿名化已随发布文案生效。下一串待办＝F8 四类天才顾问、F1–F5 新功能、D1/D2/D7 决策点、实机监测机制遗留补测（run 位置/event/list_flags/reload/hidden）、v2.8-test6 玩家复测回执与 51 用例自检轮补归档。
>
> 最近更新：2026-08-20（v2.8 正式发版串：变更记录 #7 完成——玩家反馈 #6「个别机构没点/少点」根因定案为玩家游戏停在 1.19.1、表按 1.19.2 生成；生成器补机构守卫＋自检补共享 MIO 链用例；门一/门二/门三全部拍板签收；分支已推送远端；GitHub Release v2.8 已发布；工坊上传进行中）。同日另起 A 立项「三扇门带选项机制」（§10）：门一/门二/门三已拍板，⑤K3 两轮通过，⑥四步实现门禁全绿、diff 复检通过，已提交并推送远端。

## 1. 环境与工具链

### 1.1 单位机（本次开发环境，2026-08-02）

| 项目 | 值 |
|------|-----|
| 设备定位 | 单位电脑 |
| 操作系统 | `Get-ComputerInfo` 返回 **Windows 10 Home China**，版本 2009（Build 26200），64 位 |
| 外观识别 | VS Code 环境显示 Windows 11（Build 26200 属 Win11 构建线，产品名仍报 Win10，以系统查询结果为准） |
| IDE | VS Code |
| AI 工具链 | Cline + DeepSeek 模型 |
| 默认 Shell | PowerShell（系统默认为 `cmd.exe`，实测常以 PowerShell 会话执行） |
| Python | 3.14.6 |
| Git | 2.55.0.windows.3 |
| Node.js | v24.18.0 |
| npm | 11.16.0 |
| 游戏本体路径 | `D:\SteamLibrary\steamapps\common\Hearts of Iron IV` |
| Paradox 用户目录 | `C:\Users\Admin\Documents\Paradox Interactive\Hearts of Iron IV`（`logs\`、`save games\`、`mod\`） |

### 1.2 家用以外的其他机器

本快照对工具链无关。家用机可使用任意工具链（如 Codex）继续；`docs/maintenance/` 下的既有文档（`README_FIRST.md`、`技术实现与通用化边界.md`、`测试状态与回归清单.md`）与本文共同构成交接入口，均不绑定具体模型/IDE。

## 2. 当前状态快照（防误会的核心声明）

| 项 | 状态 |
|----|------|
| 版本标识 | **v2.8 正式基准（v2.7 之上缺陷修复与补全集＋共享 MIO 机构守卫，见 4.4–4.11）** |
| 发布状态 | **已发版完成**：Git 标签 `v2.8` 已推送（提交 `d4085e7`）；正式包 ZIP SHA-256 `e34f0e462b07e5b7d0f810655eb7a31069a893ae5cb00d2eb60860c074c8b6e9`（31 文件）；GitHub Release v2.8 已发布（资产 digest 一致）；Steam 工坊物品 `3767025052` 已上传 v2.8（Manifest `6381266305931940230`，2026-08-20 15:32 workshop_log `Upload finished : OK`） |
| 与 Steam 工坊差异 | 无差异；工坊已挂 v2.8 正式内容 |
| 验证等级 | v2.8-test1–test6 静态门禁全程全绿＋K3 逐批复检通过；玩家「一般通」8 项点验 1–5/8 过、6 差异已定案为玩家版本滞后、7 代码级已锁；本机 test6 会话加载干净（error.log 0 MOD 错误）；51 用例自检轮待补归档 |
| 开发分支 | `agent/cross-machine-governance`（已推送远端）；标签 `v2.8` 指向发布提交 `d4085e7` |
| 下一步 | 工坊上传收尾（Manifest 回填）→ v2.8-test6 玩家复测回执 → 后续待办＝F8 四类天才顾问、F1–F5、D1/D2/D7、实机监测机制遗留补测（详见顶部「一句话交接」） |

## 3. 本轮已完成并实机验证（v2.6：特殊科研原型奖励·逐项选择版 26 组扩展）

| # | 交付项 | 说明 |
|---|--------|------|
| 1 | 26 组逐项选择版 | 空军 4（地震炸弹/直升机/洲际轰炸机/空天母舰，菜单 50）、陆军 3（要塞网络·混凝土加固一/二/通信改造，菜单 48）、海军 17（护航航母 A/B、冰航母、袖珍潜艇、现代战列舰 A/B、现代航母 A/B、核导弹潜艇、核潜艇、导弹潜艇、潜水母舰、超级战列舰 A/B、支援船·支援/维修方案、补给船，菜单 51）、核能 2（核同位素分离/反应堆测试完成，菜单 49）；数值全部取自原版 |
| 2 | 修复 project 去重 | `tools/generate_special_project_choice_events.py` 按 reward 生成唯一事件 id/flag（`docs/analysis/v2.6_特殊科研组事件映射.json`），同 project 多组互不遮蔽 |
| 3 | 交互接线 | 组事件选项末返回本专精菜单；新增陆/核"选择原型奖励"决议；空军/海军主菜单 `.e` 续入口；菜单 z 设专精完成 flag 隐藏决议 |
| 4 | 本地化 | `tools/finish_choice_events.py` 幂等生成四菜单与双语键（修复 test1 的 46/47 标题错位） |
| 5 | 发布与部署工具 | `tools/publish_workshop.py`：英文简介 ASCII 引号改弯引号并压缩至 4832 字符后方可上传；`tools/deploy_to_local_mod.ps1`：本地部署回归脚本（解决超长内联命令的输出吞没问题） |

涉及文件（除生成本身）：
- `common/decisions/PRC_OCS_decisions.txt`（新增陆/核 choose 决议 2 个，air/naval OR 扩新组）
- `events/PRC_OCS_choice_events[_air2/_naval/_more].txt`（事件 22–47 + 菜单 48–51 + 14/18 `.e` 续入口）
- `localisation/english/PRC_OCS_l_english.yml`、`localisation/simp_chinese/PRC_OCS_l_simp_chinese.yml`
- `tools/test_validate_mod.py`（登记新决议 + 26 组映射/菜单 z 回归测试）
- `docs/analysis/v2.6_特殊科研组事件映射.json` 等分析产物
- `docs/publishing/Steam工坊中文简介_v2.6_BBCode.txt`（工坊简介 v2.6 双语压缩版）

发布工具使用纪律：任何发布/构建/上传类操作动手前先列出 `tools/` 并读 `docs/DEVELOPMENT.md` 对应章节，确认仓库已有脚本与方法后再执行（AGENTS.md 门禁已固化）。工坊正式上传用 `python tools/publish_workshop.py --steamcmd <路径>\steamcmd.exe --username <账号>`；仅生成暂存与VDF时加 `--prepare-only`。GitHub Release 用 `python tools/publish_github_release.py --version <版本>`。

v2.6 之前第 3 节的 v2.5 间谍情报条线（2026-08-01 正式化）已随 v2.6 并入「版本与发布状态」历史。

## 4. 未决待办

### 4.1 本轮原始需求中未完成项（玩家反馈）

| # | 待办 | 状态/原因 |
|---|------|-----------|
| 1 | ~~潜艇 MIO 选择威力而非隐蔽~~ | **已关闭（维护者 2026-08-03 拍板暂不处理）**：原拟改为全局隐蔽线，因牵涉 `tools/generate_universal_mio_effect.py` 生成器与约 467K 的共享 MIO 文件重构，维护者确认暂不处理；如需重开再评估 |
| 2 | ~~间谍机构没有研发~~ | **已完成（v2.5）**：新增"一键组建间谍机构"+“一键升满机构部门”，无需科技与国策前置，`upgrade_intelligence_agency` 引擎命令逐条点满五大部门 |
| 3 | **获得两种坦克和飞机科技**（无 DLC / 有 DLC 重复） | 约定"先不管"，未处理 |
| 4 | **特殊研发科技强度**（超音速战斗机默认非最高质量、巡洋潜艇只能二选一、巡洋舰失去可靠性） | `complete_special_project` 引擎限制，暂无脚本化优化方案；需要时单独研究 |
| 5 | ~~一键满训练度（骷髅师）~~ | **已完成（v2.7）**：最终形态为「一键骷髅师」决议，复用成熟被动经验方案（经验直喂、不受训练上限封顶），现役/后续新编部队海量经验直冲满级；持续 7 天可刷新、无需先点一键开局、暂停（时间冻结）不生效；此前交接线索 `set_training_level` 经验证在 1.19.* 引擎不存在 |
| 6 | ~~科研项目选项决议效果描述不清晰~~ | **已完成（v2.7）**：「选择原型奖励」各选项补齐具体数值与取舍说明（`custom_effect_tooltip` 双语 `_tt` 键，事件 22–47） |

### 4.2 其他记录项

- 历史玩家反馈中的"PP 未获得"经排查为误报/无复现，本轮已把点数功能剥离为独立决议，规避歧义。
- 普通雷达科技（`radio_detection`/`cavity_magnatron`）已归入空军特殊科研，与高三级雷达（`centimetric_radar`/`phased_array`/`monopulse_radar`）一同在完成空军特殊科研后授予。

### 4.3 上一串启动清单（2026-08-03 交接，已全部完成）

上一串的两项待办（一键满训练度/科研选项效果描述优化）及 v2.7 正式发版均已在本串完成，详见顶部「一句话交接」、第 2 节与 4.1 表。下一串的待办仍见 4.1 表（4.1.1 已由维护者拍板关闭；4.1.3/4.1.4 为原版引擎限制项，需要时单独研究）。

### 4.4 v2.8 反馈批次（2026-08-18 新增，整理评估已入库）

- 2026-08-18 收到维护者转交的新一批用户反馈（一般通两段），已完成逐条整理评估并归档至 `docs/analysis/v2.8_用户反馈整理与评估_2026-08-18.md`（含反馈原文存档）。
- 已核实缺陷 3 组：B1 情报机构「本地化训练中心」升级遗漏（原版 `upgrade_training_centers` 未覆盖）；B2 支援船支援/维修方案与补给船选项工具提示为占位文案、无具体效果；B3 拥有喷气 DLC 时仍研发史前喷气科技并创建其满改设计（关联 4.1.3 飞机部分）。
- 高需求新功能 7 组（稳定度/战争支援、换意识形态、换法案、建设扩展、加速建设、MIO 规模、MIO 最强路线）与需拍板项 4 组（天才顾问、学说观感/精通度、阵营作弊包）已列入文档第六节批次规划；决策点 D1–D7 待维护者拍板后开搞。
- 小众功能 8 项（附庸自治、改善关系、顺从度、租借/志愿军/宣战借口、间谍意识形态支持、神风、女性飞行员、国家特殊科技）用户已表态可不搞，暂缓仅留档。

### 4.5 v2.8 变更记录 #2（C 升级流程，2026-08-18 起）

状态行：①对齐需求 完成（门一已拍板）→②立基线 完成（`3006f0d`）→③轻调研 完成 →④方案落盘 完成（门二已拍板）→⑤小步实现+回归 完成（四步全绿，`1c38273`/`cfa6ceb`/`283180b`/`2e4a7b4`）→⑥K3复检 完成（三轮，终审通过，修复 `a992ec1`/`c072d25`）→⑦交付 完成（玩家“一般通”2026-08-18 实机回归：B1/B2/B3 三项修复确认生效；同时上报两个新缺陷——巡洋潜艇 MtG 模块分支未复刻、陆军陆地巡洋舰选择缺失，已立案转入 4.6 变更记录 #3）。门三：玩家回归结果已记录，待机主签收（随 #3 门一/门二一并把关）。流程依据：工具库 `docs/立项程序.md` C 升级流程；推送仅凭机主「推送」二字。

- **⑦交付准备（2026-08-18）**：本机已部署 v2.8-test1——`mod\OCS_one_click_sandbox_start_v2_0`（common/events/localisation 镜像 + descriptor 2.8-test1，层级核验无嵌套），注册 .mod 由 packaging 引导并按 descriptor 同步，`dlc_load.json` 已启用本地注册 mod（原档备份 `dlc_load.json.bak`；备份内机主原启用 `mod/war_diary.mod`、`mod/ugc_3762643540.mod`，回归完成后如需还原原列表可覆盖回写或经启动器重选）。测试 README 与回归清单已拷入 mod 根目录。游戏日志目录现存均为 2026-08-05 旧档（上轮 v2.7 会话），实机回归后按新时间戳判读。交付物自洽性已复核：测试包 zip 哈希与包外 SHA256 文件一致（`21fefe82…`）、包内 descriptor 为 2.8-test1、配套文档 2 个在包。同日按机主指示把测试包（zip + SHA256 文件 + 测试说明.txt，含防 Expand-Archive 嵌套安装要点、三项点验与四份日志回收说明）复制到桌面，交用户实机测试；待用户反馈日志与结果后判读并回门三。

- **需求**（自 4.4 反馈批次，逐条编号见分析文档）：修缺陷 B1（机构升级补本地化训练中心）、B2（支援船支援/维修方案、补给船选项显示科技效果）、B3（拥有喷气 DLC 时不研发史前喷气科技、不创建其满改设计，关联 4.1.3 飞机部分）；候选新功能 S1/F1–F11 按机主拍板范围纳入。
- **不改**：小众功能 8 项（暂缓）；4.1.1 潜艇 MIO 重构（已关闭）；4.1.4 引擎限制项；原版国策/历史/事件链（红线 6）；AI 守卫（红线 2）不变。
- **调研（已先行落盘，详见分析文档）**：B1 缺原版 `upgrade_training_centers`（1 级）；B2 原版权威做法为选项级 `custom_effect_tooltip = tech_effect|sp_naval_..._pick_x`（原版 `common/special_projects/projects/naval_projects.txt` 同构）；B3 史前喷气科技/装备属 pre-BBA，需按 `NOT = { has_dlc = "By Blood Alone" }` 反门（待门一确认 DLC 口径）。F1–F11 引擎可行性待④方案阶段对照 `effects_documentation.md` 逐项定案。
- **未尽之意**：见 4.5 附「门一待拍板清单」；2026-08-18 曾直接落盘实现 B1/B2/B3 未经门一，已按机主指示回退（工作区已恢复至提交 `8aa155a` 干净基线）。
- **门一结论（2026-08-18 机主拍板）**：本批范围＝缺陷修复 B1+B2+B3（含坦克科技 DLC 同批核对）；B3 的 DLC 口径＝By Blood Alone；版本线＝2.8-test1；K3 复检＝codex（kimi 备用）。后续批次口径登记：F6 重开（资金+规模拉满、等级不动）、F7 只改其余公司的确定性路线、F8 四类天才顾问全上、F11 暂缓等语义澄清；推送仅凭机主「推送」二字。
- **②立基线**：基线提交 `3006f0d`（变更记录草稿），工作区干净；门禁留底（v2.7 未动内容）＝validate 0 警告 / unittest 23 项 OK / mio --check 一致。
- **③轻调研结论**：
  - 坦克科技：1.19 原版史前坦克科技线已整体移除（`gw_tank`/`light_tank_1` 等均不存在），NSB 底盘科技与 `mtg_tank_landing_craft` 均无 DLC 门禁 → 4.1.3 坦克部分随游戏版本自然消除，本批不改坦克科技。
  - 喷气（B3）：`jet_fighter1/2`、`jet_tactical_bomber1/2`、`jet_strategic_bomber1` 在 1.19 属 Götterdämmerung 特殊科研衍生科技（挂 `sp_air_jet_engine`/`sp_air_axial_jet_engine`，自身无 BBA 门），其装备为 pre-BBA 遗留 → 按门一口径 `NOT = { has_dlc = "By Blood Alone" }` 反门；`sp_centrifugal_jet_tech` 为 Götterdämmerung 喷气引擎科技，保持无条件。
- **④方案**（预计 9 文件、约 120 行）：
  - B1：`PRC_OCS_decisions.txt` 机构升级决议 Operative Training Branch 段补 `upgrade_intelligence_agency = upgrade_training_centers`（1 行）。
  - B2：事件 43/44/45 选项级 `custom_effect_tooltip` 改为 `tech_effect|sp_naval_support_ships_pick_a/b/c`、`tech_effect|sp_naval_repair_ships_pick_a/b/c`、`tech_effect|sp_naval_underway_replenishment_pick_a/b`（原版 `naval_projects.txt` 奖励选项同构，引擎自动渲染官方效果）；删除 hidden_effect 内死代码 tooltip 行；45.b 保留 `_tt`；两份 yml 删除 8 个不再引用的占位键。
  - B3：`PRC_OCS_special_project_effects.txt` 空军专精把 5 个史前喷气键移入 `if = { limit = { NOT = { has_dlc = "By Blood Alone" } } }`；`PRC_OCS_equipment_effects.txt` 两分支把 `jet_strat_bomber_equipment_1`/`jet_tac_bomber_equipment_2` 满改设计包进同款反门；空天母舰/洲际轰炸机设计不动。
  - 版本与文档：`descriptor.mod`、`packaging/OCS_one_click_sandbox_start_v2_0.mod` 升 `2.8-test1`；新建 `docs/testing/README_v2.8-test1_测试包.md`（SHA-256 构建后回填）。
  - 测试：更新 v2.7 tooltip 契约（43–45 改 tech_effect\| 规则）、新增 B1 机构升级契约、B3 BBA 门禁契约。
  - 验收：四步门禁全绿＋三条新契约通过；实机回归项（门三前机主点验）＝LaR 下机构部门点满含训练中心、菜单 51 支援船/补给船选项悬浮显示官方效果、有 BBA 无史前喷气科技与满改设计且无 BBA 行为与 v2.7 一致。
  - 回滚：`git revert` 本批提交即回 v2.7；`dist/` 不入库；实机回滚用 `tools/deploy_to_local_mod.ps1` 重装 v2.7 包。
  - 门二判定：预计改动超 5 文件阈值 → 按 C 流程④设门二，等机主拍板后进⑤。
- **门二结论（2026-08-18 机主拍板）**：拍板通过，进⑤实施。
- **⑤小步实现（四步，每步回归全绿）**：步骤1 `1c38273`（B1＋版本线 2.8-test1＋冻结测试补 test 豁免；validate 0 警告、24 项测试 OK）；步骤2 `cfa6ceb`（B2 事件改 `tech_effect|`＋yml 清占位键＋恢复 BOM＋tooltip 契约更新；366 对本地化键＝374−8、24 项 OK）；步骤3 `283180b`（B3 科技与设计双 BBA 反门＋新契约；25 项 OK、mio --check 一致）；步骤4 `2e4a7b4`（测试包 README＋实机回归清单；构建通过，测试包 SHA-256 `2611fc85b93df545eed61a6511015dea9962806fe255fd9c15a3a8ef8997fa71`，包内清单所载值受自引用滞后、以 dist 侧 SHA256 文件为准）。
- **⑥K3 复检**：codex 0.146.0 审计三轮，第 3 轮终审「通过」。历程：第 1 轮发现阻断——拥有 BBA 时进化决议 `available` 仍无条件要求史前喷气科技（B3 后不再授予），决议将永久灰显、空天母舰/洲际轰炸机设计连带失效，另报回归清单自引用 SHA 不一致；修复（`a992ec1`）后第 2 轮发现 OR 极性反了（`NOT = { has_dlc }` 在 BBA 下仍灰显、无 BBA 下取消科技门槛）且契约断言锁死错误极性、B3 契约窗口检查属极性盲区；二修（`c072d25`）改正向 `OR = { has_dlc = "By Blood Alone" AND = { 两科技 } }` 并改正向断言、删除盲区检查；终审真值推演逐条吻合（有 BBA 放行、无 BBA 两科技齐备放行、缺一不放行）判定通过。非阻断建议登记：效果文件侧的窗口断言仍属同类盲区，留待下批改为解析级断言（当前语义正确）；codex 审计环境只读不能实跑门禁，本机已实跑四步门禁全绿。测试包最终 SHA-256 `21fefe820b2cea6fdca1700c8b5e5df210b4f45c6b574c788017ee82172d5bef`（以 dist 侧 SHA256 文件为准）。

### 4.6 v2.8 变更记录 #3（C 升级流程，2026-08-18 起，家用机维护环境）

状态行：①对齐需求 完成（门一已拍板：全量修 16 组＋巡洋潜艇、新建火箭菜单、含弃权项与原版一致）→②立基线 完成（基线 `5cdf078`，工作区干净；静态门禁留底：validate 0 警告 / unittest 25 项 OK / mio --check 一致）→③轻调研 完成→④方案落盘 完成（门二已拍板通过）→⑤小步实现 完成（工具链+事件/决议/本地化/测试+版本文档+构建，静态门禁全绿，见下方实施记录）→⑥K3 复检 完成（kimi CLI moonshot/k3 1 轮终审通过）→⑦经确认提交 完成（`b076743` 已提交并推送远端）。流程依据：`docs/maintenance/维护与重构标准流程.md` C 升级流程。

- **需求**（玩家 2026-08-18 实机反馈，逐条根因已核实，证据见下方「调研」）：
  - N1 巡洋潜艇：原版奖励按 DLC 分支——有 Man the Guns 时解锁模块（`ship_extra_fuel_tank` 油箱 / `ship_light_battery_sub` 潜艇主炮），无 MtG 时给装备加成（航程/鱼雷）；本 MOD 事件 19 只复刻了无 MtG 分支，MtG 玩家模块从未解锁。
  - L1 陆军菜单：原版陆地巡洋舰有 8 组互斥原型选择（弹药/装配/底盘/通信/引擎/悬挂/履带/炮塔），超重型榴弹炮、自行超重型榴弹炮各 1 组三选一；v2.6 分析工具因 `equipment_bonus` 属项目作用域而整体排除，菜单 48 无对应选项。
  - 同类遗漏（门一拍板一并修）：海军 4 组（AIP 引擎/消声瓦/核鱼雷/近炸引信）、火箭 2 组（弹道导弹制导系统/飞行炸弹设计）。
- **不改**：风味组 11 组（要塞坍塌/领导方针、核能 5、火箭 3——无装备效果）；单选项自动奖励与 `project_output`（脚本完成已正常触发，非本批范围）；B1/B2/B3 已修内容；4.1.1/4.1.4 关闭项；修改红线不变。
- **调研**（本机原版 1.19 实文件核对，2026-08-18）：
  - `naval_projects.txt` 105-175：巡洋潜艇唯一奖励两选项均含 `enable_equipment_modules`（MtG 分支）与 `add_equipment_bonus`（无 MtG 分支），与本 MOD 事件 19 对比确认只移植了后者。
  - `land_projects.txt` 446-760+：陆地巡洋舰 8 组 `unique_prototype_rewards` 均为 `equipment_bonus`（项目作用域）+ 进度增减；榴弹炮两组同构；本 MOD 分析工具 `is_selectable_buff`（`tools/analyze_special_project_choices.py` 329-334 行）要求至少一个选项含国家作用域 buff，上述组全部被排除——重跑本机原版得 87 个奖励、32 个可选组（与 v2.6 清单一致），另有 16 个「≥2 选项但被排除」的装备加成组＝本批修复对象。
  - 可行性证据：`enable_equipment_modules` 在原版科技文件（`technologies/MTG_naval.txt` 等）于国家作用域使用 → 事件选项可用；`add_equipment_bonus` 在原版国策（`national_focus/germany.txt` 等）与 v2.6 生成事件（舰船/飞机）均有先例；陆地底盘/火箭装备为本 MOD 首次使用，列入实机点验。
  - 原版面板文案（英文，中文自译）：巡洋潜艇「Flexible Operations / Armaments」；底盘「Redesign the Chassis Entirely / Reinforce the Chassis / Use Lighter Materials」等，新选项文案沿用该口径。
  - 事件 ID 空间：1-53 已占用（53 在 `PRC_OCS_events.txt`），本批新组用 54-69，火箭菜单用 52。
- **④分步方案**（预计 12+ 文件、约 500 行含生成）：
  1. 工具链（步骤1）：`tools/analyze_special_project_choices.py` 新增「可复刻」分类（`equipment_bonus` → `add_equipment_bonus` 具名移植；`enable_equipment_modules` → 国家作用域可用），产出新清单 `docs/analysis/v2.8_特殊科研互斥选项清单.{json,md}`；`tools/generate_special_project_choice_events.py` 增量模式——冻结既有映射（22-47），16 新组分配 54-69，`equipment_bonus` 翻译为 `add_equipment_bonus = { name = PRC_OCS_<reward>_<option>_bonus … }`；`tools/finish_choice_events.py` 扩展菜单 52（火箭）与 16 组双语 GROUP_INFO/GROUP_TT（含弃权项 NO_OP 口径）。
  2. 事件与菜单（步骤1-2）：重新生成 `events/PRC_OCS_choice_events_more.txt`（42 组事件 + 菜单 48/49/50/51/52）；`events/PRC_OCS_choice_events_naval.txt` 事件 19 加 MtG 分支（`if/else` 结构，参照事件 20 已有写法），18.a 标签与 choose_naval 决议描述同步；`common/decisions/PRC_OCS_decisions.txt` 新增火箭决议 `PRC_OCS_choose_rocket_special_project_bonuses`（镜像核能决议结构，`is_ai = no`＋`ai_will_do = { factor = 0 }`），land/naval 决议 available 的 OR 列表补新组 flag。
  3. 本地化（步骤1-2）：`localisation/english|simp_chinese/PRC_OCS_l_english|simp_chinese.yml` 双语同步（带 BOM），新增约 16 组标题/描述/选项/菜单键。
  4. 测试（步骤2）：`tools/test_validate_mod.py` 更新计数与映射契约；新增契约——事件 19 MtG 分支（`enable_equipment_modules` 含两模块）、新组 `add_equipment_bonus` 名称唯一性与结构、菜单 48/51/52 接线、火箭决议守卫（红线 2）、双语键数对等。
  5. 文档与打包（步骤3-4）：交接文档本表实施记录、分析文档归档；版本线 `2.8-test2`（`descriptor.mod`＋`packaging/OCS_one_click_sandbox_start_v2_0.mod` 同步）、测试包 README 与实机回归清单、`tools/build_release.py` 构建与 SHA-256、`tools/deploy_to_local_mod.ps1` 本地部署。
- **验收**：静态四步门禁全绿（validate/unittest/mio --check/build）＋新增契约通过；实机点验（机主）＝有 MtG 时巡洋潜艇选项解锁模块且设计器可安装、陆军 10 组逐项可选数值生效、海军 4 组与火箭菜单 2 组可选、无 MtG 行为与 v2.7 一致、四份日志洁净。
- **回滚**：`git revert` 本批提交回基线；事件/本地化为生成产物，可重跑工具还原；`dist/` 不入库。
- **门二判定**：预计改动超 5 文件/200 行阈值 → 按 C 流程④设门二，等机主拍板后进⑤。
- **⑤小步实现**（四步，每步静态门禁全绿）：
  - 步骤1 工具链：`analyze_special_project_choices.py` 新增 `is_replicable_buff`（所有 buff 均为 `equipment_bonus`/`enable_equipment_modules` 项目作用域键、≥2 选项）与「可复刻」清单输出，重跑原版得 87 奖励/32 可选/16 可复刻；`generate_special_project_choice_events.py` 增量模式（v2.6 冻结 22-47、v2.8 新组 54-69，`_rework_effect` 把 `equipment_bonus` 翻译为具名 `add_equipment_bonus`、保留 `instant = yes`、丢弃 `country_effects` 进度/内部 flag 与 `scientist_effects`）；`finish_choice_events.py` 增菜单 52 与 16 组 GROUP_INFO/GROUP_TT，并修 `_option_letter` 跳过 d/t/z（海军菜单 21 项时第 18 项曾撞标题键 `.t`）。
  - 步骤2 事件/决议/本地化/测试：重新生成 `PRC_OCS_choice_events_more.txt`（42 组事件 + 菜单 48-52）；事件 19 加 MtG `if/else` 分支并补 `_tt` 提示、18.a 标签与 choose_naval 决议描述同步（灵活运用/武装）；`decisions.txt` 新增火箭决议（is_ai=no + ai_will_do=0 + 触发 PRC_OCS.52），land/naval 决议 OR 列表补 10/4 个新组 flag；双语 yml 同步；`test_validate_mod.py` 新增 v2.8 映射/工具提示/MtG/火箭决议契约并扩展菜单 z 契约至 48-52。
  - 步骤3 版本与文档：版本线 `2.8-test2`（descriptor + packaging 同步）；新建 `docs/testing/README_v2.8-test2_测试包.md` 与 `v2.8-test2_实机回归清单.md`。
  - 步骤4 构建：`tools/build_release.py` 通过，测试包 SHA-256 `81bd469f41f1c57f2c60171544ce4f674bbe22ebedc9f95755dd4ec50ac9c10b`（清单验证 29 文件）。
  - 步骤5 交付准备：`tools/deploy_to_local_mod.ps1` 已把 v2.8-test2 部署到本机 MOD 目录（common/events/localisation 镜像 + descriptor 2.8-test2 + 注册 .mod 同步 + 测试 README/回归清单拷入 mod 根；`dlc_load.json` 已启用本地注册 mod，无需改备份），供机主实机回归。
  - 静态门禁终态：validate 0 警告 / unittest 29 项 OK / mio --check 一致（444 组织、4945 特质）/ build 通过。
- **⑥K3 复检**：kimi CLI `moonshot/k3` 1 轮终审「通过」。核验佐证：16 组映射 54-69 与源清单 JSON 逐条比对一致（land 无 instant、naval 64-67 保留 `instant = yes`、country_effects 进度/内部 flag 均丢弃、无 buff 选项仅设 flag）；事件 19 双分支、菜单 48/51/52 与 z 全 flag 接线、火箭决议双 AI 防线与 PRC_OCS.52 触发、双语键同行号对等均符合验收与红线。非阻断建议登记：事件 19 沿用 v2.6 手工链的 `sp_cruiser_submarine_eq_bonus_*` 命名（非 PRC_OCS_ 前缀），与既有 18-21 链约定一致，不属本批引入。
- **⑦经确认提交（门三）**：已完成——提交 `b076743`，机主 2026-08-19 拍板「推送」后已推至 `origin/agent/cross-machine-governance`。

### 4.7 v2.8 变更记录 #4（C 升级流程，2026-08-19 起，本机维护环境）

状态行：①对齐需求 完成（门一已拍板：F6 资金+规模拉满、特质不动；F7 按特质加成强度总和排序；F8 拆到 #5）→②立基线 完成（`261d974`）→③轻调研 完成→④方案落盘 完成（门二已拍板）→⑤小步实现 完成（F6 `46d77e0`/F7 `56cf2c7`，静态门禁全绿，见下方实施记录）→⑥K3 复检 完成（kimi moonshot/k3 1 轮终审「通过」）→⑦经确认提交（门三待机主签收；推送仅凭「推送」二字）。流程依据：`docs/maintenance/维护与重构标准流程.md` C 升级流程。

- **需求**（承接 4.4 反馈批次 F6/F7/F8，门一 2026-08-19 已拍板）：
  - F6：新增独立决议「一键拉满 MIO 资金与规模」——对当前国家**实际拥有的所有**军工机构（含其他 MOD 新增机构）拉满资金（funds）与规模（size）；**不自动解锁特质**（`complete_mio_trait`），特质由玩家自行用资金解锁。语义澄清：MIO 只有资金/规模两轴，size 即俗称「等级/规模」，「等级不动」＝不自动解锁特质。
  - F7：`tools/generate_universal_mio_effect.py` 把「其余公司」（非 PRC 4 家专属、非 test7–test10 已点样本）的默认路线由「特质数量最多」改为「特质加成强度总和最高」（equipment_bonus/production_bonus 数值总和），并重建约 444K 产物。
  - F8（已拆到 #5）：角色制天才顾问与 F6/F7 机制差异大，经机主 2026-08-19 拍板另立变更记录 #5，本批不做。
- **不改**：PRC 4 家专属 MIO 路线与 test7–test10 已点样本（F7 只改其余公司）；不自动解锁 MIO 特质；AI 守卫、原版国策/历史/事件链、`PRC_OCS_` 前缀、BOM 规则等红线不变。
- **调研**（本机原版 1.19 实文件核对，已完成）：
  - F6：`effects_documentation.md` 有 `every_military_industrial_organization`（COUNTRY 下遍历所有 MIO，含 `limit` 过滤）、`add_mio_funds`（越 Size Up 上限会连带涨 size）、`add_mio_size`（直加 size，不可负）、`set_mio_funds`；`triggers_documentation.md` 有 `has_military_industrial_organization`/`has_mio_size`。当前源码按 v2.3 决策不含 `add_mio_size`/`add_mio_funds`（`test_validate_mod.py` 第 583–584 行 `assertNotIn` 需反转）。
  - F7：定位 `maximum_legal_route()`（line 147–167，现按 `len(selected)` 最大取路线）；`analyze_mio_archetypes.py` 的 `trait_from_block()`（line 199–220）**刻意丢弃** `equipment_bonus`/`production_bonus`/`organization_modifier` 数值，需扩展解析器捕获加成值后再写「功能最强」打分函数。
  - F8（#5 预调研，本批不做）：1.19 顾问为**角色(character)制**——`common/unit_leader/00_traits.txt` 军职特质带 `specialist/expert/genius_advisor_trait` 三档（`_1`/`_2`/`_3`），「天才档」＝`_3` 特质且 `skill > 7`（`has_genius_level_trigger`）；任命/强化走 `activate_advisor`/`add_trait`/`add_skill_level` 等角色效果，与现 PRC 层 `add_ideas`（idea 制）思路不同，政治顾问/部长/理论家无统一天才分级，需逐槽位枚举——留待 #5 深调研。
- **④分步方案**（F6+F7，预计 10+ 文件、约 300 行含重建）：
  1. F6 效果：`PRC_OCS_mio_effects.txt` 新增 `PRC_OCS_maximize_mios_effect`——`every_military_industrial_organization = { add_mio_funds = 100000 add_mio_size = 20 }`（`add_mio_size` 直加规模、`add_mio_funds` 加资金；原版 `FUNDS_FOR_SIZE_UP = 700`、`FUNDS_FOR_SIZE_UP_LEVEL_POW = 1.8`，规模 20 级再升级约需 15 万资金，10 万资金不触发连带升级）。
  2. F6 决议：`PRC_OCS_decisions.txt` category 内新增 `PRC_OCS_maximize_mios`（镜像 `PRC_OCS_resupply`：`is_ai = no`＋`ai_will_do = { factor = 0 }`＋`available` 需 `PRC_OCS_initialized` flag＋`days_re_enable = 1` 可重复）。
  3. F6 本地化：双语 yml 新增决议 title/desc/tt 键。
  4. F6 测试：`test_validate_mod.py` 反转第 583–584 行断言（改为 `PRC_OCS_shared_mio_effects.txt` 仍不含、新 effect 含 `add_mio_size`/`add_mio_funds`），新增决议 AI 守卫/available 契约。
  5. F7 解析器：`analyze_mio_archetypes.py` `trait_from_block()` 不再丢弃 `equipment_bonus`/`production_bonus`/`organization_modifier`，捕获其数值存入 trait data。
  6. F7 打分：`generate_universal_mio_effect.py` 新增 `trait_strength()`（equipment_bonus＋production_bonus 各数值绝对值总和，方向无关、org 内部 modifier 不计），`maximum_legal_route()` 由「特质数最多」改为「加成强度总分最高」（平局按特质数→字典序）。
  7. F7 重建：重跑生成器重建 `PRC_OCS_shared_mio_effects.txt` + `mio_generator_manifest.json`，`--check` 一致。
  8. F7 测试：`test_validate_mod.py` 更新计数/映射契约，新增「其余公司功能最强、PRC 4 家/test7–test10 样本不变」契约。
  9. 版本与文档：版本线升 `2.8-test3`（descriptor + packaging 同步），新建 `docs/testing/README_v2.8-test3_测试包.md` 与 `v2.8-test3_实机回归清单.md`，交接文档补实施记录。
  10. 构建：`tools/build_release.py` 构建测试包并记 SHA-256；`tools/deploy_to_local_mod.ps1` 本地部署供实机回归。
  - **门二判定**：改动远超 5 文件/200 行 → 按 C 流程④必设门二，等机主拍板后进⑤。
- **⑤小步实现**（两步，静态门禁全绿）：
  - 步骤1 F6 `46d77e0`：`PRC_OCS_mio_effects.txt` 新增 `PRC_OCS_maximize_mios_effect`（`every_military_industrial_organization { add_mio_funds = 100000 add_mio_size = 20 }`）；`PRC_OCS_decisions.txt` 新增决议 `PRC_OCS_maximize_mios`（`is_ai = no`＋`ai_will_do = 0`＋需 initialized flag＋可重复）；双语 yml 补 title/desc 并恢复 BOM；`test_validate_mod.py` 反转 v2.3 的 `assertNotIn` 并新增决议契约。validate 0 警告 / 30 项 OK / mio --check 一致。
  - 步骤2 F7 `56cf2c7`：`analyze_mio_archetypes.py` `trait_from_block()` 捕获 `equipment_bonus`/`production_bonus`/`organization_modifier`；`generate_universal_mio_effect.py` 新增 `trait_strength()`（equipment+production 绝对值总和）并把 `maximum_legal_route()` 改为按强度总分取路线（平局按特质数→字典序）；重建 `PRC_OCS_shared_mio_effects.txt`（444 机构、特质 4945→4944、样本 9 前缀不变）+ manifest；`test_validate_mod.py` 计数改 4944 并新增 `trait_strength` 契约。validate 0 警告 / 31 项 OK / mio --check 一致。
  - 静态门禁终态：validate 0 警告 / unittest 31 项 OK / mio --check 一致（444 组织、4944 特质）。
- **⑥K3 复检**：kimi CLI `moonshot/k3` 1 轮终审「通过」。核验佐证（K3 自跑门禁与真实数据核验）：validate 0 警告（517 对本地化键数对等）/ unittest 31 项 OK / `--check` exit 0（444 机构、4944 特质，样本机构在 `ignored_sample_organizations`、PRC 4 家在排除表）；F6 效果仅含 `every_military_industrial_organization` + 两条 add 命令、无 `complete_mio_trait`，决议镜像 `PRC_OCS_resupply` 模式（is_ai=no＋initialized flag＋`ai_will_do=0`＋可重复）；F7 `trait_strength` 对真实游戏数据（501 机构/7499 特质）实测 0 条嵌套加成、0 条有加成零分，扁平解析无漏计，`maximum_legal_route` 三级决胜（分数→特质数→字典序）确定性成立；红线纯追加无重命名、无 limit、未触原版文件。无阻断问题。
- **实机回归发现 + 修复（2026-08-19，B 轻流程）**：维护者实机回归 v2.8-test3 时 `error.log` 报 4 条 `enable_equipment_modules` 无效 effect（`events/PRC_OCS_choice_events_naval.txt` 事件 19 的 54/78 行）。根因：该 effect 仅存在于科技 `on_research_complete` 与特殊科研 `iteration_output` 专用作用域，#3 N1 误移植进事件，MtG 模块解锁实际未生效。已按机主拍板方案 A 修复：19.a MtG 分支改 `set_technology = { cruiser_submarines = 1 }`（原版科技内含 `enable_equipment_modules = { ship_extra_fuel_tank }`，授予即解锁附加油箱模块），19.b 合并为无条件 `add_equipment_bonus`（torpedo_attack 0.1，因 `ship_light_battery_sub` 无科技可授予改加成兜底）；双语 tooltip 同步、测试契约改 `assertNotIn enable_equipment_modules` + `assertIn set_technology cruiser_submarines`。K3 复检「通过」（validate 0 错误、31 单测过、双语 BOM/517 键对等）。已重新部署供复测。
- **验收**（④定案）：F6 实机点验＝有/无其他 MOD 机构时资金与规模均拉满、特质未自动解锁、四份日志洁净；F7＝其余公司路线改为功能最强且 PRC 4 家/已点样本不变、生成器 `--check` 一致。
- **回滚**：`git revert` 本批提交回基线；`shared_mio_effects.txt` 为生成产物可重跑工具还原；`dist/` 不入库。

### 4.8 v2.8 变更记录 #5（C 升级流程，2026-08-19 起）

状态行：①对齐需求 完成（门一已拍板：B4 选项标签官方化＋B5 支援/补给船 tooltip 结构＋B6 补石墨/重水设计选择＋全部 42 组选项文字统一官方中文）→②立基线 完成（`7501a6e`）→③轻调研 完成→④方案落盘 完成（门二已拍板）→⑤小步实现 完成（B4 `9e157c9`/B5 `0968e50`/B6 `caaf324`，静态门禁全绿）→⑥K3 复检 完成（kimi moonshot/k3 终审「通过」）→⑦经确认提交（门三待机主签收；推送仅凭「推送」二字）。流程依据：`docs/maintenance/维护与重构标准流程.md` C 升级流程。

- **需求**（玩家 2026-08-19 实机反馈，逐条已核实根因）：
  - B4 选项标签与 buff 不对应：`finish_choice_events.py` 用「隐蔽/折中/强度」等通用模板套所有潜艇/舰船组，但原版各 reward 语义不同（核潜艇 38 实际是「成本降低/质量」、导弹潜艇 39 的 a 是「航程+可见度」），标签与效果错位。修法＝改用原版官方中文选项名（与文字统一同批完成）。
  - B5 支援船/补给船文字消失：#2 B2 修复把 `custom_effect_tooltip = tech_effect|…` 嵌进 `hidden_effect`（被隐藏），选项级仍引用占位键。修法＝把 tech_effect 提到选项级、set_technology 放回 hidden_effect。
  - B6 重水反应堆缺失：`sp_nuclear_reactor` 有 3 个互斥奖励（设计选择石墨/重水、同位素分离、反应堆测试），核能菜单 49 只做了后两个；重水选项设 `nuclear_reactor_heavy_water_flag` 决定 `project_output` 授予 `nuclear_reactor_heavy_water` 还是普通 `nuclear_reactor` 科技。修法＝补「石墨/重水」选择组并正确授予科技/建筑。
  - 文字统一官方中文：42 组（v2.6 的 26 组 + v2.8 新增 16 组）选项 title/desc/option 标签全部改用原版中文本地化，不用概括性文案。
- **不改**：巡洋潜艇主炮（`ship_light_battery_sub` 无科技可授予，维持加成兜底）；红线不变（`PRC_OCS_` 前缀、AI 守卫、原版文件、BOM/双语对等）。
- **③轻调研**（本机原版 1.19 实文件核对，已完成）：
  - 官方选项名在 `localisation/simp_chinese/special_projects_l_simp_chinese.yml`（英文在 `english/special_projects_l_english.yml`），按选项 token 键控。实测：核潜艇 38 `sp_naval_nuclear_submarine_preference_a/c` =「削减成本/追求最高质量」（非「隐蔽/强度」）、核导弹潜艇 37 =「保持低调/忍受风暴」、巡洋潜艇 19 =「机动/军备」——`finish_choice_events.py` 的硬编码模板确系错配。
  - B6：`sp_nuclear_reactor` 的 `sp_nuclear_design_choice_reward`（石墨/重水）重水选项设 `nuclear_reactor_heavy_water_flag`，`project_output` 按该 flag 授予 `nuclear_reactor_heavy_water` 或普通 `nuclear_reactor` 科技；mod 菜单 49 漏此组。
  - B5：事件 43/44/45 的 `custom_effect_tooltip = tech_effect|…` 被嵌在 `hidden_effect` 内（不可见），选项级仍引用占位键。
  - 新发现（Issue 5）：`generic_prototype_rewards`（含正面 `sp_land_generic_reward_artillery_bonus_*` 火炮杀伤、坦克装甲等）是**迭代奖励**，`complete_special_project` 瞬间完成不触发，mod 未授予——正面 buff 存在缺口（负面/随机类同理未触发）。
- **④分步方案**（B4+B5+B6+文字统一，预计 12+ 文件、约 600 行含生成）：
  1. B5：`finish_choice_events.py` 43/44/45 的 `custom_effect_tooltip = tech_effect|…` 提到选项级、`set_technology` 放 hidden_effect，重建事件+本地化。
  2. B6：`finish_choice_events.py` 新增核能菜单 49 的「设计选择」组（事件 70，石墨/重水）；重水选项 `set_country_flag = nuclear_reactor_heavy_water_flag`＋`set_technology = { nuclear_reactor_heavy_water = 1 }`＋首都补建重水反应堆；decisions 加核能菜单入口。
  3. B4+文字统一：写提取脚本从原版双语 `special_projects_l_*.yml` 按选项 token 取官方名称，重写 `finish_choice_events.py` 的 42 组选项标签表，重建事件+双语本地化。
  4. 测试：`test_validate_mod.py` 更新选项标签契约（官方名）、新增 B6 石墨/重水契约、B5 tech_effect 选项级契约。
  5. 版本文档：升 `2.8-test4`、测试包 README/回归清单、构建 SHA、本地部署。
  - **门二判定**：改动超 5 文件/200 行 → 按 C 流程④必设门二，等机主拍板后进⑤。
  - **待机主拍板（新）**：Issue 5 正面 generic 奖励（火炮杀伤/坦克装甲等）是否补授？若补，需提取全部 generic rewards 并只挑正面授予（跳过负面/随机类），工作量另计。
- **⑤小步实现**（三步，静态门禁全绿）：
  - B4+文字统一 `9e157c9`：`finish_choice_events.py` 42 组选项标签改为官方双语名（从原版 `special_projects_l_*.yml` 提取，含 `$scripted_loc$` 递归解析，落库 `docs/analysis/v2.8_特殊科研选项官方名称.json`）；重建双语本地化。validate 0 警告 / 31 测试 OK。
  - B5 `0968e50`：事件 43/44/45 的 `tech_effect|…` 提到选项级、`set_technology` 平铺进 `hidden_effect`（8 选项）。validate 0 警告 / 31 测试 OK。
  - B6 `caaf324`：核能菜单补「石墨/重水」设计选择——事件 70 + v2.8 映射 eid70（menu 49）+ 决议 OR 补 flag + 双语本地化 + 契约（mapping 17、eids 54-70、menu 加 49）。validate 0 警告 / 32 测试 OK。
- **⑥K3 复检**：kimi CLI `moonshot/k3` 1 轮终审「通过」。核验：B4 37/38 标签=官方名、双语 BOM/461 键对等；B5 无嵌套 hidden_effect、45.b 保留 _tt；B6 事件 70 石墨仅设 flag/重水含 flag+set_technology+首都补建、菜单 49.c→70、决议 OR 含 design_choice flag；红线未触原版、`nuclear_reactor_heavy_water_flag` 为原版 flag 名（有意兼容）。非阻塞观察：`test_v28_choice_group_tooltips_contract` 遍历 range(54,70) 未覆盖 70 的 _tt，留待下批扩到 71。
- **Issue 5**：机主已拍板「补正面 generic 奖励」，另立变更记录 #6（本批不做）。

### 4.9 v2.8 变更记录 #6（C 升级流程，2026-08-19 起）

状态行：①对齐需求 完成（门一已拍板：仅陆军 9 个 equipment_bonus、一次性全给）→②立基线 完成（`6f58152`）→③轻调研 完成→④方案落盘 完成（门二已拍板）→⑤小步实现 完成（`6f58152` + K3 二修）→⑥K3 复检 完成（1 轮「需修改」→二修后「通过」）→⑦经确认提交（门三待机主签收；推送仅凭「推送」二字）。流程依据：`docs/maintenance/维护与重构标准流程.md` C 升级流程。

- **需求**（Issue 5，机主已拍板「补正面 generic 奖励」）：`complete_special_project` 瞬间完成不触发迭代奖励，正面 generic 奖励存在缺口。
- **③轻调研**（本机原版 1.19 实文件核对，已完成）：
  - `equipment_bonus` 类（玩家说的「火炮杀伤/坦克装甲」）**仅陆军 9 个**：`sp_land_generic_reward_{armor,engine,artillery}_bonus_1/2/3`——armor_value/hardness/最大速度/可靠性/soft_attack/hard_attack/air_attack 各 +2~3%。
  - `add_tech_bonus` 类（MIO 研究速度）15 个（陆/海 armor/artillery_tech、空军 discoveries、核能 industry/electronics）——mod 已一键全科技，研究速度冗余。
  - XP/进度类无用（mod 已给 999 经验、项目瞬完）；负面类（test_failure/resource_scarcity/critical_failure/political_interference）跳过。
- **④分步方案**：
  1. 新增 scripted effect `PRC_OCS_grant_land_generic_bonuses_effect`：9 个 `add_equipment_bonus`（项目作用域 equipment_bonus → 国家作用域 add_equipment_bonus，名字 `PRC_OCS_generic_<theme>_bonus_<n>`）。
  2. 在 `PRC_OCS_complete_land_special_projects_effect` 里调用（完成陆军特殊科研时授予）。
  3. 双语 tooltip（可选，或并入现有完成说明）。
  4. 测试契约 + 升 `2.8-test5` + 构建部署。
  - **门二判定**：预计 2-3 文件、约 40 行 → 低于阈值，门二按机主拍板执行。
- **门一结论（2026-08-19 机主拍板）**：范围＝仅陆军 9 个 equipment_bonus（add_tech_bonus 冗余不补）；授予方式＝完成陆军特殊科研时一次性全给。
- **⑤小步实现** `6f58152`：`PRC_OCS_special_project_effects.txt` 新增 `PRC_OCS_grant_land_generic_bonuses_effect`（9 个 add_equipment_bonus，数值取自原版 `generic_land_prototype_rewards.txt`），在 `PRC_OCS_complete_land_special_projects_effect` 的 set_technology 后、set_country_flag 前调用；新增 `test_v28_land_generic_bonuses_contract`。validate 0 警告 / 33 测试 OK。
- **⑥K3 复检**：kimi `moonshot/k3` 1 轮「需修改」2 项——①文件头注释过时（仍称「唯一损失是核反应堆」，与新增 equipment_bonus 补偿矛盾）；②契约测试不校验数值。二修：更新文件头注释、契约改为按 name 切片断言装备键/stat/数值。二修后门禁全绿，K3 判定「通过」。

### 4.10 v2.8-test5 玩家复测与工坊致谢（待办，2026-08-20 收工登记）

- **已交付**：v2.8-test5 玩家测试说明已起草并经机主转发玩家「一般通」——本地文件 `C:\Users\Admin\Desktop\v2.8-test5_玩家测试说明.txt`（桌面文件不入库）；配套测试包 `dist\开局一键爽玩_v2.8-test5_测试版.zip`（SHA-256 `e8df5bd4c98b6fae1f484608e7ab551a7fc1ebb8ba7b2c70620767d0524f495d`）。点验 8 项＝巡洋潜艇（N1）/陆地巡洋舰 16 组/核·导弹潜艇官方文案（B4）/支援·补给船 tooltip（B5）/石墨重水（B6）/MIO 资金规模＋最强路线（F6/F7）/陆军 9 项 generic 加成（#6）/老问题复核（雷达·间谍·PP·史前喷气·碉堡）。
- **待办 1（等玩家回执）**：8 项点验结果回收 → 全过则按发布门禁走 v2.8 正式发版；发现问题按既有 C 流程立案。**（2026-08-20 已收）**：1–5、8 过；6 立案变更记录 #7（见 4.11）；7 无法观测（非缺陷，代码级已锁）；玩家「可发包」。
- **待办 2（等玩家 Steam 名）**：机主已向玩家致谢并索取 Steam 账户名（或玩家想展示的信息），回复后在工坊简介「作者与源码」节后新增「特别致谢」节（中英双语），随 v2.8 正式上传生效。**（2026-08-20 更新）**：玩家希望**匿名**——正式落稿时把模板中「〈Steam账户名〉」改为「一位匿名玩家」/ "an anonymous player"，不再等待玩家名。模板（占位待填）：

  > 中文：
  > `[h2]特别致谢[/h2]`
  > `[list]`
  > `[*]感谢玩家「〈Steam账户名〉」在 v2.7–v2.8 测试期间的持续实机反馈：雷达与间谍机构补全、碉堡观感、特殊科研逐项选择、支援/补给船提示、巡洋潜艇与陆地巡洋舰模块、MIO 路线方向、42 组官方文案统一等改进，均由其一条条实测报出并推动落地。`
  > `[/list]`
  >
  > 英文：
  > `[h2]Special Thanks[/h2]`
  > `Sincere thanks to player "[Steam name]" for extensive in-game testing and feedback across v2.7–v2.8 — radar/intelligence fixes, special-project choice rework, supply-ship tooltips, cruiser-submarine & land-cruiser modules, MIO route direction, and official-localization alignment.`

  - 注意：描述内引号只用「」或弯引号，禁用 ASCII 双引号（VDF 提前闭合坑，见第 5 节第 7 条）；正式落盘时新建 `docs/publishing/Steam工坊中文简介_v2.8_BBCode.txt` 并同步 v2.8 更新说明。

**其他已知事项**：本地测试环境部署用 `powershell -ExecutionPolicy Bypass -File tools/deploy_to_local_mod.ps1`（自动发现 Paradox 目录；当前 v2.7-test1 已部署）；`error.log` 中 `ugc_1326075315.mod` 的 `Invalid supported_version = "1.*.*"` 是第三方 MOD「大礼万 / daliwan」杂音，与本 MOD 无关。

### 4.11 v2.8 变更记录 #7（C 升级流程，2026-08-20 起）

状态行：①对齐需求 完成（门一已拍板：治本全套／按 1.19.2 最新版为基准／保持 1.19.2 表数据不重建／2.8-test6 玩家复测→正式发版）→②立基线 完成（`717fcec`；静态门禁留底＝validate 0 警告 / unittest 36 项 OK / mio --check 一致 444+4944）→③轻调研 完成→④方案落盘 完成（门二已拍板）→⑤小步实现 完成（步骤1 `54a4a35`／步骤2 `35515d4`／步骤3 `5e29d90`，静态门禁全绿，见下方实施记录）→⑥K3 复检 完成（kimi moonshot/k3 1 轮终审「通过」，2 条非阻断观察）→⑦经确认提交 **完成**（门三已签收 2026-08-20：机主拍板「快速点验一键开局→转玩家复测」；提交 `094517a` 含实施记录；推送仅凭「推送」二字，尚未推送）。流程依据：`docs/maintenance/维护与重构标准流程.md` C 升级流程。

- **需求**（玩家 2026-08-20 v2.8-test5 复测回执＋机主门一拍板）：
  - 玩家 8 项点验：1–5、8「已达到描述效果，无异常」；6（MIO 资金规模＋最强路线）「基本达到，个别机构（如非主要国家的机构）出现没点或少点」；7（陆军 9 项 generic 加成）「无法观测」（非缺陷报告，代码级已锁：unittest 契约校验名称/stat/数值＋K3 已审，发布说明列数值）。玩家总体「可发包」，致谢希望匿名。
  - 玩家日志（桌面 `3.zip` 四份）判读实锤 2 类 201 条本 MOD 运行时错误：190× `has_military_industrial_organization: Could not find proper MIO … NZL_nz_railways_2_organization`（`PRC_OCS_shared_mio_effects.txt:7278`，整机构路线没点）＋ 11× `complete_mio_trait: could not find trait …`（`USA_army_ordnance_department` 缺 10 个通用中坦特质、`GER_man` 缺 `GER_mio_trait_high_velocity_armament`，少点 11 特质）；时间线 11:08:53 点 USA / 11:10:33 点 GER（tag 切换多国点验，与反馈描述吻合）。其余错误行全部第三方 MOD/原版杂音（MM_*、GM_*、原版汉化、ugc_*、第三方 `unlock_trait_command` 无效命令 24 条）；`text.log` 0 字节、`game.log` 仅已知 Conflict Risk 杂音。
  - **根因定案（门一）**：本机实为 **1.19.2.0**（黄金存档明文 `Operation Postern v1.19.2.0.a729 (7a45)`，构建 2026-06-29）；玩家游戏停在旧热修（1.19.1），而 MIO 表按 1.19.2 数据生成——`NZL_nz_railways_2`（1.19.2 新增的火车模板机构，块名带 `_2`、name 沿用旧机构）与 11 个特质（1.19.2 新增/改名）在其版本不存在。机主拍板：**按最新版 1.19.2 为基准、表数据不重建**；旧版差异由文档说明承载（玩家后续更新游戏即完整）。
  - **结构性加固（本批主体）**：生成器不读机构 `allowed` 门禁——表内 **260/444 机构**在原版带 `has_dlc` 门禁（15 个 DLC：AAT 83、GoE 34、Götterdämmerung 23、ToA 22、BfB 22…），缺 DLC 的玩家会成片触发无效键错误＋对应机构没点。修复＝生成器按原版 `allowed` 生成同款 `has_dlc` 守卫。
- **不改**：F6/F7 路线打分逻辑；444 机构/4944 特质数据本身（除守卫外零变动）；玩家版本差异不修数据（文档说明）；红线全部不变。
- **③轻调研结论**：
  - 原版 501 机构定义（30 通用模板＋471 国家机构），表覆盖 448（444 共享＋4 PRC 专属）；`analyze_mio_archetypes.py --vanilla <根>` 可对任意版本重建。
  - `allowed` 块扫描（527 块、411 机构含 has_dlc）：键集＝has_dlc(602)/tag/original_tag/original_TAG/AND/OR/NOT/is_debug/always/is_literally_china(_not_prc)/PHI_SEA/ROOT/NOR_AAT，**仅 1 处 FROM**（`SOV_okmo_organization`，在表内）。
  - 名键理论证伪：表内 23 机构块名≠name、原版 5 对重名机构，玩家日志仅 NZL_nz_railways_2 报错 → 引擎按块名键控 MIO，名不一致无害，NZL 缺失确属玩家版本差异。
  - 官方文档：`has_military_industrial_organization`（COUNTRY 作用域）；`is_military_industrial_organization`（INDUSTRIAL_ORG 作用域身份比较）存在，但动态遍历重构改动面过大，本批守卫方案即可达目标。
  - 盲区根因：自检套件 50 用例只测 F6 资金/规模，**从未执行共享 MIO 路线链**；`check_logs.py` 对 error.log 全行白名单过滤、未命中即新签名——补用例后未来键漂移自动报警。
- **④分步方案**（预计 11 文件、约 300 行含重建）：
  1. 步骤1 分析器：`analyze_mio_archetypes.py` 解析机构 `allowed` 块→守卫内容；键集白名单如上；**含 FROM 的 allowed（仅 SOV_okmo 1 个）不复制、维持现状**。
  2. 步骤1 生成器：`generate_universal_mio_effect.py` 为可复制 allowed 的机构生成 `limit = { <allowed 内容> has_military_industrial_organization = X }`（OR 结构保真）；重建 `PRC_OCS_shared_mio_effects.txt`＋`mio_generator_manifest.json`；`--check` 一致。
  3. 步骤2 自检：`generate_selftest_effects.py` 新增用例 `mio_shared_chain`（执行 `PRC_OCS_configure_shared_mios_effect` 后 `log` PASS）；重建 `PRC_OCS_selftest_effects.txt`（50→51 用例）。
  4. 步骤2 契约：`test_validate_mod.py` 新增——守卫契约（260 门禁机构 limit 含对应 has_dlc；GER_man 类 OR 保真；SOV_okmo 无守卫）、自检用例计数 51；既有 444/4944 计数不变。
  5. 步骤3 文档：交接文档实施记录；测试包 README/玩家复测说明注明「MIO 表按最新 1.19.2 生成，旧热修版本个别机构/特质不存在属版本差异，更新游戏即完整」。
  6. 步骤3–4 版本与构建：升 `2.8-test6`（descriptor＋packaging 同步）；`build_release.py` 构建测试包＋SHA；玩家复测说明（仅复测 MIO 项＋四份日志干净性）。
- **验收**：静态四步门禁全绿＋新契约通过；实机回归（机主）＝51 用例全 PASS（含 `mio_shared_chain`）、check_logs 判 PASS、error.log 无新签名；玩家复测＝MIO 资金规模拉满＋路线点选正常、四份日志无 `PRC_OCS_shared_mio_effects` 错误。
- **⑦经确认提交（门三）**：已完成——机主 2026-08-20 签收，拍板「快速点验一键开局→转玩家复测」；桌面已投递 test6 包＋SHA256＋`v2.8-test6_玩家测试说明.txt`（复测 MIO 项＋回报游戏版本号）。**待办**：①机主点验「一键开局」后复查 error.log 确认共享 MIO 链零错误（本机非正式轮已确认加载干净：setup.log 9 行 PRC_OCS 加载记录、error.log 0 条 MOD 错误、text.log 0 字节；未跑自检套件故 check_logs 判 INVALID，9 类新签名均为本机显卡/内存噪音）；②玩家复测回执后按发布门禁走 v2.8 正式发版（含匿名致谢落稿、构建/SHA、GitHub Release、工坊上传）。
- **回滚**：`git revert` 本批提交回基线；共享表/自检产物重跑工具还原；`dist/` 不入库。
- **门二判定**：改动约 11 文件/300 行（含 467K 重建产物）→ 超 5 文件/200 行阈值 → 按 C④ 必设门二，等机主拍板后进⑤。
- **门二结论（2026-08-20 机主拍板）**：照批方案，进⑤实施。
- **⑤小步实现（三步，静态门禁全绿）**：
  - 步骤1 `54a4a35`：`analyze_mio_archetypes.py` 新增 `ALLOWED_GUARD_KEYS` 白名单／`render_assignments`／`prc_dual_coverage`／`allowed_guard_lines`（解析机构 `allowed` 块；含 FROM 或白名单外键→返回 None 维持裸守卫；多词值自动加引号）；`generate_universal_mio_effect.py` `render_route` 加 guard_lines 参数与 `dlc_guarded_organizations` 统计；重建共享表（444 机构/4944 特质不变，带 has_dlc 守卫 **354** 个）＋manifest。中途自测抓出并修复两处：多词 DLC 名无引号输出、PRC 引用触发红线 3（改双覆盖后 validate 归零）。
  - 步骤2 `35515d4`：`generate_selftest_effects.py` 新增固定用例 `mio_shared_chain`（执行共享 MIO 路线链＋log PASS，错误报警由 check_logs error.log 新签名兜底），重建自检产物（50→51 用例）；`test_validate_mod.py` 新增 `test_v28_shared_mio_dlc_guards_contract`（444 守卫计数／354 带 DLC／NZL·GER_man OR 保真／USA 无 DLC／SOV_okmo FROM 豁免裸守卫／PRC 双覆盖），自检计数契约 50→51。validate 0 警告 / 37 测试 OK / mio --check 一致 / selftest --check 一致。
  - 步骤3 `5e29d90`：升 `2.8-test6`（descriptor＋packaging 同步）；新建 `docs/testing/README_v2.8-test6_测试包.md` 与 `v2.8-test6_实机回归清单.md`（含「MIO 表按最新 1.19.2 生成，旧热修版本个别机构/特质不存在属版本差异」说明）。
  - 步骤4 构建：`build_release.py` 通过（清单 31 文件），测试包 SHA-256 `42cecd85636b6d821b08099e917aa08b858bab1b115d7f76d1d0f8c5721a4b4f`；待本地部署供机主实机回归。
- **⑥K3 复检**：kimi CLI `moonshot/k3` 1 轮终审「通过」。核验佐证：全量扫描原版 551 个 allowed 块——含 FROM 仅 SOV_okmo（白名单外键亦仅此一处）；NZL/GER_man/CHI_camco 守卫与 vanilla 逐字一致、SOV_okmo 维持裸守卫；PRC 双覆盖改写生效（PRC_dalian、CHI_camco NOT 内）；16 种 has_dlc 写法多词名全部带引号（含逗号/冒号名）；自检产物 51 用例计数成立、check_logs error.log「all」模式白名单外新签名报警机制现成；manifest 前后 444/4944 不变、`--check` 实测一致；validate 0 异常、37 测试 OK、工作树干净；diff 仅 11 文件，无 decisions/国策/历史/事件/localisation/dist、无 PRC_OCS_ 重命名、common 产物无 BOM、单 limit 通过、descriptor/packaging 版本一致、送审 diff 与实际 hunk 数吻合。**非阻断观察 2 条**：①审计抽样文本中 CHI_camco 一处 `has_dlc = NCNS` 系转写失真，产物实为带引号原文（实现正确）；②`AGENTS.md:16` 历史叙述「4945 个目标特质」与现口径 4944 属既有表述差异（v2.3→F7 后计数变化），与本批无关，择机统一。

### 4.12 v2.9 变更记录 #8（C 升级流程，2026-08-22 起，批次 1）

状态行：①对齐需求 完成（门一已拍板 2026-08-22：批次 1 先行——B7 沿海民用按钮缺陷＋T1 学说预设调整＋T2 共享 MIO 前三项路线修正；T2 潜艇隐蔽线＝4.1.1 重开项，待 D12 另拍；批次 2–5 后续再立项）→②立基线 完成（`10d1f03`：评估文档＋本变更记录草稿；静态门禁留底＝validate 0 警告 / unittest 37 OK / mio --check 报「out of date」→ 根因＝本机原版构建 (d245) 与 #7 生成时家用机构建 (7a45) 数据差异＋生成器浮点累加顺序非确定性，机主拍板 D13＝接受 d245 基准）→③轻调研 完成（本机原版 1.19.2 实文件核对，见下）→④方案落盘 完成（门二已拍板 2026-08-22：按推荐方案全批——commandos 卓越单兵＋infiltration_tactics 渗透战术＋reserve_officers_spirit 优势火力系精神；版本线 2.9-test1）→⑤小步实现 完成（步骤 1–5，见下实施记录）→⑥K3 复检 完成（两轮：第 1 轮「需修改」2 项→修复；第 2 轮「通过」）→⑦经确认提交 完成（门三 2026-08-22 机主签收，提交 `7939b38`，15 文件）→⑧实机回归 完成（2026-08-22 通过，见下）。流程依据：`docs/maintenance/维护与重构标准流程.md` C 升级流程。

- **需求**（玩家「一般通」2026-08-22 反馈 22 条，整理评估见 `docs/analysis/v2.9_用户反馈整理与评估_2026-08-22.md`；机主 2026-08-22 门一拍板＝批次 1）：
  - B7：纯沿海国家「扩建全国民用工业体系」按钮不亮——`PRC_OCS_queue_civilian_industry`（decisions available 与 construction 效果两处）要求沿海州先完成船厂目标（`PRC_OCS_coastal_dockyards_queued`），无提示。
  - T1：「预设学说」观感改版——陆军总学说由人海系改为决战计划/优势火力系，空军分支按玩家点名调整，「预设军种精神」联动。
  - T2：「预设 MIO 路线」修正——支援装备制造商研究项目→高效扩展；突击炮制造商改良型高爆穿甲弹→改良型高爆弹药；长航程飞机制造商补先进生产技术＋长程战斗机。
- **不改**：潜艇 MIO 隐蔽线（D12 另拍）；F1–F5/F8/F12–F17 新功能与 C1–C8 重开项（批次 2–5）；红线全部不变。
- **③轻调研结论**（本机原版 1.19.2.0 `F:\SteamLibrary\steamapps\common\Hearts of Iron IV` 实文件核对，2026-08-22）：
  - B7：`PRC_OCS_decisions.txt` 199–224 与 `PRC_OCS_construction_effects.txt` 278–287 两处 `OR = { NOT = { is_coastal = yes } has_state_flag = PRC_OCS_coastal_dockyards_queued }` 确认；纯沿海国家（英/日等）必须先把船厂点到 20 级目标才亮民用按钮。
  - T1 键表（原版 `common/doctrines/`）：陆军 grand＝`new_mobile_warfare`/`superior_firepower`/`grand_battleplan`/`mass_assault`；步兵分支 sub＝`commandos`（卓越单兵）/`large_unit_tactics`（大规模部队战术）/`peoples_war`（人民民兵）；作战分支 sub＝`infiltration_tactics`（渗透战术）/`grand_assault`（大规模强袭）/`guerilla_war`（游击战）；空军 sub＝`air_subdoctrine_fighter_bombers`（战斗轰炸机）/`air_subdoctrine_flying_artillery`（空中火炮）/`air_subdoctrine_heavy_aircraft_focus`（重型飞行器重心）；原版中文名核对自 `doctrines_l_simp_chinese.yml`。军种精神（原版 `common/ideas/army_spirits.txt`）：`superior_firepower_army_spirit`/`grand_battleplan_army_spirit`（available 仅 has_doctrine，安全）；`superior_firepower_academy_spirit`/`meticulous_preparation_spirit` 需 `has_military_academy = yes`（不采用）；`direct_fire_support_army_spirit` available＝`superior_firepower`∨`self_propelled_support`（保留 `self_propelled_support` 即继续有效）。
  - T2 特质键（原版 `00_generic_organization.txt`＋中文本地化）：「研究项目」=`generic_mio_trait_research_program`、「高效扩展」=`generic_mio_trait_efficient_scale_up`、「改良型高爆穿甲弾」=`generic_mio_trait_light_assault_gun_anti_tank_combo`、「改良型高爆弹药」=`generic_mio_trait_light_assault_gun_improved_cannon_stabilization`、「先进生产技术」=`generic_mio_trait_advanced_production_techniques`、「长程战斗机」=`generic_mio_trait_long_range_fighters`；组织类别经 `include` 判定（`analyze_mio_archetypes.py` `Organization.include` 已解析）——`generic_support_equipment_organization`/`generic_assault_guns_organization`/`generic_range_focused_aircraft_organization`。现状抽样：IRQ_support 路线含 `efficient_scale_up` 但多国 support 组织仍含 `research_program`/`private_scientists_program`（研究加成，全科技 MOD 下无用）；HUN_mavag_assault_guns 含 `anti_tank_combo` 无 `improved_cannon_stabilization`；range_focused 类公司路线不一致（GER_focke_wulf 完整、GER_heinkel 属 heavy 类非本类、多国 range_focused 缺两项）。
- **④分步方案**（预计 7 文件、约 150 行＋生成产物重建；门二判定：文件数超 5 → 必设门二）：
  1. 步骤1（B7）：`PRC_OCS_decisions.txt`＋`PRC_OCS_construction_effects.txt` 删除民用决议两处船厂前置 OR（保留石化前置）；本地化 `PRC_OCS_queue_civilian_industry_available_tt` 文案按新条件核对微调（如提船厂则删）。
  2. 步骤2（T1）：`PRC_OCS_research_effects.txt` `PRC_OCS_configure_doctrines_effect`——grand 增 `grand_battleplan`＋`superior_firepower`、撤 `mass_assault`；sub 撤 `peoples_war`/`guerilla_war`，按机主二选一入 `commandos`/`large_unit_tactics` 与 `infiltration_tactics`/`grand_assault`；空军 `fighter_central_field`→`fighter_bombers`、`naval_strike_tactics`→`flying_artillery`、`open_ocean_air_patrol`→`heavy_aircraft_focus`（`tactical_battlefield_support` 保留）；`PRC_OCS_configure_spirits_effect`——撤 `mass_assault_academy_spirit`/`living_off_the_land_army_spirit`，入 `superior_firepower_army_spirit`＋`grand_battleplan_army_spirit`＋（二选一：`reserve_officers_spirit`[优势火力系] 或 `logistical_focus_spirit`[决战计划系]）；`self_propelled_support`/`tank_destroyer_force`/海军 sub/`marines_2` 保留（`direct_fire_support_army_spirit` 因保留 `self_propelled_support` 继续有效）。
  3. 步骤3（T2）：`generate_universal_mio_effect.py` 按 `Organization.include` 加类型偏好（maximum_legal_route 之后修正）：support 类排除 `research_program`/`private_scientists_program`、确保 `efficient_scale_up`；assault_guns 类确保 `light_assault_gun_improved_cannon_stabilization`、排除 `light_assault_gun_anti_tank_combo`；range_focused_aircraft 类确保 `advanced_production_techniques`＋`long_range_fighters`（合法时；合法性由 `validate_selected` 兜底）；重建 `PRC_OCS_shared_mio_effects.txt`＋`mio_generator_manifest.json`。
  4. 步骤4（契约与版本）：`test_validate_mod.py` 补 B7 契约（民用决议 available 不含船厂前置）、T1 契约（doctrines/spirits 新键在、旧键不在）；升 `2.9-test1`（descriptor＋packaging 同步）；`docs/testing/README_v2.9-test1_测试包.md` 与实机回归清单（建后回填）。
  5. 步骤5 构建：`build_release.py` 测试包＋SHA。
  - **验收**：静态四步门禁全绿（validate/unittest/mio --check/build）＋新契约通过；实机回归（机主/玩家）＝沿海国家民用按钮可点、学说面板显示新分支、随机 3 国 MIO 路线含新偏好、error.log 无新签名。
  - **回滚**：`git revert` 本批提交回基线；共享表/自检产物重跑工具还原；`dist/` 不入库。
  - **门二待拍板**：①T1 步兵分支取 `commandos`（卓越单兵，推荐）还是 `large_unit_tactics`（大规模部队战术）；②T1 作战分支取 `infiltration_tactics`（渗透战术，推荐）还是 `grand_assault`（大规模强袭）；③T1 精神替换取 `reserve_officers_spirit`（优势火力系，推荐）还是 `logistical_focus_spirit`（决战计划系）；④版本线确认 `2.9-test1`；⑤T2 潜艇项仍待 D12。
- **门二结论（2026-08-22 机主拍板）**：按推荐方案全批——commandos＋infiltration_tactics＋reserve_officers_spirit；版本线 2.9-test1；潜艇项待 D12。另拍板 **D13**：MIO 产物生成基准接受本机原版 1.19.2.0（d245 构建）——本机与家用机（7a45 构建）特质数据存在差异（含生成器浮点累加顺序非确定性根因修复），56 处等强度互斥换边随本批重建吸收。
- **⑤小步实现（五步，静态门禁全绿）**：
  - 步骤1（B7）：`PRC_OCS_decisions.txt`＋`PRC_OCS_construction_effects.txt` 删除民用决议两处船厂前置 OR（保留石化前置）；双语 yml 的 desc 与 `available_tt` 同步删「沿海州还需船厂」表述（期间发现 edit 工具丢 BOM，已按红线 7 恢复两份 yml BOM）。
  - 步骤2（T1）：`PRC_OCS_research_effects.txt`——grand 撤 `mass_assault`、增 `grand_battleplan`＋`superior_firepower`；sub 撤 `peoples_war`/`guerilla_war`/`air_subdoctrine_fighter_central_field`/`air_subdoctrine_naval_strike_tactics`/`air_subdoctrine_open_ocean_air_patrol`，入 `commandos`/`infiltration_tactics`/`air_subdoctrine_fighter_bombers`/`air_subdoctrine_flying_artillery`/`air_subdoctrine_heavy_aircraft_focus`；spirits 撤 `mass_assault_academy_spirit`/`living_off_the_land_army_spirit`，入 `superior_firepower_army_spirit`/`grand_battleplan_army_spirit`/`reserve_officers_spirit`（`direct_fire_support_army_spirit` 因保留 `self_propelled_support` 继续有效，未动）。
  - 步骤3（T2）：`generate_universal_mio_effect.py` 新增 `_parent_chain`＋`apply_org_type_preferences`（按 include 链识别三类组织：support 排除 `research_program`/`private_scientists_program` 并确保 `efficient_scale_up`；assault_guns 确保 `light_assault_gun_improved_cannon_stabilization`、排除 `light_assault_gun_anti_tank_combo`；range_focused 确保 `advanced_production_techniques`＋`long_range_fighters`；drop 后 prune 清理依赖；validate 失败回退原路线）；偏好应用于全部路线（含样本路线，用户可见修正跨国家一致）；**确定性修复**：`maximum_legal_route` 分数改为 `sorted(selected)` 累加（原 set 迭代顺序使浮点和依赖 PYTHONHASHSEED，跨进程/跨机选边漂移——三进程 hash 实测一致）、`order_route` pending 迭代 sorted；重建共享表 444 机构/4877 特质＋manifest（上一基准 4944，差异＝研究线移除与等强度换边）。
  - 步骤4（契约与版本）：`test_validate_mod.py` 新增 `test_v29_batch1_contracts`（B7 民用决议块无船厂 flag、T1 学说/精神键正反断言、T2 IRQ_support/HUN_mavag/GER_focke_wulf 路线断言＋全表无 `research_program`）；既有契约计数更新 4944→4877、分国特质数（GER 313/ENG 177/JAP 259/SOV 191/AST 164/CZE 176/ITA 198/USA 194/PRC 81）；`descriptor.mod`＋`packaging/OCS_one_click_sandbox_start_v2_0.mod` 升 `2.9-test1`（冻结测试自动豁免）；新建 `docs/testing/README_v2.9-test1_测试包.md` 与 `v2.9-test1_实机回归清单.md`。
  - 步骤5 构建：`tools/build_release.py` 通过（validate 0 警告 / 清单 31 文件），测试包 `dist\开局一键爽玩_v2.9-test1_测试版.zip` SHA-256 `81287ab3ed918086ea97d55e545ac8ddaabedc82445ef9a66319f890fcd61d45`（包外 SHA256 文件同值）；D14 放宽电网链后重建，最终 SHA-256 `dc53ffd3bcfd90b231fcafbcadb06a2a2b815a8928cd002eaf8575adaf753ca9`。
  - 门禁留底：validate 0 警告 / unittest **38** 项 OK（含新契约）/ mio --check **一致**（三进程确定性实测 hash 相同）。
- **⑥K3 复检（kimi CLI `kimi-code/k3`；注：本机 CLI 0.29.1 模型别名为 `kimi-code/k3`，流程文档旧口径 `moonshot/k3` 以当时可用者为准）**：
  - 第 1 轮「需修改」2 项（低 severity，B7/T1/T2/版本线/契约/红线本体全过、门禁复跑全绿）：①`docs/analysis/v2.9_用户反馈整理与评估_2026-08-22.md:56` 键名 `grand_battle_plan` 与实现/原版 `grand_battleplan` 不一致（多一个下划线）——已修为 `grand_battleplan`；②`PRC_OCS_construction_effects.txt` 电网优先级链两支 else_if 仍保留沿海船厂前置（B7 后沿海州民用可完成但电网仍锁）——机主拍板 **D14＝同步放宽**，两处 OR 已删、契约测试扩至 `queue_transport_and_bases_effect` 块、回归清单加观察项 4。
  - 第 2 轮「通过」（2026-08-22）：项①评估文档键名已修正无残留、项②电网链 OR 已删＋契约扩至 transport 块＋回归清单观察项 4 就位；门禁复跑全绿（validate 0 问题 / 38 tests OK / --check 一致 444+4877）；红线全过（无批量重命名、AI 守卫未动、未触原版文件、yml 带 BOM/common 无 BOM、单一 limit、dist 未手改）。
- **⑦经确认提交 未开始（门三待机主签收）**。
- **验收**：静态四步门禁全绿＋新契约通过（已完成）；实机回归项（机主/玩家点验）＝沿海国家民用按钮可点、学说面板显示新分支与新精神、三类 MIO 路线符合偏好、error.log 无新签名（见 `docs/testing/v2.9-test1_实机回归清单.md`）。
- **回滚**：`git revert` 本批提交回基线 `10d1f03`；共享表重跑生成器还原；`dist/` 不入库。
- **⑧实机回归（2026-08-22 通过，归档 `docs/testing/v2.9-test1/`）**：
  - 起点变更：黄金存档 `v2.8test5verdict.hoi4` 本机缺失（v2.8-test5 轮实测环境为 a729d47bd，家用机侧；登记表位置核无此档）→ 机主拍板**新开 PRC 局**（1936.1.1）跑全序列；旧档 `PRC_1936_01_01_12.hoi4`（v2.5 期）已置一键开局 flag 且缺 v2.6+ 菜单，弃用。
  - 三会话闭环：第一会话（新局全序列）自检 36/51——15 个选择组未点选（menu 48 陆军 13 组：堡垒网络 3＋陆地巡洋舰 8＋超重型榴弹炮 2；menu 52 火箭 2 组：飞弹/弹道导弹），非 MOD 缺陷；error.log 5 条 `Invalid event id`（加载旧档探索阶段产物，非正式序列）→ 机主补点 15 组（判定档 b，SHA `628a73db...`，补点会话 error.log 0 字节）→ 复检会话自检 **51/51 全 PASS**、error.log 0 字节，check_logs 判定 PASS（exit 0）。
  - 判定档：`save v29test1verdictc.hoi4`（SHA-256 `ac7abd19183e70db740d203bbcde70cda3ae04689a0552312ddb50b43069951e`）。
  - 机主口述点验（2026-08-22，界面截图按机主拍板豁免）：B7 沿海民用按钮亮起/排队/tooltip 无船厂（ENG）、D14 电网（PRC 沿海州）、T1 学说面板（决战计划＋优势火力、卓越单兵/渗透战术、空军三分支、军种精神）、T2 三国 MIO 路线（IRQ 支援装备/HUN 毛沃格/GER 福克-沃尔夫）全部符合预期。
  - 结论：**实机回归通过**；测试包 `dist\开局一键爽玩_v2.9-test1_测试版.zip`（SHA `dc53ffd3...`）待转玩家复测后定正式版。

### 4.13 v2.9 变更记录 #9（C 升级流程，2026-08-22 起，批次 2）

状态行：①对齐需求 完成（门一已拍板 2026-08-22：**全批**——S1 两级拆分＋F1 稳定度/战争支援＋D12 潜艇 MIO 隐蔽线；版本线 2.9-test2；D1 收口＝按用户粒度拆两级不追加子项）→②立基线 完成（本记录草稿；静态门禁留底＝HEAD `11346fe` validate 0 警告 / unittest 38 OK / mio --check 一致 444+4877）→③轻调研 完成（见下）→④方案落盘 完成（门二已拍板 2026-08-22：按推荐方案全批——库存可重复＋装备设计一次性 flag＋D12 互斥根全链扩散；版本线 2.9-test2）→⑤小步实现 完成（七步，见下实施记录；门禁：validate 0 警告 / unittest **39** OK / mio --check 一致 444+**4858** / 构建 SHA `60d2ec5e...`）→⑥K3 复检 完成（五轮：1「需修改」2条→2「R2-1」→3「R3-1」→4「R4-1」→5「通过」，见下）→⑦经确认提交 未开始。流程依据：`docs/maintenance/维护与重构标准流程.md` C 升级流程。

- **需求**（依据 `docs/analysis/v2.9_用户反馈整理与评估_2026-08-22.md` 批次 2 规划＋机主门一拍板）：
  - S1-a：从「启动一键爽玩开局」拆出**获得巨量装备库存**独立按钮（用户标注重要——库存混乱＋CPU 负担，原话：AI 斗蛐蛐每人十几万飞机卡死）：`PRC_OCS_add_stockpile_effect`（PRC）／`PRC_OCS_add_generic_stockpile_effect`（通用），每类装备 100 万。
  - S1-b：拆出**创建预设装备设计**独立按钮（用户标注一般——制造列表混乱）：`PRC_OCS_create_variants_effect`＋`PRC_OCS_create_templates_effect`（PRC）／generic 版。
  - F1：**稳定度/战争支援直接各 +500** 独立决议（用户明确「必定是满的」；独立于一键开局，不干扰内战玩法）。
  - D12：潜艇 MIO 隐蔽线重开（T2 第 4 条：「右侧鱼雷侧科技整体改左侧隐蔽侧科技」；4.1.1 曾于 2026-08-03 拍板关闭，F7 后生成器已重构，机主本批拍板重开）。
  - 兼容性（评估文档 §七）：拆出按钮独立可用、不受 `PRC_OCS_initialized` 影响；初始化总按钮保留「科技＋学说＋精神＋MIO」核心；已初始化存档不破坏。
- **③轻调研结论**（2026-08-22）：
  - S1：`PRC_OCS_effects.txt` `PRC_OCS_initialize_effect`——PRC 分支 3 行（variants/templates/stockpile）与 else 分支 3 行（generic 版）可整体剥离为独立 effect 调用；`add_equipment_to_stockpile` 重复调用无害（累加）；`create_equipment_variant` 同名变体重复创建行为未定（需实机观察或一次性 flag 规避）。
  - F1：原版 effect `add_stability`/`add_war_support` 单位＝小数（0.01＝1 点）；`add_stability = 5`＋`add_war_support = 5`（500 点）溢出即拉满。
  - D12：原版 6 个潜艇组织（AUS_ELIN/GER_hdw/IRQ_submarine/ITA_cantieri_navali_tosi/JAP_mitsubishi_kobe_shipyard/USA_electric_boat_company，全部 include `generic_submarine_organization`）；`00_generic_organization.txt` 内该组织 trait 树双 header：`mio_header_stealth_focus`（隐蔽侧，x=2）vs `mio_header_supremacy_focus`（鱼雷侧，x=5）；互斥根 `generic_mio_trait_long_range_raiding`（隐蔽，sub_visibility）↔ `generic_mio_trait_decalin_fueled_torpedo`（鱼雷，torpedo_attack），两侧子链可沿 `all_parents`/`any_parent` 从互斥根扩散自动提取（避免硬编码遗漏）；与 F7「最强路线」冲突处理＝对 submarine 类直接 drop 鱼雷侧＋ensure 隐蔽侧（优先级高于评分）。
- **④分步方案（草案，预计 12–14 文件；门二判定：文件数超 5 → 必设门二）**：
  1. 步骤1（S1-a 库存按钮）：`PRC_OCS_decisions.txt` 新增 `PRC_OCS_add_stockpile` 决议（is_ai=no＋ai_will_do=0；不依赖 initialized；complete_effect＝if PRC → `add_stockpile_effect` / else → `add_generic_stockpile_effect`）；双语本地化（name/desc/available_tt）。
  2. 步骤2（S1-b 装备设计按钮）：新增 `PRC_OCS_create_preset_designs` 决议（同上结构，PRC → variants＋templates / 通用 → generic 版）；一次性 flag `PRC_OCS_preset_designs_created`（防同名变体重复创建）；双语本地化。
  3. 步骤3（F1）：新增 `PRC_OCS_max_stability` 决议（`add_stability = 5`＋`add_war_support = 5`，可重复）；双语本地化。
  4. 步骤4（S1 剥离）：`PRC_OCS_initialize_effect` PRC/else 分支各删 3 行（variants/templates/stockpile 相关），总按钮保留核心。
  5. 步骤5（D12）：`generate_universal_mio_effect.py` `apply_org_type_preferences` 扩展 submarine 类（include 链含 `generic_submarine_organization`）——从原版 trait 树按互斥根扩散提取两侧清单，drop 鱼雷侧＋ensure 隐蔽侧（validate 失败回退）；重建共享表＋manifest。
  6. 步骤6（契约与版本）：`test_validate_mod.py` 新增 `test_v29_batch2_contracts`（三新决议存在＋守卫断言＋initialize 剥离断言＋潜艇 MIO 路线断言：GER_hdw/AUS_ELIN 含 `long_range_raiding` 无 `decalin_fueled_torpedo`）；升 `2.9-test2`（descriptor＋packaging）；`docs/testing/README_v2.9-test2_测试包.md`＋回归清单。
  7. 步骤7 构建：`build_release.py` 测试包＋SHA。
- **验收**：静态四步门禁全绿＋新契约通过；实机回归＝新按钮独立可用（含旧档）、F1 拉满、潜艇 MIO 路线隐蔽侧、error.log 无新签名。
- **回滚**：`git revert` 本批提交回基线 `11346fe`；共享表重跑生成器还原；`dist/` 不入库。
- **门二结论（2026-08-22 机主拍板）**：按推荐方案全批——库存可重复＋装备设计一次性 flag＋D12 互斥根全链扩散；版本线确认 2.9-test2。
- **⑤小步实现（七步，静态门禁全绿）**：
  - 步骤1–3（S1-a/S1-b/F1）：`PRC_OCS_decisions.txt` 新增 `PRC_OCS_add_stockpile`（可重复、不依赖 initialized、PRC/通用双分支）与 `PRC_OCS_create_preset_designs`（一次性 flag `PRC_OCS_preset_designs_created` 仅 visible NOT＋complete_effect set、if/else 外执行）与 `PRC_OCS_max_stability`（`add_stability = 5`＋`add_war_support = 5`，可重复）；双语 6 键本地化（python 保留 BOM，键插 initialize 前）。
  - 步骤4（S1 剥离）：`PRC_OCS_effects.txt` `PRC_OCS_initialize_effect` PRC 分支删 variants/templates/stockpile 3 行、else 分支删 generic 3 行，保留核心四调用＋顾问/MIO/flag/event；resupply 保留 add_stockpile（需 initialized 的旧入口，向后兼容）。
  - 步骤5（D12）：`generate_universal_mio_effect.py` 新增 `SUBMARINE_STEALTH_TRAITS`/`SUBMARINE_TORPEDO_TRAITS`（8+10=18，与原版 generic_submarine_organization 的 18 个 generic trait 精确对应）与 `check_submarine_side_coverage`（覆盖校验兜底原版更新）；`apply_org_type_preferences` 增 is_submarine 分支（drop 鱼雷侧/ensure 隐蔽侧）；**ensure 改 fixpoint 循环**（anechoic 的 any_parent 依赖排序靠后的 highly_efficient，单轮会漏）；重建共享表 444 机构/**4858** 特质（上版 4877，潜艇 6 家鱼雷侧→隐蔽侧净值 −19：AUS_ELIN/GER_hdw/IRQ_submarine/ITA_cantieri/JAP_mitsubishi/USA_electric_boat 全部 stealth 8/8、torpedo 0）＋manifest。
  - 步骤6（契约与版本）：`test_validate_mod.py` 新增 `test_v29_batch2_contracts`（三决议存在＋is_ai/ai_will_do 守卫＋add_stockpile/max_stability 无 has_country_flag＋initialize 剥离断言＋6 家潜艇组织 stealth 全含 torpedo 全无；route_traits 用增强正则兼容 DLC 守卫嵌套 limit）；`one_time_flags` 登记 `PRC_OCS_create_preset_designs`；MIO 计数 4877→4858、分国 GER 313→310/JAP 259→256/ITA 198→195/USA 194→190；`descriptor.mod`＋packaging 升 `2.9-test2`；新建 `docs/testing/README_v2.9-test2_测试包.md` 与 `v2.9-test2_实机回归清单.md`。
  - 步骤7 构建：`build_release.py` 通过（validate 0 警告 / 清单 31 文件），测试包 SHA 历次 `afb6ca17`→`49dfd8a2`→`dd157452`→`b7a5c839`→终版 **`60d2ec5e53db95ffc74005b867141a84090e08a96e7fbcc42638eed672ac8853`**（K3 文案修复逐轮重建）。
- **⑥K3 复检（kimi CLI `kimi-code/k3`，五轮）**：
  - 第 1 轮「需修改」2 条（同类：S1 剥离后 initialize 相关文案未同步）：①`initialize_desc`（英/中各 1 处）仍称「同时创建预设装备设计、8个师级编制和巨量装备库存」——改为指向独立决议；②`PRC_OCS.1.d`/`.7.d`（英/中各 2 处）事件文案仍称设计/编制/库存已建立——改为仅核心完成＋提示独立决议。已修（python 保 BOM）。
  - 第 2 轮「需修改」1 条（R2-1）：`PRC_OCS_raise_24_infantry_divisions_desc`（英/中）把编制归因 initialize——改为指向「创建预设装备设计」决议。已修。
  - 第 3 轮「需修改」1 条（R3-1）：英文 raise_24_desc 值内嵌入未转义 ASCII 双引号（HOI4 yml 解析截断）——修复时只删了开引号。
  - 第 4 轮「需修改」1 条（R4-1）：`Designs"` 闭引号仍残留（整行 3 引号）——修复验证逻辑误用贪婪正则；已改为按 `Designs" decision` 精确替换并整行计数验证（现整行恰 2 引号）。
  - 第 5 轮「通过」（2026-08-22）：R4-1 修复复核通过（整行 2 引号、句意完整含 AI 不可用与可重复提示）；四轮修复一致性通过（events/ 无残留）；门禁复跑全绿（validate 0 警告 / unittest 39 OK / 构建 SHA `60d2ec5e...` 逐位一致）；红线全过。
  - 非阻断提示（K3 第 3 轮附记，记档待评估）：`PRC_OCS_raise_24_infantry_divisions` 的 available 未要求 `PRC_OCS_preset_designs_created`——玩家未先建预设设计即组建 24 师时模板不存在，事件 PRC_OCS.11 仍宣告完成；建议后续批次评估补前置。
- **⑦经确认提交 未开始（门三待机主签收）**。

### 4.14 v2.9 变更记录 #10（C 升级流程，2026-08-22 起，批次 3）

状态行：①对齐需求 完成（门一已拍板 2026-08-22：**全批＋通用化**——F12 全国军工厂＋F5 加速建设＋F4 建设扩展＋F3 法案＋F2 意识形态；D2＝电网/意识形态通用化到所有玩家国家；D7＝按用户点名建筑清单；版本线 2.9-test3；机主拍板「不实机、不复测，批次 3–5 全做完后统一实机回归＋玩家复测＋定版」）→②立基线 完成（本记录草稿；静态门禁留底＝HEAD `c609ff4` validate 0 警告 / unittest 39 OK / mio --check 一致 444+4858）→③轻调研 完成（见下）→④方案落盘 完成（门二已拍板 2026-08-22：按推荐全批——F2 支持度＋set_ideology 尝试、F5 点名 5 类＋基建 8 类、电网通用化、版本线 2.9-test3）→⑤小步实现 完成（七步，见下实施记录；门禁：validate 0 警告 / unittest **40** OK / mio --check 一致 444+4858 / 构建 SHA `6514d248...`）→⑥K3 复检 完成（两轮：第 1 轮「需修改」4 项＋3 非阻断已修，第 2 轮「通过」，见下）→⑦经确认提交 未开始。流程依据：`docs/maintenance/维护与重构标准流程.md` C 升级流程。

- **需求**（依据 `docs/analysis/v2.9_用户反馈整理与评估_2026-08-22.md` §七.3 批次 3 规划＋机主门一拍板）：
  - F12：一键铺设全国军工厂（已核实缺失，高需求简单项；复用船坞模式）。
  - F5：一键加速建设（用户明确反对铺满，只做加速 modifier；清单＝用户点名 5 类：铁路/雷达站/火箭基地/要塞/海岸炮＋常用基建）。
  - F4：建设扩展——要塞群（原版 `bunker`，与 v2.4 移除 mod 自加陆地 bunker 不冲突）、核反应堆（`nuclear_reactor`）、民用核反应堆（`commercial_nuclear_reactor`）、强化/大容量电网（`energy_infrastructure`/`industrial_infrastructure`，**D2 通用化**）、海军补给中心（`naval_supply_hub`，D7 语义已澄清＝该建筑键）。
  - F3：一键换法案（经济/外贸/征兵三组，可反复点随便改——用户 14 条明确）。
  - F2：意识形态支持度拉满＋更换（四大意识形态；「更换」无原版 effect 先例，方案待门二）。
- **③轻调研结论**（2026-08-22，原版 1.19.2.0 `F:\SteamLibrary` 实文件核对）：
  - 建筑键：`arms_factory`/`bunker`（陆地要塞）/`commercial_nuclear_reactor`/`nuclear_reactor`/`energy_infrastructure`/`industrial_infrastructure`/`naval_supply_hub`/`rail_way`/`radar_station`/`rocket_site`/`coastal_bunker`/`supply_node` 全部存在（00_buildings.txt）；`land_fort`/`civilian_nuclear_reactor`/`naval_supply_node` 不存在（1.19 键名分别为 bunker/commercial_nuclear_reactor/naval_supply_hub）。
  - 加速模板：`PRC_OCS_boost_supply_construction` 决议级 modifier（`production_speed_supply_node_factor = 10`，days_remove=365）现成；原版 modifier 键确认：`production_speed_rail_way_factor`/`radar_station`/`rocket_site`/`coastal_bunker`/`bunker`/`infrastructure`/`air_base`/`naval_base`/`arms_factory`/`industrial_complex`/`dockyard`/`energy_infrastructure`/`industrial_infrastructure`/`supply_node` 均存在。
  - 法案 idea 键（_economic.txt/_manpower.txt）：经济 `undisturbed_isolation`/`isolation`/`civilian_economy`/`low_economic_mobilisation`/`partial_economic_mobilisation`/`war_economy`/`tot_economic_mobilisation`/`totaler_krieg_economy`/`national_defense_state`；外贸 `uncontrolled_exports`/`free_trade`/`export_focus`/`limited_exports`/`autarkic_economy`/`autarkic_economy_improved`/`closed_economy`/`special_economic_zones`；征兵 `disarmed_nation`/`volunteer_only`/`limited_conscription`/`extensive_conscription`/`service_by_requirement`/`all_adults_serve`/`scraping_the_barrel`。
  - F2：支持度 effect 确认 `add_popularity = { ideology = X popularity = N }`（原版 decisions 大量先例）；**国体切换 effect（set_ideology 等）在原版 events/decisions 无任何先例**——原版切国体走 popularity 机制，非民主国家不因 popularity 切换；切换实现风险高，方案待门二（尝试 vs 留档）。
  - 电网现状：`PRC_OCS_queue_transport_and_bases_effect` 的电网优先级链（energy/industrial_infrastructure）在 PRC/original_PRC 分支内（406–483 行），通用化＝提到通用层（行为变更，需回归观察）；23 个固定中国补给站（`PRC_OCS_build_target_supply_nodes_effect`）保持 PRC 层。
- **④分步方案（草案，预计 11–13 文件；门二判定：文件数超 5 → 必设门二）**：
  1. 步骤1（F12）：`PRC_OCS_queue_arms_factories` 决议＋effect（every_owned_state＋controlled＋石化前置＋每州 5 座可重复至 20＋flag＋事件；模式照 `queue_coastal_dockyards`）。
  2. 步骤2（F5）：`PRC_OCS_boost_construction` 决议级 modifier（用户点名 5 类＋基建 6 类，`production_speed_*_factor = 10`，days_remove=365，可重复）。
  3. 步骤3（F4）：`PRC_OCS_queue_major_structures` 决议＋effect（bunker 要塞群/nuclear_reactor/commercial_nuclear_reactor/naval_supply_hub）；电网通用化＝`queue_transport_and_bases` 电网链移出 PRC 分支（补给站保持 PRC 层）。
  4. 步骤4（F3）：三个法案决议（经济/外贸/征兵）＋事件菜单（选项＝各组法案，remove 同组＋add 目标，可反复点）。
  5. 步骤5（F2）：`PRC_OCS_max_ideology_support` 决议（`add_popularity` 四大各 +5 溢出拉满）；「更换国体」按门二拍板（尝试 `set_ideology` 或留档）。
  6. 步骤6（契约与版本）：`test_validate_mod.py` 新增 `test_v29_batch3_contracts`；升 `2.9-test3`（descriptor＋packaging）；`docs/testing/README_v2.9-test3_测试包.md`＋回归清单。
  7. 步骤7 构建：`build_release.py` 测试包＋SHA。
- **验收**：静态四步门禁全绿＋新契约通过；统一实机回归（批次 3–5 完成后）＝军工厂排队/加速生效/新建筑排队/法案切换/意识形态拉满＋error.log 无新签名。
- **回滚**：`git revert` 本批提交回基线 `c609ff4`；`dist/` 不入库。
- **门二结论（2026-08-22 机主拍板）**：按推荐全批——F2 支持度拉满＋尝试 `set_ideology`（无原版先例，实机验证 error.log 报错即删）；F5 点名 5 类＋基建 8 类；F4 电网通用化（D2）；版本线确认 2.9-test3。
- **⑤小步实现（七步，静态门禁全绿）**：
  - 步骤1（F12）：`PRC_OCS_queue_arms_factories` 决议＋effect（石化前置＋每州 5 座可重复至 20＋flag＋事件 71；照民用模式）。
  - 步骤2（F5）：`PRC_OCS_boost_construction` 决议级 modifier（rail_way/radar_station/rocket_site/bunker/coastal_bunker/infrastructure/air_base/naval_base/arms_factory/industrial_complex/dockyard/supply_node/energy/industrial_infrastructure 共 14 项 `= 10`，days_remove=365，可重复；空 complete_effect）。
  - 步骤3（F4＋D2）：`PRC_OCS_queue_major_structures` 决议＋effect（bunker 省 10/naval_supply_hub 沿海省 1/nuclear_reactor＋commercial_nuclear_reactor 州 1＋事件 75）；电网优先级链从 `queue_transport_and_bases` 的 PRC 分支移出通用层（决议 available 同步去掉 PRC 限定），23 固定中国补给站保持 PRC 层。
  - 步骤4（F3）：`PRC_OCS_set_economy_law`/`set_trade_law`/`set_conscription_law` 决议→事件菜单 72/73/74（经济 9 项＋不改变、外贸 8 项＋不改变、征兵 7 项＋不改变；选项 remove 同组全部＋add 目标）。
  - 步骤5（F2）：`PRC_OCS_max_ideology_support` 决议（`add_popularity` 四大各 +5 溢出拉满）＋事件 76 菜单（不更换/democratic/fascism/communism/non_aligned，切换选项 `set_ideology` 尝试）。
  - 步骤6（契约与版本）：`test_validate_mod.py` 新增 `test_v29_batch3_contracts`（7 决议守卫、F5 modifier 键、F2 popularity＋事件 76、F3 事件引用 72–74、F12/F4 effect 文本与事件 71/75、电网通用化断言＝PRC 分支无 energy/industrial_infrastructure、事件 71–76 定义与 add_ideas/set_ideology）；**事件 id 14–19 已被特殊科研菜单占用，改分配 71–76**；`descriptor.mod`＋packaging 升 `2.9-test3`；新建 `docs/testing/README_v2.9-test3_测试包.md` 与 `v2.9-test3_实机回归清单.md`。
  - 步骤7 构建：`build_release.py` 通过（validate 0 警告 / 清单 31 文件），测试包 SHA-256 **`6514d2487bd19445e08462d506fee94abf349b6d8c9a0c56876cf34c340cd960`**（K3 第 1 轮修复后重建；初版 `abb55377...`）。
- **⑥K3 复检（kimi CLI `kimi-code/k3`）**：
  - 第 1 轮「需修改」4 项（均为事件重编号引入）＋3 条非阻断建议：①`PRC_OCS_choose_air_special_project_bonuses` 的 complete_effect 被误从事件 14 改 71（v2.6 空军原型菜单回归）——已恢复 14；②海军同名决议 18→75 误改——已恢复 18；③事件 71–76 的 title/desc/option 本地化引用仍用 14–19 系列键（被特殊科研菜单占用）——已整体改为 71–76 系列；④事件 72/73/74/76 的 desc 与选项 d 键冲突（`.d` 同键）——desc 改用 `.desc` 独立键并补双语文案。非阻断：refresh 补 arms_factory 完成行、契约补 14/18 回归与 loc 引用一致性断言、construction_effects 末尾补换行——均已落实。修复后门禁复跑全绿（validate 0 警告 / unittest 40 OK / 构建 SHA `6514d248...`）。
  - 第 2 轮「通过」（2026-08-22）：4+3 项修复全部落地核对（choose_air/choose_naval 恢复 14/18 且事件本体仍在、71–76 loc 引用全系列无串号、72/73/74/76 desc 用 .desc 与选项 d 无冲突、双语 46 键一一对应无重复、契约新断言与实现一致、refresh 补行与文件尾换行正常）；门禁复跑全绿（validate 0 警告 / unittest 40 OK / 构建 SHA `6514d248...` 逐位一致，确定性构建成立）。无新引入问题。

## 5. 踩坑经验（通用条款，跨工具链）

1. **`Expand-Archive` 解压嵌套**：将 ZIP 解压到已存在的同名目录会得到 `根/子目录/内容` 的双层嵌套；启动器按 `.mod` 的 `path` 找 `descriptor.mod` 时会扑空 → MOD 未加载、无决议。正确做法：先删除旧目录，再解压到 `mod` 根目录，并立即用目录列表核对 `descriptor.mod` 层级。
2. **`.mod` 文件 `path`**：仓库约定 `path="mod/OCS_one_click_sandbox_start_v2_0"`，内容目录必须与该相对路径匹配（`mod/OCS_one_click_sandbox_start_v2_0/descriptor.mod`）。启动器层面 `dlc_load.json` 会记录启用状态。
3. **启动器缓存**：`Documents\Paradox Interactive\Hearts of Iron IV\` 下的 `dlc_load.json` / `launcher-v2.sqlite` 记录 MOD 启用与注册；反复删除/重建 MOD 目录后若启动器不识别，可清理相关缓存后重开启动器。
4. **日志读取注意事项**：
   - 编辑器占用日志文件通常不影响读取（共享读锁），但游戏启动时若文件被独占占用可能写不进去，进游戏前建议关闭日志查看窗口。
   - `read_file` / `search_files` 可能返回**缓存旧内容**；判断"最新一轮"要看日志文件时间戳，而不是依赖对话中的历史读取结果。必要时应改用 `python` 直读文件系统绕过缓存。
   - `error.log` 顶部 6 行 `gamelobby.cpp` / `remotefile.cpp` 的 `Failed allocate data buffer` 是游戏本体网络模块噪音，**与本 MOD 无关**，无 MOD 时也会出现。
5. **PowerShell 转义**：默认终端为 PowerShell；`&&` 不是有效语句分隔符（用 `;`），`grep` 命令不存在（用 `findstr`），`$_` / 中文引号在命令行粘贴时会丢字符或报参数空，复杂命令建议写成独立的 `.ps1` 或避免内联。
6. **steamcmd 交互登录**：steamcmd 是交互式控制台程序，AI 执行环境的键盘输入无法实时转发（会读空密码导致 Invalid Password）。正确做法：先跑 `python tools/publish_workshop.py --steamcmd D:\steamcmd\steamcmd.exe --prepare-only` 生成 VDF 与暂存目录，再用 `cmd /c start "OCS Workshop Upload" cmd /K "D:\steamcmd\steamcmd.exe +login <账号> +workshop_build_item D:\steamcmd\hoi4_ocs_workshop.vdf +quit"` 弹出独立窗口，由维护者在该窗口输入密码（**输入不回显属正常**）与 Steam Guard；上传成功从 `D:\steamcmd\logs\workshop_log.txt` 判读 `Upload finished for workshop item 3767025052 : OK` 与 `ManifestID xxx`，再把 Manifest 同步到 AGENTS.md 与 docs。**注意：`+login` 后必须带账号名，否则 steamcmd 直接打印 `Usage: login <username>` 退出**。
7. **工坊简介 VDF 解析与长度**：VDF 的 `description` 若含 ASCII 双引号会提前闭合字符串（报 `got } in key` / `AddStringToPool: key name too long`）；工坊简介文本内的引号应统一用弯引号（与中文段一致）。上传提交阶段若报 `Committing update...ERROR! Failed to update workshop item (Invalid Parameter)`，优先压缩英文简介长度（v2.6 中文 2209 + 英文 2595、合计 4832 字符可通过）；预览图控制在 1MB 内。
8. **steamcmd 路径**：`F:\steamcmd` 是家用机（Codex 维护环境）写入的真实路径；单位机本次安装在 `D:\steamcmd`。两者都是实际路径，按当前机器选用并传给 `publish_workshop.py` 的 `--steamcmd` 参数（该参数天然支持任意盘符）。
9. **仓库根目录**：单位机与家用机的项目地址可以不同，任何脚本都不得写死仓库绝对路径；应从脚本自身位置推导仓库根目录。游戏、Paradox用户目录与steamcmd路径通过参数、`HOI4_VANILLA_PATH`或当前机器自动探测处理。
10. **Steam 客户端订阅缓存**：工坊上传成功后，已登录的 Steam 客户端不一定立即拉取新内容——它可能继续持有旧 manifest 的本地订阅缓存（表现为订阅目录 `steamapps\workshop\content\394360\3767025052\descriptor.mod` 仍为旧版本、进 MOD 仍是旧内容）。此时**不是上传失败**：退出 Steam 客户端后重新登录（或取消订阅再重新订阅）即会拉取新文件。判定上传结果必须看 `D:\steamcmd\logs\workshop_log.txt` 的 `Upload finished ... : OK` 与 ManifestID，以及 Steam API `GetPublishedFileDetails` 的 `time_updated`，而不是看客户端本地缓存。v2.6 实盘：`Upload finished ... OK`（manifest `3609146315690391433`）后客户端缓存仍显示 v2.5，重订阅后变 v2.6 且实机通过。
11. **发布包换行与跨机器复现**：v2.6 官方附件来自单位机混合 LF/CRLF 工作区；官方 SHA-256 `484030065363ab1366604d17e2f92ee114208b2edb7ffe13b001a0172de48ff1` 在该工作区两次一致，但干净标签检出不能字节级复现。统一换行后所有差异文本内容一致。当前 `build_release.py` 与工坊暂存会统一文本为 LF、保留本地化 BOM；v2.6 规范化审计重建 SHA-256 为 `9b4cc601ca9c82e59541665a076f16c972dab9c18d6871677f4887e2df1e7467`。

## 6. 路径核对清单（跨机器差异提示）

以下路径为**单位机专属**，换机器后需按实际调整：

| 用途 | 单位机路径 |
|------|-----------|
| 游戏本体 | `D:\SteamLibrary\steamapps\common\Hearts of Iron IV` |
| 仓库根目录 | 每台机器可不同；脚本从自身位置推导，不设固定盘符 |
| Paradox 用户目录 | `C:\Users\Admin\Documents\Paradox Interactive\Hearts of Iron IV` |
| MOD 安装目录 | `C:\Users\Admin\Documents\Paradox Interactive\Hearts of Iron IV\mod\OCS_one_click_sandbox_start_v2_0` |
| steamcmd（若需上传） | 单位机 `D:\steamcmd`；家用机（Codex 维护环境）记录为 `F:\steamcmd`，两者均为实际路径，按机器选用 |

## 7. 版本与发布状态（显式声明）

- 正式标签：`v2.4`（指向 `1269f84`）、`v2.5`（指向正式包提交，被 v2.6 覆盖）、`v2.6`（附注标签对象 `1ff3aac`，目标提交 `99e7c60`）、`v2.7`（提交 `3a5901a`）与 **`v2.8`（当前正式基准，附注标签指向发布提交 `d4085e7`）**。v2.6 标签保留完整功能源码，但因发布时工作区混合换行，不能字节级复现已发布附件；详见第5节第11条。
- v2.8 正式包：`tools/build_release.py` 构建，SHA-256 `e34f0e462b07e5b7d0f810655eb7a31069a893ae5cb00d2eb60860c074c8b6e9`（31 文件）；GitHub Release v2.8 已发布（资产 `OCS_one_click_sandbox_start_v2.8.zip` digest 一致）。
- Steam 工坊：物品 `3767025052` 已上传 v2.8 正式内容——2026-08-20 15:32 `workshop_log.txt` 记录 `Uploaded new content ( ManifestID 6381266305931940230 )`＋`Upload finished ... : OK`；上一 v2.7 Manifest `8538688044863448269` 已被覆盖。客户端缓存可能滞后（见第 5 节第 10 条），判读以 workshop_log 与 Steam API `time_updated` 为准。
- GitHub Release：自 v2.4 起挂正式包 ZIP 附件（ASCII 文件名 + 中文显示名）。
- 后续发版仍按 `docs/DEVELOPMENT.md` 与 `AGENTS.md` 的发布门禁执行（先定版本号、测试包回归、构建、维护者确认、再打标签/上传）。

## 8. 本机本地副本转正（2026-08-18）

- 本机（工具库主机）clone 全量仓库至 `D:\Workspaces\开局一键爽玩`，基线切至 `agent/cross-machine-governance`（v2.7 标签锚点提交 `3a5901a`、发布收尾提交 `acd82b2`；HEAD 随维护提交前移，以 `origin/agent/cross-machine-governance` 为准）。工具库（tool-library）TL-062~071 本地路径已补全，「本地副本缺失」由 3 库降为 2 库；维护记录自此一律写本 repo，工具库只留指针文档（`docs/方案-开局一键爽玩.md`）。
- **本机即发版环境**：`D:\SteamLibrary\steamapps\common\Hearts of Iron IV`、`D:\steamcmd`、`C:\Users\Admin\Documents\Paradox Interactive\Hearts of Iron IV\mod` 三路径本机全部存在，与第 1.1 节单位机路径一致（第 6 节路径核对清单全命中）；本机可完成 修改→校验→构建→本地部署→实机回归→工坊上传 全链路，无需换机。
- **v2.7 工坊上传实锤**：`D:\steamcmd\logs\workshop_log.txt` 2026-08-03 15:37:21 记录 `Uploaded new content ( ManifestID 8538688044863448269 ) for item 3767025052`＋`Upload finished ... : OK`，与 AGENTS.md 回填的 Manifest 一致；v2.6 Manifest `3609146315690391433` 已于 2026-08-03 被覆盖。
- 2026-08-18 维护轮：`main` 由 `2a0e4d8` 快进至 `912212c` 并推送（开发分支维护提交并回）；`docs/maintenance/测试状态与回归清单.md` 补记 v2.7 稳定线实机回归与正式包记录、`技术实现与通用化边界.md` 更新至 v2.7 完成态。
- 杂散分支 `agent/import-v2-source`、`codex/v2.2-release` 未核实用途，暂不动。

## 9. A 立项：实机监测机制（2026-08-19 起）

- 状态行：①对齐需求（门一**已拍板** 2026-08-19：统一监测体系 / 接受 MOD 内自检套件 / 半自动分工 / 强制可复核，结论见立项文档 1.4）→②调研 完成（外部调研＋本仓盘点＋本机官方文档实测三路）→③再次对齐 完成（方向＝统一监测体系·六模块）→④分步方案 已落盘（立项文档第六节，六步；门二**已拍板** 2026-08-19：照批六步 / 自检随正式包常驻 / 黄金存档哈希入仓 / 文档衔接随批提交，结论见 6.9）→⑤K3 复检 **完成**（kimi moonshot/k3 七轮终审「通过」；第 1–6 轮 14 项需修改＋10 条附记全部落实，逐轮历程见立项文档第七节）→⑥小步实现（**步骤1–5 完成**，实施记录见立项文档第八节：日志判读工具/归档制度/自检套件（50 用例）/存档断言选型（暂缓）/全链路编排；四步门禁全程全绿）→ diff 经 K3 复检（**分块审计＋复核轮终审「通过」**：块1–5 全部修毕并复核全绿，见立项文档第九节）→机主确认（门三）**已拍板**（2026-08-19）→ 提交 `4a978a5` 并已推送远端；推送仅凭「推送」二字。2026-08-20 实机轮（步骤0/3/5 合并）：首跑 50 用例全 PASS→机制两处修正（标记格式/ASCII 存档名）→复跑 check_logs 判 **PASS**，首个按新制度归档轮次 `docs/testing/v2.8-test5/` 落盘；修正提交 `8b51ccc`（K3 复检通过）已推送远端。机主已签收（2026-08-20）；判定档升格黄金存档已登记（`daaf14fb…eeb3`）。待维护者实机项：步骤0/3/5 合并一轮实机（run_regression.ps1 编排，见立项文档第十节）。流程依据：`docs/maintenance/维护与重构标准流程.md` A 全流程。
- 立项文档：`docs/maintenance/实机监测机制_立项与调研.md`（需求/盘点/调研/候选方向/留痕）。
- 机主原话：「现在，我们的监测的机制还不健全，特别是那种需要进游戏实测的，我认为应该有成熟的，固定化的监测机制，需要进行一波调研并吸纳进来。」
- 本批不动 MOD 功能代码；边界（AI 对齐理解，待门一确认）：不改玩家运行时行为、不做无头全自动、监测能力落仓走既有门禁、证据脱敏。
- 关键调研结论速览：引擎自带场景断言测试框架（`tests\*.txt` success/fail 触发器格式＋`scenario_testing.log` 通道＋`test_log`/`test_save_mode` 命令）；console `run`/`effect`/`event`/`testevent`/`savecheck` 可脚本驱动实测（`run` 存在性以本机游戏官方文档 1.19.2 为准，wiki 口径落后；脚本文件路径待实测）；effects `log`＋`debug = yes` meta effect 可写 game.log 做游戏内断言，`list_flags`/`reload` 供查询与热重载；官方调试模式＝`-debug_mode`（launcher-settings.json 内建入口）；存档解析有 rakaly 生态（jomini/hoi4save/rakaly-cli＋PyPI 绑定）成熟库可依；社区静态校验主流＝CWTools（Kaiserreich 版 Validator 已弃用），Irony Mod Manager 可固定 MOD 组合，Pdx-Unlimiter 供人工核对；Millennium Dawn 工作流为大型 MOD 工程化参照；游戏无头自动化无公开先例（大概率不可行）。本机日志资产 23 个、test1–5.hoi4 黄金存档先例；噪音白名单散落 markdown 无机器文件；MOD 现无任何自检埋点。落地排序建议：日志白名单＋JSON 留痕 → 游戏内自检＋固定脚本/黄金存档 → 存档断言＋全链路编排（见立项文档 4.7）。

## 10. A 立项：三扇门带选项机制（2026-08-20 起）

- 状态行：①对齐需求 完成（门一**已拍板** 2026-08-20：照此立项，A 六步全流程；范围＝5 份文档；选项以弹窗呈现）→②调研 完成（弹窗通道实测／工具库制度先例／选项拍板边界先例／推荐项先例／体例，出处见立项文档第二节）→③再次对齐 完成（三问全选推荐项：正文＋弹窗降级编号列表／门三可含提交不含推送／门二门三真门实测＋门一回放）→④方案落盘 完成（门二**已拍板** 2026-08-20：照批方案）→⑤K3 复检 **完成**（kimi CLI moonshot/k3 两轮：第 1 轮「需修改」4 项修毕，第 2 轮终审「通过」）→⑥小步实现（完成：步骤1 标准流程/步骤2 AGENTS.md/步骤3 开工手册/步骤4 留痕，四步门禁全程全绿，diff 经 K3 复检**通过**）→门三**已拍板** 2026-08-20（机主回「按推荐」）→ git commit 已执行 → 已推送远端（机主 2026-08-20 授权「推送」）。流程依据：`docs/maintenance/维护与重构标准流程.md` A 全流程。
- 立项文档：`docs/maintenance/三扇门带选项机制_立项与调研.md`（需求/调研/方案/K3 历程载体）。
- 需求一句话：每次到门 AI 报告同步以弹窗给出 2–4 个建议选项（推荐第一标「（推荐）」，每项一句取舍），机主点选或回「选 n」/「按推荐」即生效、自由回复不锁死；三扇门与全部纪律/门禁/红线不动。
- 门一问答原文：AI 对齐回执 → 机主「给我建议选项」（试点 4 选项）→ 机主「应该是个弹窗」→ 弹窗重发，机主点选「1 照此立项，A 六步全流程（推荐）」。结论见立项文档 1.4。
