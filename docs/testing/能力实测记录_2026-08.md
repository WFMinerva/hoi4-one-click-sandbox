# 本机能力实测记录（2026-08-20，A 立项「实机监测机制」步骤0）

> 定位：步骤0「本机能力实测」与步骤3/5 合并为一轮实机执行（v2.8-test5 轮，见同目录归档）。
> 环境：游戏 1.19.2（code_revisions.log game_hash_short `a729d47bd`），启动方式＝启动器「以调试模式打开游戏」（`-debug_mode`）。

## 五项能力实测逐项结论

| # | 验证项 | 结论 | 证据与说明 |
|---|---|---|---|
| ① | `run` 脚本文件存放位置 | **未实测**（本轮用 `effect PRC <effect>` 直触发兜底，官方文档已确认该命令可用；50 标记两次实测证明兜底路径完全可行） | `run` 位置仍列开放项，后续轮次择机补测 |
| ② | `log` 实际写 game.log 的行为与格式 | **已实测，有出入**：消息串开头的 `[...]` 分组被引擎消费（替换为引擎自身 `[时间][日期][源]` 前缀），其余文本原样写入；`log` 输出行外层另有 `effectbase.cpp:1799` 包装前缀（双前缀共存）；`Conflict Risk` 为 `log` 效果已知噪音（白名单已含） | 修正：自检标记由 `[OCS_TEST] ` 改为无方括号 `OCS_TEST `，复跑验证字面量完整存活（game.log 52 处） |
| ③ | 控制台 `effect`/`event`/`list_flags`/`reload` 在 `-debug_mode` 下可用性 | **部分实测**：`effect` 两轮实测可用（50 标记为证）；`event`/`list_flags`/`reload` 本轮未执行 | 后续轮次补测 `event`（注意：与 `effect` 同轮执行会标记翻倍，须二选一） |
| ④ | 自检事件 `is_triggered_only = yes` 是否影响 error.log | **实测无影响**：两轮 error.log 均仅含白名单 gamelobby/remotefile「Failed allocate data buffer」噪音，无任何自检事件相关错误 | 初证成立；随后续轮次持续观察 |
| ⑤ | 事件级 `hidden` 字段在 1.19.2 的支持性 | **未实测**（自检事件未采用 `hidden`，用 `is_triggered_only`＋无玩家入口达成不可见，见 6.9-2） | 不阻塞；如需采用再补测 |

## 附带实测发现（本轮新增）

1. **中文存档名保存失败**：`save v2.8test5判定` → `_temp.hoi4` 无法 rename（errno 2），判定档滞留 `_temp` 名。→ 归档制度与编排提示已定：判定档/存档名一律 ASCII。
2. **判定档可作为新黄金存档候选**：本轮判定档 `v2.8test5verdict.hoi4`（1936-01-01 全序列完成态）已改名留本机，待机主拍板是否登记为 2.8-test5 期黄金存档（按黄金存档制度六项登记）。
3. **暂停态即可全流程执行**：固定序列＋自检全程可在暂停状态完成（骷髅师 7 天倒计时不流失），实机轮无需等待游戏时间推进。
