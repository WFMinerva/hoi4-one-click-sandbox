# v2.9 正式版静态复核

复核时间：2026-08-23。

- `python tools/validate_mod.py`：通过，0 警告。
- `python -m unittest tools.test_validate_mod`：48 项通过。
- `python tools/generate_universal_mio_effect.py --check --no-vanilla`：锁定产物 444 家机构、4870 项目标特质一致。按当前原版目录全量重算仍有已登记的版本漂移，未越权覆盖锁定产物。
- `python tools/generate_year_tech_effects.py --check`：生成结果一致。
- `powershell -ExecutionPolicy Bypass -File tools/run_cwtools.ps1 -Offline`：0 errors、5 warnings、退出码 0；警告为已审计的规则边界。
- `python tools/build_release.py`：正式包构建结果与 SHA-256 在发布提交完成后记录于仓库外发布日志及维护状态。
- `git diff --check`：通过。

实机证据边界见同目录 `README_v2.9_正式版.md`，静态检查不替代未逐项完成的游戏内回归。
