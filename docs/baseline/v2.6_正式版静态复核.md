# v2.6 正式版静态复核

复核时间：2026-08-02。工具：`python tools/validate_mod.py`、`python -m unittest tools.test_validate_mod` 与 `python tools/build_release.py`。

- 通过：19/19 必需文件存在。
- 通过：17 个脚本文件编码与结构检查。
- 通过：本地化英/简中双文件键对应且均保留 UTF-8 BOM（292 对键）。
- 通过：descriptor.mod 与外层 `.mod` 的版本、名称和支持版本一致（version 2.6）。
- 通过：35 个 scripted effects、23 个玩家决议（较 v2.5 新增陆/核"选择原型奖励"2 个；空军/海军 choice 决议 OR 覆盖 26 组扩展 flag），0 个警告。
- 通过：检查器回归测试 16 项，其中新增：
  - 26 组 reward → 事件 id/flag 映射唯一性与跨度断言（`docs/analysis/v2.6_特殊科研组事件映射.json`）；
  - 48/49/50/51 菜单 z 触发器必须 AND 覆盖该专精全部 reward flag 且 hidden_effect 设置专精完成 flag。
- 通过：444 家公司、4945 项目标特质的生成表可确定性重建。
- 通过：所有玩家决议保留 `visible`、`available` 中的 `is_ai = no` 以及 `ai_will_do = 0`；分类以 `allowed = { always = yes }` 为所有国家实例化；新决议的 `allowed` 不含 `is_ai`，且无 `has_decision` 死引用。
- 通过：发布 ZIP 内嵌清单逐文件复核；连续两次构建的 ZIP SHA-256 一致。

实机验证（维护者，2026-08-02）：v2.6-test2 测试包实机点验通过，未发现功能问题；`error.log` 仅原版网络噪音（6 行 gamelobby/remotefile）、`game.log` 仅原版 Conflict Risk 杂音（1 条）、`setup.log` 正常加载 PRC_OCS（决策分类 1、玩家决议 23、事件 5 文件全加载，其中 `PRC_OCS_choice_events_more.txt` 加载 30 个事件＝26 组＋48–51 菜单 4 个，精确吻合）、`text.log` 0 字节，被认定测试通过。