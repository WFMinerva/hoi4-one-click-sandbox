# v2.8 正式版静态复核

复核时间：2026-08-20。工具：`python tools/validate_mod.py`、`python -m unittest tools.test_validate_mod`、`python tools/generate_universal_mio_effect.py --check` 与 `python tools/build_release.py`。

- 通过：19/19 必需文件存在。
- 通过：20 个脚本文件编码与结构检查。
- 通过：本地化英/简中双文件键对应且均保留 UTF-8 BOM（527 对键）。
- 通过：descriptor.mod 与外层 `.mod` 的版本、名称和支持版本一致（version 2.8）。
- 通过：90 个 scripted effects、28 个玩家决议（较 v2.7 新增「拉满军工机构资金与规模」），0 个警告。
- 通过：检查器回归测试 37 项，其中 v2.8 新增 11 项：`test_v28_agency_training_centers_contract`（B1）、`test_v28_choice_group_tooltips_contract`（B2/B5）、`test_v28_legacy_jet_techs_gated_without_bba_contract`（B3）、`test_v28_choice_group_map_contract`（B4/B6/L1 映射）、`test_v28_cruiser_submarine_mtg_contract`（N1）、`test_v28_nuclear_design_choice_contract`（B6）、`test_v28_maximize_mios_contract`（F6）、`test_v28_mio_trait_strength_contract`（F7）、`test_v28_land_generic_bonuses_contract`（#6）、`test_v28_rocket_decision_contract`（火箭决议 AI 守卫）、`test_v28_shared_mio_dlc_guards_contract`（#7：444 机构守卫计数、354 个含 DLC 守卫、GER_man OR 结构保真、SOV_okmo FROM 豁免裸守卫、PRC 双覆盖）；
  - 自检套件生成一致性契约（51 用例＝8 固定＋43 选择组，含 `mio_shared_chain`）。
- 通过：444 家公司、4944 项目标特质的生成表可确定性重建（`--check` 一致；机构守卫按原版 allowed 生成，多词 DLC 名带引号，PRC 引用双覆盖）。
- 通过：所有玩家决议保留 `visible`、`available` 中的 `is_ai = no` 以及 `ai_will_do = 0`。
- 通过：发布 ZIP 内嵌清单逐文件复核；构建 SHA-256 见发布记录（构建后回填）。
