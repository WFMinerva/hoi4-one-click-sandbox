# v2.5 正式版静态复核

复核时间：2026-08-01。工具：`python tools/validate_mod.py`、`python -m unittest tools.test_validate_mod` 与 `python tools/build_release.py`。

- 通过：19/19 必需文件存在。
- 通过：13 个脚本文件编码与结构检查。
- 通过：本地化英/简中双文件键对应且均保留 UTF-8 BOM。
- 通过：descriptor.mod 与外层 `.mod` 的版本、名称和支持版本一致（version 2.5）。
- 通过：35 个 scripted effects、19 个玩家决议（较 v2.4 新增 2 个间谍决议）、83 对本地化键（较 v2.4 新增 6 对），0 个警告。
- 通过：检查器回归测试 14 项（含新增决议的一次性 flag 登记断言）。
- 通过：444 家公司、4945 项目标特质的生成表可确定性重建。
- 通过：所有玩家决议保留 `visible`、`available` 中的 `is_ai = no` 以及 `ai_will_do = 0`；分类以 `allowed = { always = yes }` 为所有国家实例化；新决议的 `allowed` 不含 `is_ai`，且无 `has_decision` 死引用（规避参考 MOD 的两类日志杂音）。
- 通过：发布 ZIP 内嵌清单逐文件复核；连续两次构建的 ZIP SHA-256 一致。

实机验证（维护者，2026-08-01）：v2.5 正式包加载后逐项点验"一键组建间谍机构"与"一键升满机构部门"，机构创建成功、五大部门全部满级、无等待时间；`error.log` 仅原版网络噪音、`game.log` 仅原版 Conflict Risk 杂音、`setup.log` 正常加载 PRC_OCS 分类/决议/事件且无参考 MOD 痕迹、`text.log` 0 字节，被认定测试通过。