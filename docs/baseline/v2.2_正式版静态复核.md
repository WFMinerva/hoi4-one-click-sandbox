# v2.2 正式版静态复核

复核时间：2026-07-30。工具：`python tools/validate_mod.py` 与 `python -m unittest tools/test_validate_mod.py`。

- 通过：19/19 必需文件存在。
- 通过：12/12 脚本文件编码与结构检查。
- 通过：本地化英/简中双文件键对应且均保留UTF-8 BOM。
- 通过：descriptor.mod 与外层 `.mod` 的版本、名称和支持版本一致（version 2.2）。
- 通过：34个 scripted effects、16个玩家决议、73对本地化键，0个警告。
- 通过：检查器回归测试9项。
- 通过：二阶段精确依赖、装备设计、模板规格、兵牌边界和危险模板删除effect禁用断言。
- 通过：发布ZIP内嵌清单逐文件复核。
- 通过：连续两次构建的ZIP SHA-256一致。

实机验证（维护者，2026-07-30）：完整 `v2.2-test1` 回归清单通过；`error.log` 为0字节，`game.log` 仅见已知原版 `Conflict Risk` 杂音，`setup.log` 正常，`text.log` 为0字节且中文文本正常显示。
