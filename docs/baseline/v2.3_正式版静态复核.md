# v2.3 正式版静态复核

复核时间：2026-07-31。工具：`python tools/validate_mod.py`、`python -m unittest tools.test_validate_mod` 与 `python tools/build_release.py`。

- 通过：19/19 必需文件存在。
- 通过：13个脚本文件编码与结构检查。
- 通过：本地化英/简中双文件键对应且均保留UTF-8 BOM。
- 通过：descriptor.mod 与外层 `.mod` 的版本、名称和支持版本一致（version 2.3）。
- 通过：35个 scripted effects、16个玩家决议、75对本地化键，0个警告。
- 通过：检查器回归测试14项。
- 通过：444家公司、4945项目标特质的生成表可确定性重建。
- 通过：所有玩家决议保留 `visible`、`available` 中的 `is_ai = no` 以及 `ai_will_do = 0`；分类以 `allowed = { always = yes }` 为所有国家实例化。
- 通过：发布ZIP内嵌清单逐文件复核；连续两次构建的ZIP SHA-256一致。

实机验证（维护者，2026-07-31）：确认通用MIO初始化路线、多个主要国家样本及同局切换国家后的分类显示关键路径通过；v2.3-test13被认定测试通过。