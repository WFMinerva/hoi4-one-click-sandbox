# v2.7 正式版静态复核

复核时间：2026-08-03。工具：`python tools/validate_mod.py`、`python -m unittest tools.test_validate_mod`、`python tools/generate_universal_mio_effect.py --check` 与 `python tools/build_release.py`。

- 通过：19/19 必需文件存在。
- 通过：18 个脚本文件编码与结构检查。
- 通过：本地化英/简中双文件键对应且均保留 UTF-8 BOM（374 对键）。
- 通过：descriptor.mod 与外层 `.mod` 的版本、名称和支持版本一致（version 2.7）。
- 通过：36 个 scripted effects、26 个玩家决议（较 v2.6 删「一键训练」试作、新增「一键骷髅师」「开发占领区」「加速补给站建设」），0 个警告。
- 通过：检查器回归测试 11 项，其中新增：
  - `test_v27_skull_divisions_contract`：只有「一键骷髅师」一条训练决议、单 idea 容器结构、`add_timed_idea days=7`、事件 53 存在且事件 52/旧 training 键已移除；
  - `test_v27_choice_tooltips_contract`：组事件 22–47 每选项携带双语 `custom_effect_tooltip` 键。
- 通过：444 家公司、4945 项目标特质的生成表可确定性重建。
- 通过：所有玩家决议保留 `visible`、`available` 中的 `is_ai = no` 以及 `ai_will_do = 0`。
- 通过：发布 ZIP 内嵌清单逐文件复核；两次构建的 ZIP SHA-256 一致。